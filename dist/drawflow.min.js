(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory();
	else if(typeof define === 'function' && define.amd)
		define([], factory);
	else if(typeof exports === 'object')
		exports["Drawflow"] = factory();
	else
		root["Drawflow"] = factory();
})((typeof self !== 'undefined' ? self : this), function() {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 1);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports, __webpack_require__) {

/*!
* sweetalert2 v11.26.3
* Released under the MIT License.
*/
(function (global, factory) {
   true ? module.exports = factory() :
  undefined;
})(this, (function () { 'use strict';

  function _assertClassBrand(e, t, n) {
    if ("function" == typeof e ? e === t : e.has(t)) return arguments.length < 3 ? t : n;
    throw new TypeError("Private element is not present on this object");
  }
  function _checkPrivateRedeclaration(e, t) {
    if (t.has(e)) throw new TypeError("Cannot initialize the same private elements twice on an object");
  }
  function _classPrivateFieldGet2(s, a) {
    return s.get(_assertClassBrand(s, a));
  }
  function _classPrivateFieldInitSpec(e, t, a) {
    _checkPrivateRedeclaration(e, t), t.set(e, a);
  }
  function _classPrivateFieldSet2(s, a, r) {
    return s.set(_assertClassBrand(s, a), r), r;
  }

  const RESTORE_FOCUS_TIMEOUT = 100;

  /** @type {GlobalState} */
  const globalState = {};
  const focusPreviousActiveElement = () => {
    if (globalState.previousActiveElement instanceof HTMLElement) {
      globalState.previousActiveElement.focus();
      globalState.previousActiveElement = null;
    } else if (document.body) {
      document.body.focus();
    }
  };

  /**
   * Restore previous active (focused) element
   *
   * @param {boolean} returnFocus
   * @returns {Promise<void>}
   */
  const restoreActiveElement = returnFocus => {
    return new Promise(resolve => {
      if (!returnFocus) {
        return resolve();
      }
      const x = window.scrollX;
      const y = window.scrollY;
      globalState.restoreFocusTimeout = setTimeout(() => {
        focusPreviousActiveElement();
        resolve();
      }, RESTORE_FOCUS_TIMEOUT); // issues/900

      window.scrollTo(x, y);
    });
  };

  const swalPrefix = 'swal2-';

  /**
   * @typedef {Record<SwalClass, string>} SwalClasses
   */

  /**
   * @typedef {'success' | 'warning' | 'info' | 'question' | 'error'} SwalIcon
   * @typedef {Record<SwalIcon, string>} SwalIcons
   */

  /** @type {SwalClass[]} */
  const classNames = ['container', 'shown', 'height-auto', 'iosfix', 'popup', 'modal', 'no-backdrop', 'no-transition', 'toast', 'toast-shown', 'show', 'hide', 'close', 'title', 'html-container', 'actions', 'confirm', 'deny', 'cancel', 'footer', 'icon', 'icon-content', 'image', 'input', 'file', 'range', 'select', 'radio', 'checkbox', 'label', 'textarea', 'inputerror', 'input-label', 'validation-message', 'progress-steps', 'active-progress-step', 'progress-step', 'progress-step-line', 'loader', 'loading', 'styled', 'top', 'top-start', 'top-end', 'top-left', 'top-right', 'center', 'center-start', 'center-end', 'center-left', 'center-right', 'bottom', 'bottom-start', 'bottom-end', 'bottom-left', 'bottom-right', 'grow-row', 'grow-column', 'grow-fullscreen', 'rtl', 'timer-progress-bar', 'timer-progress-bar-container', 'scrollbar-measure', 'icon-success', 'icon-warning', 'icon-info', 'icon-question', 'icon-error', 'draggable', 'dragging'];
  const swalClasses = classNames.reduce((acc, className) => {
    acc[className] = swalPrefix + className;
    return acc;
  }, /** @type {SwalClasses} */{});

  /** @type {SwalIcon[]} */
  const icons = ['success', 'warning', 'info', 'question', 'error'];
  const iconTypes = icons.reduce((acc, icon) => {
    acc[icon] = swalPrefix + icon;
    return acc;
  }, /** @type {SwalIcons} */{});

  const consolePrefix = 'SweetAlert2:';

  /**
   * Capitalize the first letter of a string
   *
   * @param {string} str
   * @returns {string}
   */
  const capitalizeFirstLetter = str => str.charAt(0).toUpperCase() + str.slice(1);

  /**
   * Standardize console warnings
   *
   * @param {string | string[]} message
   */
  const warn = message => {
    console.warn(`${consolePrefix} ${typeof message === 'object' ? message.join(' ') : message}`);
  };

  /**
   * Standardize console errors
   *
   * @param {string} message
   */
  const error = message => {
    console.error(`${consolePrefix} ${message}`);
  };

  /**
   * Private global state for `warnOnce`
   *
   * @type {string[]}
   * @private
   */
  const previousWarnOnceMessages = [];

  /**
   * Show a console warning, but only if it hasn't already been shown
   *
   * @param {string} message
   */
  const warnOnce = message => {
    if (!previousWarnOnceMessages.includes(message)) {
      previousWarnOnceMessages.push(message);
      warn(message);
    }
  };

  /**
   * Show a one-time console warning about deprecated params/methods
   *
   * @param {string} deprecatedParam
   * @param {string?} useInstead
   */
  const warnAboutDeprecation = (deprecatedParam, useInstead = null) => {
    warnOnce(`"${deprecatedParam}" is deprecated and will be removed in the next major release.${useInstead ? ` Use "${useInstead}" instead.` : ''}`);
  };

  /**
   * If `arg` is a function, call it (with no arguments or context) and return the result.
   * Otherwise, just pass the value through
   *
   * @param {(() => *) | *} arg
   * @returns {*}
   */
  const callIfFunction = arg => typeof arg === 'function' ? arg() : arg;

  /**
   * @param {*} arg
   * @returns {boolean}
   */
  const hasToPromiseFn = arg => arg && typeof arg.toPromise === 'function';

  /**
   * @param {*} arg
   * @returns {Promise<*>}
   */
  const asPromise = arg => hasToPromiseFn(arg) ? arg.toPromise() : Promise.resolve(arg);

  /**
   * @param {*} arg
   * @returns {boolean}
   */
  const isPromise = arg => arg && Promise.resolve(arg) === arg;

  /**
   * Gets the popup container which contains the backdrop and the popup itself.
   *
   * @returns {HTMLElement | null}
   */
  const getContainer = () => document.body.querySelector(`.${swalClasses.container}`);

  /**
   * @param {string} selectorString
   * @returns {HTMLElement | null}
   */
  const elementBySelector = selectorString => {
    const container = getContainer();
    return container ? container.querySelector(selectorString) : null;
  };

  /**
   * @param {string} className
   * @returns {HTMLElement | null}
   */
  const elementByClass = className => {
    return elementBySelector(`.${className}`);
  };

  /**
   * @returns {HTMLElement | null}
   */
  const getPopup = () => elementByClass(swalClasses.popup);

  /**
   * @returns {HTMLElement | null}
   */
  const getIcon = () => elementByClass(swalClasses.icon);

  /**
   * @returns {HTMLElement | null}
   */
  const getIconContent = () => elementByClass(swalClasses['icon-content']);

  /**
   * @returns {HTMLElement | null}
   */
  const getTitle = () => elementByClass(swalClasses.title);

  /**
   * @returns {HTMLElement | null}
   */
  const getHtmlContainer = () => elementByClass(swalClasses['html-container']);

  /**
   * @returns {HTMLElement | null}
   */
  const getImage = () => elementByClass(swalClasses.image);

  /**
   * @returns {HTMLElement | null}
   */
  const getProgressSteps = () => elementByClass(swalClasses['progress-steps']);

  /**
   * @returns {HTMLElement | null}
   */
  const getValidationMessage = () => elementByClass(swalClasses['validation-message']);

  /**
   * @returns {HTMLButtonElement | null}
   */
  const getConfirmButton = () => (/** @type {HTMLButtonElement} */elementBySelector(`.${swalClasses.actions} .${swalClasses.confirm}`));

  /**
   * @returns {HTMLButtonElement | null}
   */
  const getCancelButton = () => (/** @type {HTMLButtonElement} */elementBySelector(`.${swalClasses.actions} .${swalClasses.cancel}`));

  /**
   * @returns {HTMLButtonElement | null}
   */
  const getDenyButton = () => (/** @type {HTMLButtonElement} */elementBySelector(`.${swalClasses.actions} .${swalClasses.deny}`));

  /**
   * @returns {HTMLElement | null}
   */
  const getInputLabel = () => elementByClass(swalClasses['input-label']);

  /**
   * @returns {HTMLElement | null}
   */
  const getLoader = () => elementBySelector(`.${swalClasses.loader}`);

  /**
   * @returns {HTMLElement | null}
   */
  const getActions = () => elementByClass(swalClasses.actions);

  /**
   * @returns {HTMLElement | null}
   */
  const getFooter = () => elementByClass(swalClasses.footer);

  /**
   * @returns {HTMLElement | null}
   */
  const getTimerProgressBar = () => elementByClass(swalClasses['timer-progress-bar']);

  /**
   * @returns {HTMLElement | null}
   */
  const getCloseButton = () => elementByClass(swalClasses.close);

  // https://github.com/jkup/focusable/blob/master/index.js
  const focusable = `
  a[href],
  area[href],
  input:not([disabled]),
  select:not([disabled]),
  textarea:not([disabled]),
  button:not([disabled]),
  iframe,
  object,
  embed,
  [tabindex="0"],
  [contenteditable],
  audio[controls],
  video[controls],
  summary
`;
  /**
   * @returns {HTMLElement[]}
   */
  const getFocusableElements = () => {
    const popup = getPopup();
    if (!popup) {
      return [];
    }
    /** @type {NodeListOf<HTMLElement>} */
    const focusableElementsWithTabindex = popup.querySelectorAll('[tabindex]:not([tabindex="-1"]):not([tabindex="0"])');
    const focusableElementsWithTabindexSorted = Array.from(focusableElementsWithTabindex)
    // sort according to tabindex
    .sort((a, b) => {
      const tabindexA = parseInt(a.getAttribute('tabindex') || '0');
      const tabindexB = parseInt(b.getAttribute('tabindex') || '0');
      if (tabindexA > tabindexB) {
        return 1;
      } else if (tabindexA < tabindexB) {
        return -1;
      }
      return 0;
    });

    /** @type {NodeListOf<HTMLElement>} */
    const otherFocusableElements = popup.querySelectorAll(focusable);
    const otherFocusableElementsFiltered = Array.from(otherFocusableElements).filter(el => el.getAttribute('tabindex') !== '-1');
    return [...new Set(focusableElementsWithTabindexSorted.concat(otherFocusableElementsFiltered))].filter(el => isVisible$1(el));
  };

  /**
   * @returns {boolean}
   */
  const isModal = () => {
    return hasClass(document.body, swalClasses.shown) && !hasClass(document.body, swalClasses['toast-shown']) && !hasClass(document.body, swalClasses['no-backdrop']);
  };

  /**
   * @returns {boolean}
   */
  const isToast = () => {
    const popup = getPopup();
    if (!popup) {
      return false;
    }
    return hasClass(popup, swalClasses.toast);
  };

  /**
   * @returns {boolean}
   */
  const isLoading = () => {
    const popup = getPopup();
    if (!popup) {
      return false;
    }
    return popup.hasAttribute('data-loading');
  };

  /**
   * Securely set innerHTML of an element
   * https://github.com/sweetalert2/sweetalert2/issues/1926
   *
   * @param {HTMLElement} elem
   * @param {string} html
   */
  const setInnerHtml = (elem, html) => {
    elem.textContent = '';
    if (html) {
      const parser = new DOMParser();
      const parsed = parser.parseFromString(html, `text/html`);
      const head = parsed.querySelector('head');
      if (head) {
        Array.from(head.childNodes).forEach(child => {
          elem.appendChild(child);
        });
      }
      const body = parsed.querySelector('body');
      if (body) {
        Array.from(body.childNodes).forEach(child => {
          if (child instanceof HTMLVideoElement || child instanceof HTMLAudioElement) {
            elem.appendChild(child.cloneNode(true)); // https://github.com/sweetalert2/sweetalert2/issues/2507
          } else {
            elem.appendChild(child);
          }
        });
      }
    }
  };

  /**
   * @param {HTMLElement} elem
   * @param {string} className
   * @returns {boolean}
   */
  const hasClass = (elem, className) => {
    if (!className) {
      return false;
    }
    const classList = className.split(/\s+/);
    for (let i = 0; i < classList.length; i++) {
      if (!elem.classList.contains(classList[i])) {
        return false;
      }
    }
    return true;
  };

  /**
   * @param {HTMLElement} elem
   * @param {SweetAlertOptions} params
   */
  const removeCustomClasses = (elem, params) => {
    Array.from(elem.classList).forEach(className => {
      if (!Object.values(swalClasses).includes(className) && !Object.values(iconTypes).includes(className) && !Object.values(params.showClass || {}).includes(className)) {
        elem.classList.remove(className);
      }
    });
  };

  /**
   * @param {HTMLElement} elem
   * @param {SweetAlertOptions} params
   * @param {string} className
   */
  const applyCustomClass = (elem, params, className) => {
    removeCustomClasses(elem, params);
    if (!params.customClass) {
      return;
    }
    const customClass = params.customClass[(/** @type {keyof SweetAlertCustomClass} */className)];
    if (!customClass) {
      return;
    }
    if (typeof customClass !== 'string' && !customClass.forEach) {
      warn(`Invalid type of customClass.${className}! Expected string or iterable object, got "${typeof customClass}"`);
      return;
    }
    addClass(elem, customClass);
  };

  /**
   * @param {HTMLElement} popup
   * @param {import('./renderers/renderInput').InputClass | SweetAlertInput} inputClass
   * @returns {HTMLInputElement | null}
   */
  const getInput$1 = (popup, inputClass) => {
    if (!inputClass) {
      return null;
    }
    switch (inputClass) {
      case 'select':
      case 'textarea':
      case 'file':
        return popup.querySelector(`.${swalClasses.popup} > .${swalClasses[inputClass]}`);
      case 'checkbox':
        return popup.querySelector(`.${swalClasses.popup} > .${swalClasses.checkbox} input`);
      case 'radio':
        return popup.querySelector(`.${swalClasses.popup} > .${swalClasses.radio} input:checked`) || popup.querySelector(`.${swalClasses.popup} > .${swalClasses.radio} input:first-child`);
      case 'range':
        return popup.querySelector(`.${swalClasses.popup} > .${swalClasses.range} input`);
      default:
        return popup.querySelector(`.${swalClasses.popup} > .${swalClasses.input}`);
    }
  };

  /**
   * @param {HTMLInputElement | HTMLTextAreaElement | HTMLSelectElement} input
   */
  const focusInput = input => {
    input.focus();

    // place cursor at end of text in text input
    if (input.type !== 'file') {
      // http://stackoverflow.com/a/2345915
      const val = input.value;
      input.value = '';
      input.value = val;
    }
  };

  /**
   * @param {HTMLElement | HTMLElement[] | null} target
   * @param {string | string[] | readonly string[] | undefined} classList
   * @param {boolean} condition
   */
  const toggleClass = (target, classList, condition) => {
    if (!target || !classList) {
      return;
    }
    if (typeof classList === 'string') {
      classList = classList.split(/\s+/).filter(Boolean);
    }
    classList.forEach(className => {
      if (Array.isArray(target)) {
        target.forEach(elem => {
          if (condition) {
            elem.classList.add(className);
          } else {
            elem.classList.remove(className);
          }
        });
      } else {
        if (condition) {
          target.classList.add(className);
        } else {
          target.classList.remove(className);
        }
      }
    });
  };

  /**
   * @param {HTMLElement | HTMLElement[] | null} target
   * @param {string | string[] | readonly string[] | undefined} classList
   */
  const addClass = (target, classList) => {
    toggleClass(target, classList, true);
  };

  /**
   * @param {HTMLElement | HTMLElement[] | null} target
   * @param {string | string[] | readonly string[] | undefined} classList
   */
  const removeClass = (target, classList) => {
    toggleClass(target, classList, false);
  };

  /**
   * Get direct child of an element by class name
   *
   * @param {HTMLElement} elem
   * @param {string} className
   * @returns {HTMLElement | undefined}
   */
  const getDirectChildByClass = (elem, className) => {
    const children = Array.from(elem.children);
    for (let i = 0; i < children.length; i++) {
      const child = children[i];
      if (child instanceof HTMLElement && hasClass(child, className)) {
        return child;
      }
    }
  };

  /**
   * @param {HTMLElement} elem
   * @param {string} property
   * @param {string | number | null | undefined} value
   */
  const applyNumericalStyle = (elem, property, value) => {
    if (value === `${parseInt(`${value}`)}`) {
      value = parseInt(value);
    }
    if (value || parseInt(`${value}`) === 0) {
      elem.style.setProperty(property, typeof value === 'number' ? `${value}px` : value);
    } else {
      elem.style.removeProperty(property);
    }
  };

  /**
   * @param {HTMLElement | null} elem
   * @param {string} display
   */
  const show = (elem, display = 'flex') => {
    if (!elem) {
      return;
    }
    elem.style.display = display;
  };

  /**
   * @param {HTMLElement | null} elem
   */
  const hide = elem => {
    if (!elem) {
      return;
    }
    elem.style.display = 'none';
  };

  /**
   * @param {HTMLElement | null} elem
   * @param {string} display
   */
  const showWhenInnerHtmlPresent = (elem, display = 'block') => {
    if (!elem) {
      return;
    }
    new MutationObserver(() => {
      toggle(elem, elem.innerHTML, display);
    }).observe(elem, {
      childList: true,
      subtree: true
    });
  };

  /**
   * @param {HTMLElement} parent
   * @param {string} selector
   * @param {string} property
   * @param {string} value
   */
  const setStyle = (parent, selector, property, value) => {
    /** @type {HTMLElement | null} */
    const el = parent.querySelector(selector);
    if (el) {
      el.style.setProperty(property, value);
    }
  };

  /**
   * @param {HTMLElement} elem
   * @param {boolean | string | null | undefined} condition
   * @param {string} display
   */
  const toggle = (elem, condition, display = 'flex') => {
    if (condition) {
      show(elem, display);
    } else {
      hide(elem);
    }
  };

  /**
   * borrowed from jquery $(elem).is(':visible') implementation
   *
   * @param {HTMLElement | null} elem
   * @returns {boolean}
   */
  const isVisible$1 = elem => !!(elem && (elem.offsetWidth || elem.offsetHeight || elem.getClientRects().length));

  /**
   * @returns {boolean}
   */
  const allButtonsAreHidden = () => !isVisible$1(getConfirmButton()) && !isVisible$1(getDenyButton()) && !isVisible$1(getCancelButton());

  /**
   * @param {HTMLElement} elem
   * @returns {boolean}
   */
  const isScrollable = elem => !!(elem.scrollHeight > elem.clientHeight);

  /**
   * @param {HTMLElement} element
   * @param {HTMLElement} stopElement
   * @returns {boolean}
   */
  const selfOrParentIsScrollable = (element, stopElement) => {
    let parent = element;
    while (parent && parent !== stopElement) {
      if (isScrollable(parent)) {
        return true;
      }
      parent = parent.parentElement;
    }
    return false;
  };

  /**
   * borrowed from https://stackoverflow.com/a/46352119
   *
   * @param {HTMLElement} elem
   * @returns {boolean}
   */
  const hasCssAnimation = elem => {
    const style = window.getComputedStyle(elem);
    const animDuration = parseFloat(style.getPropertyValue('animation-duration') || '0');
    const transDuration = parseFloat(style.getPropertyValue('transition-duration') || '0');
    return animDuration > 0 || transDuration > 0;
  };

  /**
   * @param {number} timer
   * @param {boolean} reset
   */
  const animateTimerProgressBar = (timer, reset = false) => {
    const timerProgressBar = getTimerProgressBar();
    if (!timerProgressBar) {
      return;
    }
    if (isVisible$1(timerProgressBar)) {
      if (reset) {
        timerProgressBar.style.transition = 'none';
        timerProgressBar.style.width = '100%';
      }
      setTimeout(() => {
        timerProgressBar.style.transition = `width ${timer / 1000}s linear`;
        timerProgressBar.style.width = '0%';
      }, 10);
    }
  };
  const stopTimerProgressBar = () => {
    const timerProgressBar = getTimerProgressBar();
    if (!timerProgressBar) {
      return;
    }
    const timerProgressBarWidth = parseInt(window.getComputedStyle(timerProgressBar).width);
    timerProgressBar.style.removeProperty('transition');
    timerProgressBar.style.width = '100%';
    const timerProgressBarFullWidth = parseInt(window.getComputedStyle(timerProgressBar).width);
    const timerProgressBarPercent = timerProgressBarWidth / timerProgressBarFullWidth * 100;
    timerProgressBar.style.width = `${timerProgressBarPercent}%`;
  };

  /**
   * Detect Node env
   *
   * @returns {boolean}
   */
  const isNodeEnv = () => typeof window === 'undefined' || typeof document === 'undefined';

  const sweetHTML = `
 <div aria-labelledby="${swalClasses.title}" aria-describedby="${swalClasses['html-container']}" class="${swalClasses.popup}" tabindex="-1">
   <button type="button" class="${swalClasses.close}"></button>
   <ul class="${swalClasses['progress-steps']}"></ul>
   <div class="${swalClasses.icon}"></div>
   <img class="${swalClasses.image}" />
   <h2 class="${swalClasses.title}" id="${swalClasses.title}"></h2>
   <div class="${swalClasses['html-container']}" id="${swalClasses['html-container']}"></div>
   <input class="${swalClasses.input}" id="${swalClasses.input}" />
   <input type="file" class="${swalClasses.file}" />
   <div class="${swalClasses.range}">
     <input type="range" />
     <output></output>
   </div>
   <select class="${swalClasses.select}" id="${swalClasses.select}"></select>
   <div class="${swalClasses.radio}"></div>
   <label class="${swalClasses.checkbox}">
     <input type="checkbox" id="${swalClasses.checkbox}" />
     <span class="${swalClasses.label}"></span>
   </label>
   <textarea class="${swalClasses.textarea}" id="${swalClasses.textarea}"></textarea>
   <div class="${swalClasses['validation-message']}" id="${swalClasses['validation-message']}"></div>
   <div class="${swalClasses.actions}">
     <div class="${swalClasses.loader}"></div>
     <button type="button" class="${swalClasses.confirm}"></button>
     <button type="button" class="${swalClasses.deny}"></button>
     <button type="button" class="${swalClasses.cancel}"></button>
   </div>
   <div class="${swalClasses.footer}"></div>
   <div class="${swalClasses['timer-progress-bar-container']}">
     <div class="${swalClasses['timer-progress-bar']}"></div>
   </div>
 </div>
`.replace(/(^|\n)\s*/g, '');

  /**
   * @returns {boolean}
   */
  const resetOldContainer = () => {
    const oldContainer = getContainer();
    if (!oldContainer) {
      return false;
    }
    oldContainer.remove();
    removeClass([document.documentElement, document.body], [swalClasses['no-backdrop'], swalClasses['toast-shown'], swalClasses['has-column']]);
    return true;
  };
  const resetValidationMessage$1 = () => {
    globalState.currentInstance.resetValidationMessage();
  };
  const addInputChangeListeners = () => {
    const popup = getPopup();
    const input = getDirectChildByClass(popup, swalClasses.input);
    const file = getDirectChildByClass(popup, swalClasses.file);
    /** @type {HTMLInputElement} */
    const range = popup.querySelector(`.${swalClasses.range} input`);
    /** @type {HTMLOutputElement} */
    const rangeOutput = popup.querySelector(`.${swalClasses.range} output`);
    const select = getDirectChildByClass(popup, swalClasses.select);
    /** @type {HTMLInputElement} */
    const checkbox = popup.querySelector(`.${swalClasses.checkbox} input`);
    const textarea = getDirectChildByClass(popup, swalClasses.textarea);
    input.oninput = resetValidationMessage$1;
    file.onchange = resetValidationMessage$1;
    select.onchange = resetValidationMessage$1;
    checkbox.onchange = resetValidationMessage$1;
    textarea.oninput = resetValidationMessage$1;
    range.oninput = () => {
      resetValidationMessage$1();
      rangeOutput.value = range.value;
    };
    range.onchange = () => {
      resetValidationMessage$1();
      rangeOutput.value = range.value;
    };
  };

  /**
   * @param {string | HTMLElement} target
   * @returns {HTMLElement}
   */
  const getTarget = target => typeof target === 'string' ? document.querySelector(target) : target;

  /**
   * @param {SweetAlertOptions} params
   */
  const setupAccessibility = params => {
    const popup = getPopup();
    popup.setAttribute('role', params.toast ? 'alert' : 'dialog');
    popup.setAttribute('aria-live', params.toast ? 'polite' : 'assertive');
    if (!params.toast) {
      popup.setAttribute('aria-modal', 'true');
    }
  };

  /**
   * @param {HTMLElement} targetElement
   */
  const setupRTL = targetElement => {
    if (window.getComputedStyle(targetElement).direction === 'rtl') {
      addClass(getContainer(), swalClasses.rtl);
    }
  };

  /**
   * Add modal + backdrop to DOM
   *
   * @param {SweetAlertOptions} params
   */
  const init = params => {
    // Clean up the old popup container if it exists
    const oldContainerExisted = resetOldContainer();
    if (isNodeEnv()) {
      error('SweetAlert2 requires document to initialize');
      return;
    }
    const container = document.createElement('div');
    container.className = swalClasses.container;
    if (oldContainerExisted) {
      addClass(container, swalClasses['no-transition']);
    }
    setInnerHtml(container, sweetHTML);
    container.dataset['swal2Theme'] = params.theme;
    const targetElement = getTarget(params.target);
    targetElement.appendChild(container);
    if (params.topLayer) {
      container.setAttribute('popover', '');
      container.showPopover();
    }
    setupAccessibility(params);
    setupRTL(targetElement);
    addInputChangeListeners();
  };

  /**
   * @param {HTMLElement | object | string} param
   * @param {HTMLElement} target
   */
  const parseHtmlToContainer = (param, target) => {
    // DOM element
    if (param instanceof HTMLElement) {
      target.appendChild(param);
    }

    // Object
    else if (typeof param === 'object') {
      handleObject(param, target);
    }

    // Plain string
    else if (param) {
      setInnerHtml(target, param);
    }
  };

  /**
   * @param {object} param
   * @param {HTMLElement} target
   */
  const handleObject = (param, target) => {
    // JQuery element(s)
    if (param.jquery) {
      handleJqueryElem(target, param);
    }

    // For other objects use their string representation
    else {
      setInnerHtml(target, param.toString());
    }
  };

  /**
   * @param {HTMLElement} target
   * @param {object} elem
   */
  const handleJqueryElem = (target, elem) => {
    target.textContent = '';
    if (0 in elem) {
      for (let i = 0; i in elem; i++) {
        target.appendChild(elem[i].cloneNode(true));
      }
    } else {
      target.appendChild(elem.cloneNode(true));
    }
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const renderActions = (instance, params) => {
    const actions = getActions();
    const loader = getLoader();
    if (!actions || !loader) {
      return;
    }

    // Actions (buttons) wrapper
    if (!params.showConfirmButton && !params.showDenyButton && !params.showCancelButton) {
      hide(actions);
    } else {
      show(actions);
    }

    // Custom class
    applyCustomClass(actions, params, 'actions');

    // Render all the buttons
    renderButtons(actions, loader, params);

    // Loader
    setInnerHtml(loader, params.loaderHtml || '');
    applyCustomClass(loader, params, 'loader');
  };

  /**
   * @param {HTMLElement} actions
   * @param {HTMLElement} loader
   * @param {SweetAlertOptions} params
   */
  function renderButtons(actions, loader, params) {
    const confirmButton = getConfirmButton();
    const denyButton = getDenyButton();
    const cancelButton = getCancelButton();
    if (!confirmButton || !denyButton || !cancelButton) {
      return;
    }

    // Render buttons
    renderButton(confirmButton, 'confirm', params);
    renderButton(denyButton, 'deny', params);
    renderButton(cancelButton, 'cancel', params);
    handleButtonsStyling(confirmButton, denyButton, cancelButton, params);
    if (params.reverseButtons) {
      if (params.toast) {
        actions.insertBefore(cancelButton, confirmButton);
        actions.insertBefore(denyButton, confirmButton);
      } else {
        actions.insertBefore(cancelButton, loader);
        actions.insertBefore(denyButton, loader);
        actions.insertBefore(confirmButton, loader);
      }
    }
  }

  /**
   * @param {HTMLElement} confirmButton
   * @param {HTMLElement} denyButton
   * @param {HTMLElement} cancelButton
   * @param {SweetAlertOptions} params
   */
  function handleButtonsStyling(confirmButton, denyButton, cancelButton, params) {
    if (!params.buttonsStyling) {
      removeClass([confirmButton, denyButton, cancelButton], swalClasses.styled);
      return;
    }
    addClass([confirmButton, denyButton, cancelButton], swalClasses.styled);

    // Apply custom background colors to action buttons
    if (params.confirmButtonColor) {
      confirmButton.style.setProperty('--swal2-confirm-button-background-color', params.confirmButtonColor);
    }
    if (params.denyButtonColor) {
      denyButton.style.setProperty('--swal2-deny-button-background-color', params.denyButtonColor);
    }
    if (params.cancelButtonColor) {
      cancelButton.style.setProperty('--swal2-cancel-button-background-color', params.cancelButtonColor);
    }

    // Apply the outline color to action buttons
    applyOutlineColor(confirmButton);
    applyOutlineColor(denyButton);
    applyOutlineColor(cancelButton);
  }

  /**
   * @param {HTMLElement} button
   */
  function applyOutlineColor(button) {
    const buttonStyle = window.getComputedStyle(button);
    if (buttonStyle.getPropertyValue('--swal2-action-button-focus-box-shadow')) {
      // If the button already has a custom outline color, no need to change it
      return;
    }
    const outlineColor = buttonStyle.backgroundColor.replace(/rgba?\((\d+), (\d+), (\d+).*/, 'rgba($1, $2, $3, 0.5)');
    button.style.setProperty('--swal2-action-button-focus-box-shadow', buttonStyle.getPropertyValue('--swal2-outline').replace(/ rgba\(.*/, ` ${outlineColor}`));
  }

  /**
   * @param {HTMLElement} button
   * @param {'confirm' | 'deny' | 'cancel'} buttonType
   * @param {SweetAlertOptions} params
   */
  function renderButton(button, buttonType, params) {
    const buttonName = /** @type {'Confirm' | 'Deny' | 'Cancel'} */capitalizeFirstLetter(buttonType);
    toggle(button, params[`show${buttonName}Button`], 'inline-block');
    setInnerHtml(button, params[`${buttonType}ButtonText`] || ''); // Set caption text
    button.setAttribute('aria-label', params[`${buttonType}ButtonAriaLabel`] || ''); // ARIA label

    // Add buttons custom classes
    button.className = swalClasses[buttonType];
    applyCustomClass(button, params, `${buttonType}Button`);
  }

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const renderCloseButton = (instance, params) => {
    const closeButton = getCloseButton();
    if (!closeButton) {
      return;
    }
    setInnerHtml(closeButton, params.closeButtonHtml || '');

    // Custom class
    applyCustomClass(closeButton, params, 'closeButton');
    toggle(closeButton, params.showCloseButton);
    closeButton.setAttribute('aria-label', params.closeButtonAriaLabel || '');
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const renderContainer = (instance, params) => {
    const container = getContainer();
    if (!container) {
      return;
    }
    handleBackdropParam(container, params.backdrop);
    handlePositionParam(container, params.position);
    handleGrowParam(container, params.grow);

    // Custom class
    applyCustomClass(container, params, 'container');
  };

  /**
   * @param {HTMLElement} container
   * @param {SweetAlertOptions['backdrop']} backdrop
   */
  function handleBackdropParam(container, backdrop) {
    if (typeof backdrop === 'string') {
      container.style.background = backdrop;
    } else if (!backdrop) {
      addClass([document.documentElement, document.body], swalClasses['no-backdrop']);
    }
  }

  /**
   * @param {HTMLElement} container
   * @param {SweetAlertOptions['position']} position
   */
  function handlePositionParam(container, position) {
    if (!position) {
      return;
    }
    if (position in swalClasses) {
      addClass(container, swalClasses[position]);
    } else {
      warn('The "position" parameter is not valid, defaulting to "center"');
      addClass(container, swalClasses.center);
    }
  }

  /**
   * @param {HTMLElement} container
   * @param {SweetAlertOptions['grow']} grow
   */
  function handleGrowParam(container, grow) {
    if (!grow) {
      return;
    }
    addClass(container, swalClasses[`grow-${grow}`]);
  }

  /**
   * This module contains `WeakMap`s for each effectively-"private  property" that a `Swal` has.
   * For example, to set the private property "foo" of `this` to "bar", you can `privateProps.foo.set(this, 'bar')`
   * This is the approach that Babel will probably take to implement private methods/fields
   *   https://github.com/tc39/proposal-private-methods
   *   https://github.com/babel/babel/pull/7555
   * Once we have the changes from that PR in Babel, and our core class fits reasonable in *one module*
   *   then we can use that language feature.
   */

  var privateProps = {
    innerParams: new WeakMap(),
    domCache: new WeakMap()
  };

  /// <reference path="../../../../sweetalert2.d.ts"/>


  /** @type {InputClass[]} */
  const inputClasses = ['input', 'file', 'range', 'select', 'radio', 'checkbox', 'textarea'];

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const renderInput = (instance, params) => {
    const popup = getPopup();
    if (!popup) {
      return;
    }
    const innerParams = privateProps.innerParams.get(instance);
    const rerender = !innerParams || params.input !== innerParams.input;
    inputClasses.forEach(inputClass => {
      const inputContainer = getDirectChildByClass(popup, swalClasses[inputClass]);
      if (!inputContainer) {
        return;
      }

      // set attributes
      setAttributes(inputClass, params.inputAttributes);

      // set class
      inputContainer.className = swalClasses[inputClass];
      if (rerender) {
        hide(inputContainer);
      }
    });
    if (params.input) {
      if (rerender) {
        showInput(params);
      }
      // set custom class
      setCustomClass(params);
    }
  };

  /**
   * @param {SweetAlertOptions} params
   */
  const showInput = params => {
    if (!params.input) {
      return;
    }
    if (!renderInputType[params.input]) {
      error(`Unexpected type of input! Expected ${Object.keys(renderInputType).join(' | ')}, got "${params.input}"`);
      return;
    }
    const inputContainer = getInputContainer(params.input);
    if (!inputContainer) {
      return;
    }
    const input = renderInputType[params.input](inputContainer, params);
    show(inputContainer);

    // input autofocus
    if (params.inputAutoFocus) {
      setTimeout(() => {
        focusInput(input);
      });
    }
  };

  /**
   * @param {HTMLInputElement} input
   */
  const removeAttributes = input => {
    for (let i = 0; i < input.attributes.length; i++) {
      const attrName = input.attributes[i].name;
      if (!['id', 'type', 'value', 'style'].includes(attrName)) {
        input.removeAttribute(attrName);
      }
    }
  };

  /**
   * @param {InputClass} inputClass
   * @param {SweetAlertOptions['inputAttributes']} inputAttributes
   */
  const setAttributes = (inputClass, inputAttributes) => {
    const popup = getPopup();
    if (!popup) {
      return;
    }
    const input = getInput$1(popup, inputClass);
    if (!input) {
      return;
    }
    removeAttributes(input);
    for (const attr in inputAttributes) {
      input.setAttribute(attr, inputAttributes[attr]);
    }
  };

  /**
   * @param {SweetAlertOptions} params
   */
  const setCustomClass = params => {
    if (!params.input) {
      return;
    }
    const inputContainer = getInputContainer(params.input);
    if (inputContainer) {
      applyCustomClass(inputContainer, params, 'input');
    }
  };

  /**
   * @param {HTMLInputElement | HTMLTextAreaElement} input
   * @param {SweetAlertOptions} params
   */
  const setInputPlaceholder = (input, params) => {
    if (!input.placeholder && params.inputPlaceholder) {
      input.placeholder = params.inputPlaceholder;
    }
  };

  /**
   * @param {Input} input
   * @param {Input} prependTo
   * @param {SweetAlertOptions} params
   */
  const setInputLabel = (input, prependTo, params) => {
    if (params.inputLabel) {
      const label = document.createElement('label');
      const labelClass = swalClasses['input-label'];
      label.setAttribute('for', input.id);
      label.className = labelClass;
      if (typeof params.customClass === 'object') {
        addClass(label, params.customClass.inputLabel);
      }
      label.innerText = params.inputLabel;
      prependTo.insertAdjacentElement('beforebegin', label);
    }
  };

  /**
   * @param {SweetAlertInput} inputType
   * @returns {HTMLElement | undefined}
   */
  const getInputContainer = inputType => {
    const popup = getPopup();
    if (!popup) {
      return;
    }
    return getDirectChildByClass(popup, swalClasses[(/** @type {SwalClass} */inputType)] || swalClasses.input);
  };

  /**
   * @param {HTMLInputElement | HTMLOutputElement | HTMLTextAreaElement} input
   * @param {SweetAlertOptions['inputValue']} inputValue
   */
  const checkAndSetInputValue = (input, inputValue) => {
    if (['string', 'number'].includes(typeof inputValue)) {
      input.value = `${inputValue}`;
    } else if (!isPromise(inputValue)) {
      warn(`Unexpected type of inputValue! Expected "string", "number" or "Promise", got "${typeof inputValue}"`);
    }
  };

  /** @type {Record<SweetAlertInput, (input: Input | HTMLElement, params: SweetAlertOptions) => Input>} */
  const renderInputType = {};

  /**
   * @param {HTMLInputElement} input
   * @param {SweetAlertOptions} params
   * @returns {HTMLInputElement}
   */
  renderInputType.text = renderInputType.email = renderInputType.password = renderInputType.number = renderInputType.tel = renderInputType.url = renderInputType.search = renderInputType.date = renderInputType['datetime-local'] = renderInputType.time = renderInputType.week = renderInputType.month = /** @type {(input: Input | HTMLElement, params: SweetAlertOptions) => Input} */
  (input, params) => {
    checkAndSetInputValue(input, params.inputValue);
    setInputLabel(input, input, params);
    setInputPlaceholder(input, params);
    input.type = params.input;
    return input;
  };

  /**
   * @param {HTMLInputElement} input
   * @param {SweetAlertOptions} params
   * @returns {HTMLInputElement}
   */
  renderInputType.file = (input, params) => {
    setInputLabel(input, input, params);
    setInputPlaceholder(input, params);
    return input;
  };

  /**
   * @param {HTMLInputElement} range
   * @param {SweetAlertOptions} params
   * @returns {HTMLInputElement}
   */
  renderInputType.range = (range, params) => {
    const rangeInput = range.querySelector('input');
    const rangeOutput = range.querySelector('output');
    checkAndSetInputValue(rangeInput, params.inputValue);
    rangeInput.type = params.input;
    checkAndSetInputValue(rangeOutput, params.inputValue);
    setInputLabel(rangeInput, range, params);
    return range;
  };

  /**
   * @param {HTMLSelectElement} select
   * @param {SweetAlertOptions} params
   * @returns {HTMLSelectElement}
   */
  renderInputType.select = (select, params) => {
    select.textContent = '';
    if (params.inputPlaceholder) {
      const placeholder = document.createElement('option');
      setInnerHtml(placeholder, params.inputPlaceholder);
      placeholder.value = '';
      placeholder.disabled = true;
      placeholder.selected = true;
      select.appendChild(placeholder);
    }
    setInputLabel(select, select, params);
    return select;
  };

  /**
   * @param {HTMLInputElement} radio
   * @returns {HTMLInputElement}
   */
  renderInputType.radio = radio => {
    radio.textContent = '';
    return radio;
  };

  /**
   * @param {HTMLLabelElement} checkboxContainer
   * @param {SweetAlertOptions} params
   * @returns {HTMLInputElement}
   */
  renderInputType.checkbox = (checkboxContainer, params) => {
    const checkbox = getInput$1(getPopup(), 'checkbox');
    checkbox.value = '1';
    checkbox.checked = Boolean(params.inputValue);
    const label = checkboxContainer.querySelector('span');
    setInnerHtml(label, params.inputPlaceholder || params.inputLabel);
    return checkbox;
  };

  /**
   * @param {HTMLTextAreaElement} textarea
   * @param {SweetAlertOptions} params
   * @returns {HTMLTextAreaElement}
   */
  renderInputType.textarea = (textarea, params) => {
    checkAndSetInputValue(textarea, params.inputValue);
    setInputPlaceholder(textarea, params);
    setInputLabel(textarea, textarea, params);

    /**
     * @param {HTMLElement} el
     * @returns {number}
     */
    const getMargin = el => parseInt(window.getComputedStyle(el).marginLeft) + parseInt(window.getComputedStyle(el).marginRight);

    // https://github.com/sweetalert2/sweetalert2/issues/2291
    setTimeout(() => {
      // https://github.com/sweetalert2/sweetalert2/issues/1699
      if ('MutationObserver' in window) {
        const initialPopupWidth = parseInt(window.getComputedStyle(getPopup()).width);
        const textareaResizeHandler = () => {
          // check if texarea is still in document (i.e. popup wasn't closed in the meantime)
          if (!document.body.contains(textarea)) {
            return;
          }
          const textareaWidth = textarea.offsetWidth + getMargin(textarea);
          if (textareaWidth > initialPopupWidth) {
            getPopup().style.width = `${textareaWidth}px`;
          } else {
            applyNumericalStyle(getPopup(), 'width', params.width);
          }
        };
        new MutationObserver(textareaResizeHandler).observe(textarea, {
          attributes: true,
          attributeFilter: ['style']
        });
      }
    });
    return textarea;
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const renderContent = (instance, params) => {
    const htmlContainer = getHtmlContainer();
    if (!htmlContainer) {
      return;
    }
    showWhenInnerHtmlPresent(htmlContainer);
    applyCustomClass(htmlContainer, params, 'htmlContainer');

    // Content as HTML
    if (params.html) {
      parseHtmlToContainer(params.html, htmlContainer);
      show(htmlContainer, 'block');
    }

    // Content as plain text
    else if (params.text) {
      htmlContainer.textContent = params.text;
      show(htmlContainer, 'block');
    }

    // No content
    else {
      hide(htmlContainer);
    }
    renderInput(instance, params);
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const renderFooter = (instance, params) => {
    const footer = getFooter();
    if (!footer) {
      return;
    }
    showWhenInnerHtmlPresent(footer);
    toggle(footer, Boolean(params.footer), 'block');
    if (params.footer) {
      parseHtmlToContainer(params.footer, footer);
    }

    // Custom class
    applyCustomClass(footer, params, 'footer');
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const renderIcon = (instance, params) => {
    const innerParams = privateProps.innerParams.get(instance);
    const icon = getIcon();
    if (!icon) {
      return;
    }

    // if the given icon already rendered, apply the styling without re-rendering the icon
    if (innerParams && params.icon === innerParams.icon) {
      // Custom or default content
      setContent(icon, params);
      applyStyles(icon, params);
      return;
    }
    if (!params.icon && !params.iconHtml) {
      hide(icon);
      return;
    }
    if (params.icon && Object.keys(iconTypes).indexOf(params.icon) === -1) {
      error(`Unknown icon! Expected "success", "error", "warning", "info" or "question", got "${params.icon}"`);
      hide(icon);
      return;
    }
    show(icon);

    // Custom or default content
    setContent(icon, params);
    applyStyles(icon, params);

    // Animate icon
    addClass(icon, params.showClass && params.showClass.icon);

    // Re-adjust the success icon on system theme change
    const colorSchemeQueryList = window.matchMedia('(prefers-color-scheme: dark)');
    colorSchemeQueryList.addEventListener('change', adjustSuccessIconBackgroundColor);
  };

  /**
   * @param {HTMLElement} icon
   * @param {SweetAlertOptions} params
   */
  const applyStyles = (icon, params) => {
    for (const [iconType, iconClassName] of Object.entries(iconTypes)) {
      if (params.icon !== iconType) {
        removeClass(icon, iconClassName);
      }
    }
    addClass(icon, params.icon && iconTypes[params.icon]);

    // Icon color
    setColor(icon, params);

    // Success icon background color
    adjustSuccessIconBackgroundColor();

    // Custom class
    applyCustomClass(icon, params, 'icon');
  };

  // Adjust success icon background color to match the popup background color
  const adjustSuccessIconBackgroundColor = () => {
    const popup = getPopup();
    if (!popup) {
      return;
    }
    const popupBackgroundColor = window.getComputedStyle(popup).getPropertyValue('background-color');
    /** @type {NodeListOf<HTMLElement>} */
    const successIconParts = popup.querySelectorAll('[class^=swal2-success-circular-line], .swal2-success-fix');
    for (let i = 0; i < successIconParts.length; i++) {
      successIconParts[i].style.backgroundColor = popupBackgroundColor;
    }
  };

  /**
   *
   * @param {SweetAlertOptions} params
   * @returns {string}
   */
  const successIconHtml = params => `
  ${params.animation ? '<div class="swal2-success-circular-line-left"></div>' : ''}
  <span class="swal2-success-line-tip"></span> <span class="swal2-success-line-long"></span>
  <div class="swal2-success-ring"></div>
  ${params.animation ? '<div class="swal2-success-fix"></div>' : ''}
  ${params.animation ? '<div class="swal2-success-circular-line-right"></div>' : ''}
`;
  const errorIconHtml = `
  <span class="swal2-x-mark">
    <span class="swal2-x-mark-line-left"></span>
    <span class="swal2-x-mark-line-right"></span>
  </span>
`;

  /**
   * @param {HTMLElement} icon
   * @param {SweetAlertOptions} params
   */
  const setContent = (icon, params) => {
    if (!params.icon && !params.iconHtml) {
      return;
    }
    let oldContent = icon.innerHTML;
    let newContent = '';
    if (params.iconHtml) {
      newContent = iconContent(params.iconHtml);
    } else if (params.icon === 'success') {
      newContent = successIconHtml(params);
      oldContent = oldContent.replace(/ style=".*?"/g, ''); // undo adjustSuccessIconBackgroundColor()
    } else if (params.icon === 'error') {
      newContent = errorIconHtml;
    } else if (params.icon) {
      const defaultIconHtml = {
        question: '?',
        warning: '!',
        info: 'i'
      };
      newContent = iconContent(defaultIconHtml[params.icon]);
    }
    if (oldContent.trim() !== newContent.trim()) {
      setInnerHtml(icon, newContent);
    }
  };

  /**
   * @param {HTMLElement} icon
   * @param {SweetAlertOptions} params
   */
  const setColor = (icon, params) => {
    if (!params.iconColor) {
      return;
    }
    icon.style.color = params.iconColor;
    icon.style.borderColor = params.iconColor;
    for (const sel of ['.swal2-success-line-tip', '.swal2-success-line-long', '.swal2-x-mark-line-left', '.swal2-x-mark-line-right']) {
      setStyle(icon, sel, 'background-color', params.iconColor);
    }
    setStyle(icon, '.swal2-success-ring', 'border-color', params.iconColor);
  };

  /**
   * @param {string} content
   * @returns {string}
   */
  const iconContent = content => `<div class="${swalClasses['icon-content']}">${content}</div>`;

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const renderImage = (instance, params) => {
    const image = getImage();
    if (!image) {
      return;
    }
    if (!params.imageUrl) {
      hide(image);
      return;
    }
    show(image, '');

    // Src, alt
    image.setAttribute('src', params.imageUrl);
    image.setAttribute('alt', params.imageAlt || '');

    // Width, height
    applyNumericalStyle(image, 'width', params.imageWidth);
    applyNumericalStyle(image, 'height', params.imageHeight);

    // Class
    image.className = swalClasses.image;
    applyCustomClass(image, params, 'image');
  };

  let dragging = false;
  let mousedownX = 0;
  let mousedownY = 0;
  let initialX = 0;
  let initialY = 0;

  /**
   * @param {HTMLElement} popup
   */
  const addDraggableListeners = popup => {
    popup.addEventListener('mousedown', down);
    document.body.addEventListener('mousemove', move);
    popup.addEventListener('mouseup', up);
    popup.addEventListener('touchstart', down);
    document.body.addEventListener('touchmove', move);
    popup.addEventListener('touchend', up);
  };

  /**
   * @param {HTMLElement} popup
   */
  const removeDraggableListeners = popup => {
    popup.removeEventListener('mousedown', down);
    document.body.removeEventListener('mousemove', move);
    popup.removeEventListener('mouseup', up);
    popup.removeEventListener('touchstart', down);
    document.body.removeEventListener('touchmove', move);
    popup.removeEventListener('touchend', up);
  };

  /**
   * @param {MouseEvent | TouchEvent} event
   */
  const down = event => {
    const popup = getPopup();
    if (event.target === popup || getIcon().contains(/** @type {HTMLElement} */event.target)) {
      dragging = true;
      const clientXY = getClientXY(event);
      mousedownX = clientXY.clientX;
      mousedownY = clientXY.clientY;
      initialX = parseInt(popup.style.insetInlineStart) || 0;
      initialY = parseInt(popup.style.insetBlockStart) || 0;
      addClass(popup, 'swal2-dragging');
    }
  };

  /**
   * @param {MouseEvent | TouchEvent} event
   */
  const move = event => {
    const popup = getPopup();
    if (dragging) {
      let {
        clientX,
        clientY
      } = getClientXY(event);
      popup.style.insetInlineStart = `${initialX + (clientX - mousedownX)}px`;
      popup.style.insetBlockStart = `${initialY + (clientY - mousedownY)}px`;
    }
  };
  const up = () => {
    const popup = getPopup();
    dragging = false;
    removeClass(popup, 'swal2-dragging');
  };

  /**
   * @param {MouseEvent | TouchEvent} event
   * @returns {{ clientX: number, clientY: number }}
   */
  const getClientXY = event => {
    let clientX = 0,
      clientY = 0;
    if (event.type.startsWith('mouse')) {
      clientX = /** @type {MouseEvent} */event.clientX;
      clientY = /** @type {MouseEvent} */event.clientY;
    } else if (event.type.startsWith('touch')) {
      clientX = /** @type {TouchEvent} */event.touches[0].clientX;
      clientY = /** @type {TouchEvent} */event.touches[0].clientY;
    }
    return {
      clientX,
      clientY
    };
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const renderPopup = (instance, params) => {
    const container = getContainer();
    const popup = getPopup();
    if (!container || !popup) {
      return;
    }

    // Width
    // https://github.com/sweetalert2/sweetalert2/issues/2170
    if (params.toast) {
      applyNumericalStyle(container, 'width', params.width);
      popup.style.width = '100%';
      const loader = getLoader();
      if (loader) {
        popup.insertBefore(loader, getIcon());
      }
    } else {
      applyNumericalStyle(popup, 'width', params.width);
    }

    // Padding
    applyNumericalStyle(popup, 'padding', params.padding);

    // Color
    if (params.color) {
      popup.style.color = params.color;
    }

    // Background
    if (params.background) {
      popup.style.background = params.background;
    }
    hide(getValidationMessage());

    // Classes
    addClasses$1(popup, params);
    if (params.draggable && !params.toast) {
      addClass(popup, swalClasses.draggable);
      addDraggableListeners(popup);
    } else {
      removeClass(popup, swalClasses.draggable);
      removeDraggableListeners(popup);
    }
  };

  /**
   * @param {HTMLElement} popup
   * @param {SweetAlertOptions} params
   */
  const addClasses$1 = (popup, params) => {
    const showClass = params.showClass || {};
    // Default Class + showClass when updating Swal.update({})
    popup.className = `${swalClasses.popup} ${isVisible$1(popup) ? showClass.popup : ''}`;
    if (params.toast) {
      addClass([document.documentElement, document.body], swalClasses['toast-shown']);
      addClass(popup, swalClasses.toast);
    } else {
      addClass(popup, swalClasses.modal);
    }

    // Custom class
    applyCustomClass(popup, params, 'popup');
    // TODO: remove in the next major
    if (typeof params.customClass === 'string') {
      addClass(popup, params.customClass);
    }

    // Icon class (#1842)
    if (params.icon) {
      addClass(popup, swalClasses[`icon-${params.icon}`]);
    }
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const renderProgressSteps = (instance, params) => {
    const progressStepsContainer = getProgressSteps();
    if (!progressStepsContainer) {
      return;
    }
    const {
      progressSteps,
      currentProgressStep
    } = params;
    if (!progressSteps || progressSteps.length === 0 || currentProgressStep === undefined) {
      hide(progressStepsContainer);
      return;
    }
    show(progressStepsContainer);
    progressStepsContainer.textContent = '';
    if (currentProgressStep >= progressSteps.length) {
      warn('Invalid currentProgressStep parameter, it should be less than progressSteps.length ' + '(currentProgressStep like JS arrays starts from 0)');
    }
    progressSteps.forEach((step, index) => {
      const stepEl = createStepElement(step);
      progressStepsContainer.appendChild(stepEl);
      if (index === currentProgressStep) {
        addClass(stepEl, swalClasses['active-progress-step']);
      }
      if (index !== progressSteps.length - 1) {
        const lineEl = createLineElement(params);
        progressStepsContainer.appendChild(lineEl);
      }
    });
  };

  /**
   * @param {string} step
   * @returns {HTMLLIElement}
   */
  const createStepElement = step => {
    const stepEl = document.createElement('li');
    addClass(stepEl, swalClasses['progress-step']);
    setInnerHtml(stepEl, step);
    return stepEl;
  };

  /**
   * @param {SweetAlertOptions} params
   * @returns {HTMLLIElement}
   */
  const createLineElement = params => {
    const lineEl = document.createElement('li');
    addClass(lineEl, swalClasses['progress-step-line']);
    if (params.progressStepsDistance) {
      applyNumericalStyle(lineEl, 'width', params.progressStepsDistance);
    }
    return lineEl;
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const renderTitle = (instance, params) => {
    const title = getTitle();
    if (!title) {
      return;
    }
    showWhenInnerHtmlPresent(title);
    toggle(title, Boolean(params.title || params.titleText), 'block');
    if (params.title) {
      parseHtmlToContainer(params.title, title);
    }
    if (params.titleText) {
      title.innerText = params.titleText;
    }

    // Custom class
    applyCustomClass(title, params, 'title');
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const render = (instance, params) => {
    renderPopup(instance, params);
    renderContainer(instance, params);
    renderProgressSteps(instance, params);
    renderIcon(instance, params);
    renderImage(instance, params);
    renderTitle(instance, params);
    renderCloseButton(instance, params);
    renderContent(instance, params);
    renderActions(instance, params);
    renderFooter(instance, params);
    const popup = getPopup();
    if (typeof params.didRender === 'function' && popup) {
      params.didRender(popup);
    }
    globalState.eventEmitter.emit('didRender', popup);
  };

  /*
   * Global function to determine if SweetAlert2 popup is shown
   */
  const isVisible = () => {
    return isVisible$1(getPopup());
  };

  /*
   * Global function to click 'Confirm' button
   */
  const clickConfirm = () => {
    var _dom$getConfirmButton;
    return (_dom$getConfirmButton = getConfirmButton()) === null || _dom$getConfirmButton === void 0 ? void 0 : _dom$getConfirmButton.click();
  };

  /*
   * Global function to click 'Deny' button
   */
  const clickDeny = () => {
    var _dom$getDenyButton;
    return (_dom$getDenyButton = getDenyButton()) === null || _dom$getDenyButton === void 0 ? void 0 : _dom$getDenyButton.click();
  };

  /*
   * Global function to click 'Cancel' button
   */
  const clickCancel = () => {
    var _dom$getCancelButton;
    return (_dom$getCancelButton = getCancelButton()) === null || _dom$getCancelButton === void 0 ? void 0 : _dom$getCancelButton.click();
  };

  /** @type {Record<DismissReason, DismissReason>} */
  const DismissReason = Object.freeze({
    cancel: 'cancel',
    backdrop: 'backdrop',
    close: 'close',
    esc: 'esc',
    timer: 'timer'
  });

  /**
   * @param {GlobalState} globalState
   */
  const removeKeydownHandler = globalState => {
    if (globalState.keydownTarget && globalState.keydownHandlerAdded) {
      globalState.keydownTarget.removeEventListener('keydown', globalState.keydownHandler, {
        capture: globalState.keydownListenerCapture
      });
      globalState.keydownHandlerAdded = false;
    }
  };

  /**
   * @param {GlobalState} globalState
   * @param {SweetAlertOptions} innerParams
   * @param {(dismiss: DismissReason) => void} dismissWith
   */
  const addKeydownHandler = (globalState, innerParams, dismissWith) => {
    removeKeydownHandler(globalState);
    if (!innerParams.toast) {
      globalState.keydownHandler = e => keydownHandler(innerParams, e, dismissWith);
      globalState.keydownTarget = innerParams.keydownListenerCapture ? window : getPopup();
      globalState.keydownListenerCapture = innerParams.keydownListenerCapture;
      globalState.keydownTarget.addEventListener('keydown', globalState.keydownHandler, {
        capture: globalState.keydownListenerCapture
      });
      globalState.keydownHandlerAdded = true;
    }
  };

  /**
   * @param {number} index
   * @param {number} increment
   */
  const setFocus = (index, increment) => {
    var _dom$getPopup;
    const focusableElements = getFocusableElements();
    // search for visible elements and select the next possible match
    if (focusableElements.length) {
      index = index + increment;

      // shift + tab when .swal2-popup is focused
      if (index === -2) {
        index = focusableElements.length - 1;
      }

      // rollover to first item
      if (index === focusableElements.length) {
        index = 0;

        // go to last item
      } else if (index === -1) {
        index = focusableElements.length - 1;
      }
      focusableElements[index].focus();
      return;
    }
    // no visible focusable elements, focus the popup
    (_dom$getPopup = getPopup()) === null || _dom$getPopup === void 0 || _dom$getPopup.focus();
  };
  const arrowKeysNextButton = ['ArrowRight', 'ArrowDown'];
  const arrowKeysPreviousButton = ['ArrowLeft', 'ArrowUp'];

  /**
   * @param {SweetAlertOptions} innerParams
   * @param {KeyboardEvent} event
   * @param {(dismiss: DismissReason) => void} dismissWith
   */
  const keydownHandler = (innerParams, event, dismissWith) => {
    if (!innerParams) {
      return; // This instance has already been destroyed
    }

    // Ignore keydown during IME composition
    // https://developer.mozilla.org/en-US/docs/Web/API/Document/keydown_event#ignoring_keydown_during_ime_composition
    // https://github.com/sweetalert2/sweetalert2/issues/720
    // https://github.com/sweetalert2/sweetalert2/issues/2406
    if (event.isComposing || event.keyCode === 229) {
      return;
    }
    if (innerParams.stopKeydownPropagation) {
      event.stopPropagation();
    }

    // ENTER
    if (event.key === 'Enter') {
      handleEnter(event, innerParams);
    }

    // TAB
    else if (event.key === 'Tab') {
      handleTab(event);
    }

    // ARROWS - switch focus between buttons
    else if ([...arrowKeysNextButton, ...arrowKeysPreviousButton].includes(event.key)) {
      handleArrows(event.key);
    }

    // ESC
    else if (event.key === 'Escape') {
      handleEsc(event, innerParams, dismissWith);
    }
  };

  /**
   * @param {KeyboardEvent} event
   * @param {SweetAlertOptions} innerParams
   */
  const handleEnter = (event, innerParams) => {
    // https://github.com/sweetalert2/sweetalert2/issues/2386
    if (!callIfFunction(innerParams.allowEnterKey)) {
      return;
    }
    const input = getInput$1(getPopup(), innerParams.input);
    if (event.target && input && event.target instanceof HTMLElement && event.target.outerHTML === input.outerHTML) {
      if (['textarea', 'file'].includes(innerParams.input)) {
        return; // do not submit
      }
      clickConfirm();
      event.preventDefault();
    }
  };

  /**
   * @param {KeyboardEvent} event
   */
  const handleTab = event => {
    const targetElement = event.target;
    const focusableElements = getFocusableElements();
    let btnIndex = -1;
    for (let i = 0; i < focusableElements.length; i++) {
      if (targetElement === focusableElements[i]) {
        btnIndex = i;
        break;
      }
    }

    // Cycle to the next button
    if (!event.shiftKey) {
      setFocus(btnIndex, 1);
    }

    // Cycle to the prev button
    else {
      setFocus(btnIndex, -1);
    }
    event.stopPropagation();
    event.preventDefault();
  };

  /**
   * @param {string} key
   */
  const handleArrows = key => {
    const actions = getActions();
    const confirmButton = getConfirmButton();
    const denyButton = getDenyButton();
    const cancelButton = getCancelButton();
    if (!actions || !confirmButton || !denyButton || !cancelButton) {
      return;
    }
    /** @type HTMLElement[] */
    const buttons = [confirmButton, denyButton, cancelButton];
    if (document.activeElement instanceof HTMLElement && !buttons.includes(document.activeElement)) {
      return;
    }
    const sibling = arrowKeysNextButton.includes(key) ? 'nextElementSibling' : 'previousElementSibling';
    let buttonToFocus = document.activeElement;
    if (!buttonToFocus) {
      return;
    }
    for (let i = 0; i < actions.children.length; i++) {
      buttonToFocus = buttonToFocus[sibling];
      if (!buttonToFocus) {
        return;
      }
      if (buttonToFocus instanceof HTMLButtonElement && isVisible$1(buttonToFocus)) {
        break;
      }
    }
    if (buttonToFocus instanceof HTMLButtonElement) {
      buttonToFocus.focus();
    }
  };

  /**
   * @param {KeyboardEvent} event
   * @param {SweetAlertOptions} innerParams
   * @param {(dismiss: DismissReason) => void} dismissWith
   */
  const handleEsc = (event, innerParams, dismissWith) => {
    event.preventDefault();
    if (callIfFunction(innerParams.allowEscapeKey)) {
      dismissWith(DismissReason.esc);
    }
  };

  /**
   * This module contains `WeakMap`s for each effectively-"private  property" that a `Swal` has.
   * For example, to set the private property "foo" of `this` to "bar", you can `privateProps.foo.set(this, 'bar')`
   * This is the approach that Babel will probably take to implement private methods/fields
   *   https://github.com/tc39/proposal-private-methods
   *   https://github.com/babel/babel/pull/7555
   * Once we have the changes from that PR in Babel, and our core class fits reasonable in *one module*
   *   then we can use that language feature.
   */

  var privateMethods = {
    swalPromiseResolve: new WeakMap(),
    swalPromiseReject: new WeakMap()
  };

  // From https://developer.paciellogroup.com/blog/2018/06/the-current-state-of-modal-dialog-accessibility/
  // Adding aria-hidden="true" to elements outside of the active modal dialog ensures that
  // elements not within the active modal dialog will not be surfaced if a user opens a screen
  // reader’s list of elements (headings, form controls, landmarks, etc.) in the document.

  const setAriaHidden = () => {
    const container = getContainer();
    const bodyChildren = Array.from(document.body.children);
    bodyChildren.forEach(el => {
      if (el.contains(container)) {
        return;
      }
      if (el.hasAttribute('aria-hidden')) {
        el.setAttribute('data-previous-aria-hidden', el.getAttribute('aria-hidden') || '');
      }
      el.setAttribute('aria-hidden', 'true');
    });
  };
  const unsetAriaHidden = () => {
    const bodyChildren = Array.from(document.body.children);
    bodyChildren.forEach(el => {
      if (el.hasAttribute('data-previous-aria-hidden')) {
        el.setAttribute('aria-hidden', el.getAttribute('data-previous-aria-hidden') || '');
        el.removeAttribute('data-previous-aria-hidden');
      } else {
        el.removeAttribute('aria-hidden');
      }
    });
  };

  // @ts-ignore
  const isSafariOrIOS = typeof window !== 'undefined' && !!window.GestureEvent; // true for Safari desktop + all iOS browsers https://stackoverflow.com/a/70585394

  /**
   * Fix iOS scrolling
   * http://stackoverflow.com/q/39626302
   */
  const iOSfix = () => {
    if (isSafariOrIOS && !hasClass(document.body, swalClasses.iosfix)) {
      const offset = document.body.scrollTop;
      document.body.style.top = `${offset * -1}px`;
      addClass(document.body, swalClasses.iosfix);
      lockBodyScroll();
    }
  };

  /**
   * https://github.com/sweetalert2/sweetalert2/issues/1246
   */
  const lockBodyScroll = () => {
    const container = getContainer();
    if (!container) {
      return;
    }
    /** @type {boolean} */
    let preventTouchMove;
    /**
     * @param {TouchEvent} event
     */
    container.ontouchstart = event => {
      preventTouchMove = shouldPreventTouchMove(event);
    };
    /**
     * @param {TouchEvent} event
     */
    container.ontouchmove = event => {
      if (preventTouchMove) {
        event.preventDefault();
        event.stopPropagation();
      }
    };
  };

  /**
   * @param {TouchEvent} event
   * @returns {boolean}
   */
  const shouldPreventTouchMove = event => {
    const target = event.target;
    const container = getContainer();
    const htmlContainer = getHtmlContainer();
    if (!container || !htmlContainer) {
      return false;
    }
    if (isStylus(event) || isZoom(event)) {
      return false;
    }
    if (target === container) {
      return true;
    }
    if (!isScrollable(container) && target instanceof HTMLElement && !selfOrParentIsScrollable(target, htmlContainer) &&
    // #2823
    target.tagName !== 'INPUT' &&
    // #1603
    target.tagName !== 'TEXTAREA' &&
    // #2266
    !(isScrollable(htmlContainer) &&
    // #1944
    htmlContainer.contains(target))) {
      return true;
    }
    return false;
  };

  /**
   * https://github.com/sweetalert2/sweetalert2/issues/1786
   *
   * @param {object} event
   * @returns {boolean}
   */
  const isStylus = event => {
    return event.touches && event.touches.length && event.touches[0].touchType === 'stylus';
  };

  /**
   * https://github.com/sweetalert2/sweetalert2/issues/1891
   *
   * @param {TouchEvent} event
   * @returns {boolean}
   */
  const isZoom = event => {
    return event.touches && event.touches.length > 1;
  };
  const undoIOSfix = () => {
    if (hasClass(document.body, swalClasses.iosfix)) {
      const offset = parseInt(document.body.style.top, 10);
      removeClass(document.body, swalClasses.iosfix);
      document.body.style.top = '';
      document.body.scrollTop = offset * -1;
    }
  };

  /**
   * Measure scrollbar width for padding body during modal show/hide
   * https://github.com/twbs/bootstrap/blob/master/js/src/modal.js
   *
   * @returns {number}
   */
  const measureScrollbar = () => {
    const scrollDiv = document.createElement('div');
    scrollDiv.className = swalClasses['scrollbar-measure'];
    document.body.appendChild(scrollDiv);
    const scrollbarWidth = scrollDiv.getBoundingClientRect().width - scrollDiv.clientWidth;
    document.body.removeChild(scrollDiv);
    return scrollbarWidth;
  };

  /**
   * Remember state in cases where opening and handling a modal will fiddle with it.
   * @type {number | null}
   */
  let previousBodyPadding = null;

  /**
   * @param {string} initialBodyOverflow
   */
  const replaceScrollbarWithPadding = initialBodyOverflow => {
    // for queues, do not do this more than once
    if (previousBodyPadding !== null) {
      return;
    }
    // if the body has overflow
    if (document.body.scrollHeight > window.innerHeight || initialBodyOverflow === 'scroll' // https://github.com/sweetalert2/sweetalert2/issues/2663
    ) {
      // add padding so the content doesn't shift after removal of scrollbar
      previousBodyPadding = parseInt(window.getComputedStyle(document.body).getPropertyValue('padding-right'));
      document.body.style.paddingRight = `${previousBodyPadding + measureScrollbar()}px`;
    }
  };
  const undoReplaceScrollbarWithPadding = () => {
    if (previousBodyPadding !== null) {
      document.body.style.paddingRight = `${previousBodyPadding}px`;
      previousBodyPadding = null;
    }
  };

  /**
   * @param {SweetAlert} instance
   * @param {HTMLElement} container
   * @param {boolean} returnFocus
   * @param {() => void} didClose
   */
  function removePopupAndResetState(instance, container, returnFocus, didClose) {
    if (isToast()) {
      triggerDidCloseAndDispose(instance, didClose);
    } else {
      restoreActiveElement(returnFocus).then(() => triggerDidCloseAndDispose(instance, didClose));
      removeKeydownHandler(globalState);
    }

    // workaround for https://github.com/sweetalert2/sweetalert2/issues/2088
    // for some reason removing the container in Safari will scroll the document to bottom
    if (isSafariOrIOS) {
      container.setAttribute('style', 'display:none !important');
      container.removeAttribute('class');
      container.innerHTML = '';
    } else {
      container.remove();
    }
    if (isModal()) {
      undoReplaceScrollbarWithPadding();
      undoIOSfix();
      unsetAriaHidden();
    }
    removeBodyClasses();
  }

  /**
   * Remove SweetAlert2 classes from body
   */
  function removeBodyClasses() {
    removeClass([document.documentElement, document.body], [swalClasses.shown, swalClasses['height-auto'], swalClasses['no-backdrop'], swalClasses['toast-shown']]);
  }

  /**
   * Instance method to close sweetAlert
   *
   * @param {SweetAlertResult | undefined} resolveValue
   */
  function close(resolveValue) {
    resolveValue = prepareResolveValue(resolveValue);
    const swalPromiseResolve = privateMethods.swalPromiseResolve.get(this);
    const didClose = triggerClosePopup(this);
    if (this.isAwaitingPromise) {
      // A swal awaiting for a promise (after a click on Confirm or Deny) cannot be dismissed anymore #2335
      if (!resolveValue.isDismissed) {
        handleAwaitingPromise(this);
        swalPromiseResolve(resolveValue);
      }
    } else if (didClose) {
      // Resolve Swal promise
      swalPromiseResolve(resolveValue);
    }
  }
  const triggerClosePopup = instance => {
    const popup = getPopup();
    if (!popup) {
      return false;
    }
    const innerParams = privateProps.innerParams.get(instance);
    if (!innerParams || hasClass(popup, innerParams.hideClass.popup)) {
      return false;
    }
    removeClass(popup, innerParams.showClass.popup);
    addClass(popup, innerParams.hideClass.popup);
    const backdrop = getContainer();
    removeClass(backdrop, innerParams.showClass.backdrop);
    addClass(backdrop, innerParams.hideClass.backdrop);
    handlePopupAnimation(instance, popup, innerParams);
    return true;
  };

  /**
   * @param {Error | string} error
   */
  function rejectPromise(error) {
    const rejectPromise = privateMethods.swalPromiseReject.get(this);
    handleAwaitingPromise(this);
    if (rejectPromise) {
      // Reject Swal promise
      rejectPromise(error);
    }
  }

  /**
   * @param {SweetAlert} instance
   */
  const handleAwaitingPromise = instance => {
    if (instance.isAwaitingPromise) {
      delete instance.isAwaitingPromise;
      // The instance might have been previously partly destroyed, we must resume the destroy process in this case #2335
      if (!privateProps.innerParams.get(instance)) {
        instance._destroy();
      }
    }
  };

  /**
   * @param {SweetAlertResult | undefined} resolveValue
   * @returns {SweetAlertResult}
   */
  const prepareResolveValue = resolveValue => {
    // When user calls Swal.close()
    if (typeof resolveValue === 'undefined') {
      return {
        isConfirmed: false,
        isDenied: false,
        isDismissed: true
      };
    }
    return Object.assign({
      isConfirmed: false,
      isDenied: false,
      isDismissed: false
    }, resolveValue);
  };

  /**
   * @param {SweetAlert} instance
   * @param {HTMLElement} popup
   * @param {SweetAlertOptions} innerParams
   */
  const handlePopupAnimation = (instance, popup, innerParams) => {
    var _globalState$eventEmi;
    const container = getContainer();
    // If animation is supported, animate
    const animationIsSupported = hasCssAnimation(popup);
    if (typeof innerParams.willClose === 'function') {
      innerParams.willClose(popup);
    }
    (_globalState$eventEmi = globalState.eventEmitter) === null || _globalState$eventEmi === void 0 || _globalState$eventEmi.emit('willClose', popup);
    if (animationIsSupported) {
      animatePopup(instance, popup, container, innerParams.returnFocus, innerParams.didClose);
    } else {
      // Otherwise, remove immediately
      removePopupAndResetState(instance, container, innerParams.returnFocus, innerParams.didClose);
    }
  };

  /**
   * @param {SweetAlert} instance
   * @param {HTMLElement} popup
   * @param {HTMLElement} container
   * @param {boolean} returnFocus
   * @param {() => void} didClose
   */
  const animatePopup = (instance, popup, container, returnFocus, didClose) => {
    globalState.swalCloseEventFinishedCallback = removePopupAndResetState.bind(null, instance, container, returnFocus, didClose);
    /**
     * @param {AnimationEvent | TransitionEvent} e
     */
    const swalCloseAnimationFinished = function (e) {
      if (e.target === popup) {
        var _globalState$swalClos;
        (_globalState$swalClos = globalState.swalCloseEventFinishedCallback) === null || _globalState$swalClos === void 0 || _globalState$swalClos.call(globalState);
        delete globalState.swalCloseEventFinishedCallback;
        popup.removeEventListener('animationend', swalCloseAnimationFinished);
        popup.removeEventListener('transitionend', swalCloseAnimationFinished);
      }
    };
    popup.addEventListener('animationend', swalCloseAnimationFinished);
    popup.addEventListener('transitionend', swalCloseAnimationFinished);
  };

  /**
   * @param {SweetAlert} instance
   * @param {() => void} didClose
   */
  const triggerDidCloseAndDispose = (instance, didClose) => {
    setTimeout(() => {
      var _globalState$eventEmi2;
      if (typeof didClose === 'function') {
        didClose.bind(instance.params)();
      }
      (_globalState$eventEmi2 = globalState.eventEmitter) === null || _globalState$eventEmi2 === void 0 || _globalState$eventEmi2.emit('didClose');
      // instance might have been destroyed already
      if (instance._destroy) {
        instance._destroy();
      }
    });
  };

  /**
   * Shows loader (spinner), this is useful with AJAX requests.
   * By default the loader be shown instead of the "Confirm" button.
   *
   * @param {HTMLButtonElement | null} [buttonToReplace]
   */
  const showLoading = buttonToReplace => {
    let popup = getPopup();
    if (!popup) {
      new Swal();
    }
    popup = getPopup();
    if (!popup) {
      return;
    }
    const loader = getLoader();
    if (isToast()) {
      hide(getIcon());
    } else {
      replaceButton(popup, buttonToReplace);
    }
    show(loader);
    popup.setAttribute('data-loading', 'true');
    popup.setAttribute('aria-busy', 'true');
    popup.focus();
  };

  /**
   * @param {HTMLElement} popup
   * @param {HTMLButtonElement | null} [buttonToReplace]
   */
  const replaceButton = (popup, buttonToReplace) => {
    const actions = getActions();
    const loader = getLoader();
    if (!actions || !loader) {
      return;
    }
    if (!buttonToReplace && isVisible$1(getConfirmButton())) {
      buttonToReplace = getConfirmButton();
    }
    show(actions);
    if (buttonToReplace) {
      hide(buttonToReplace);
      loader.setAttribute('data-button-to-replace', buttonToReplace.className);
      actions.insertBefore(loader, buttonToReplace);
    }
    addClass([popup, actions], swalClasses.loading);
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const handleInputOptionsAndValue = (instance, params) => {
    if (params.input === 'select' || params.input === 'radio') {
      handleInputOptions(instance, params);
    } else if (['text', 'email', 'number', 'tel', 'textarea'].some(i => i === params.input) && (hasToPromiseFn(params.inputValue) || isPromise(params.inputValue))) {
      showLoading(getConfirmButton());
      handleInputValue(instance, params);
    }
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} innerParams
   * @returns {SweetAlertInputValue}
   */
  const getInputValue = (instance, innerParams) => {
    const input = instance.getInput();
    if (!input) {
      return null;
    }
    switch (innerParams.input) {
      case 'checkbox':
        return getCheckboxValue(input);
      case 'radio':
        return getRadioValue(input);
      case 'file':
        return getFileValue(input);
      default:
        return innerParams.inputAutoTrim ? input.value.trim() : input.value;
    }
  };

  /**
   * @param {HTMLInputElement} input
   * @returns {number}
   */
  const getCheckboxValue = input => input.checked ? 1 : 0;

  /**
   * @param {HTMLInputElement} input
   * @returns {string | null}
   */
  const getRadioValue = input => input.checked ? input.value : null;

  /**
   * @param {HTMLInputElement} input
   * @returns {FileList | File | null}
   */
  const getFileValue = input => input.files && input.files.length ? input.getAttribute('multiple') !== null ? input.files : input.files[0] : null;

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const handleInputOptions = (instance, params) => {
    const popup = getPopup();
    if (!popup) {
      return;
    }
    /**
     * @param {*} inputOptions
     */
    const processInputOptions = inputOptions => {
      if (params.input === 'select') {
        populateSelectOptions(popup, formatInputOptions(inputOptions), params);
      } else if (params.input === 'radio') {
        populateRadioOptions(popup, formatInputOptions(inputOptions), params);
      }
    };
    if (hasToPromiseFn(params.inputOptions) || isPromise(params.inputOptions)) {
      showLoading(getConfirmButton());
      asPromise(params.inputOptions).then(inputOptions => {
        instance.hideLoading();
        processInputOptions(inputOptions);
      });
    } else if (typeof params.inputOptions === 'object') {
      processInputOptions(params.inputOptions);
    } else {
      error(`Unexpected type of inputOptions! Expected object, Map or Promise, got ${typeof params.inputOptions}`);
    }
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertOptions} params
   */
  const handleInputValue = (instance, params) => {
    const input = instance.getInput();
    if (!input) {
      return;
    }
    hide(input);
    asPromise(params.inputValue).then(inputValue => {
      input.value = params.input === 'number' ? `${parseFloat(inputValue) || 0}` : `${inputValue}`;
      show(input);
      input.focus();
      instance.hideLoading();
    }).catch(err => {
      error(`Error in inputValue promise: ${err}`);
      input.value = '';
      show(input);
      input.focus();
      instance.hideLoading();
    });
  };

  /**
   * @param {HTMLElement} popup
   * @param {InputOptionFlattened[]} inputOptions
   * @param {SweetAlertOptions} params
   */
  function populateSelectOptions(popup, inputOptions, params) {
    const select = getDirectChildByClass(popup, swalClasses.select);
    if (!select) {
      return;
    }
    /**
     * @param {HTMLElement} parent
     * @param {string} optionLabel
     * @param {string} optionValue
     */
    const renderOption = (parent, optionLabel, optionValue) => {
      const option = document.createElement('option');
      option.value = optionValue;
      setInnerHtml(option, optionLabel);
      option.selected = isSelected(optionValue, params.inputValue);
      parent.appendChild(option);
    };
    inputOptions.forEach(inputOption => {
      const optionValue = inputOption[0];
      const optionLabel = inputOption[1];
      // <optgroup> spec:
      // https://www.w3.org/TR/html401/interact/forms.html#h-17.6
      // "...all OPTGROUP elements must be specified directly within a SELECT element (i.e., groups may not be nested)..."
      // check whether this is a <optgroup>
      if (Array.isArray(optionLabel)) {
        // if it is an array, then it is an <optgroup>
        const optgroup = document.createElement('optgroup');
        optgroup.label = optionValue;
        optgroup.disabled = false; // not configurable for now
        select.appendChild(optgroup);
        optionLabel.forEach(o => renderOption(optgroup, o[1], o[0]));
      } else {
        // case of <option>
        renderOption(select, optionLabel, optionValue);
      }
    });
    select.focus();
  }

  /**
   * @param {HTMLElement} popup
   * @param {InputOptionFlattened[]} inputOptions
   * @param {SweetAlertOptions} params
   */
  function populateRadioOptions(popup, inputOptions, params) {
    const radio = getDirectChildByClass(popup, swalClasses.radio);
    if (!radio) {
      return;
    }
    inputOptions.forEach(inputOption => {
      const radioValue = inputOption[0];
      const radioLabel = inputOption[1];
      const radioInput = document.createElement('input');
      const radioLabelElement = document.createElement('label');
      radioInput.type = 'radio';
      radioInput.name = swalClasses.radio;
      radioInput.value = radioValue;
      if (isSelected(radioValue, params.inputValue)) {
        radioInput.checked = true;
      }
      const label = document.createElement('span');
      setInnerHtml(label, radioLabel);
      label.className = swalClasses.label;
      radioLabelElement.appendChild(radioInput);
      radioLabelElement.appendChild(label);
      radio.appendChild(radioLabelElement);
    });
    const radios = radio.querySelectorAll('input');
    if (radios.length) {
      radios[0].focus();
    }
  }

  /**
   * Converts `inputOptions` into an array of `[value, label]`s
   *
   * @param {*} inputOptions
   * @typedef {string[]} InputOptionFlattened
   * @returns {InputOptionFlattened[]}
   */
  const formatInputOptions = inputOptions => {
    /** @type {InputOptionFlattened[]} */
    const result = [];
    if (inputOptions instanceof Map) {
      inputOptions.forEach((value, key) => {
        let valueFormatted = value;
        if (typeof valueFormatted === 'object') {
          // case of <optgroup>
          valueFormatted = formatInputOptions(valueFormatted);
        }
        result.push([key, valueFormatted]);
      });
    } else {
      Object.keys(inputOptions).forEach(key => {
        let valueFormatted = inputOptions[key];
        if (typeof valueFormatted === 'object') {
          // case of <optgroup>
          valueFormatted = formatInputOptions(valueFormatted);
        }
        result.push([key, valueFormatted]);
      });
    }
    return result;
  };

  /**
   * @param {string} optionValue
   * @param {SweetAlertInputValue} inputValue
   * @returns {boolean}
   */
  const isSelected = (optionValue, inputValue) => {
    return !!inputValue && inputValue.toString() === optionValue.toString();
  };

  /**
   * @param {SweetAlert} instance
   */
  const handleConfirmButtonClick = instance => {
    const innerParams = privateProps.innerParams.get(instance);
    instance.disableButtons();
    if (innerParams.input) {
      handleConfirmOrDenyWithInput(instance, 'confirm');
    } else {
      confirm(instance, true);
    }
  };

  /**
   * @param {SweetAlert} instance
   */
  const handleDenyButtonClick = instance => {
    const innerParams = privateProps.innerParams.get(instance);
    instance.disableButtons();
    if (innerParams.returnInputValueOnDeny) {
      handleConfirmOrDenyWithInput(instance, 'deny');
    } else {
      deny(instance, false);
    }
  };

  /**
   * @param {SweetAlert} instance
   * @param {(dismiss: DismissReason) => void} dismissWith
   */
  const handleCancelButtonClick = (instance, dismissWith) => {
    instance.disableButtons();
    dismissWith(DismissReason.cancel);
  };

  /**
   * @param {SweetAlert} instance
   * @param {'confirm' | 'deny'} type
   */
  const handleConfirmOrDenyWithInput = (instance, type) => {
    const innerParams = privateProps.innerParams.get(instance);
    if (!innerParams.input) {
      error(`The "input" parameter is needed to be set when using returnInputValueOn${capitalizeFirstLetter(type)}`);
      return;
    }
    const input = instance.getInput();
    const inputValue = getInputValue(instance, innerParams);
    if (innerParams.inputValidator) {
      handleInputValidator(instance, inputValue, type);
    } else if (input && !input.checkValidity()) {
      instance.enableButtons();
      instance.showValidationMessage(innerParams.validationMessage || input.validationMessage);
    } else if (type === 'deny') {
      deny(instance, inputValue);
    } else {
      confirm(instance, inputValue);
    }
  };

  /**
   * @param {SweetAlert} instance
   * @param {SweetAlertInputValue} inputValue
   * @param {'confirm' | 'deny'} type
   */
  const handleInputValidator = (instance, inputValue, type) => {
    const innerParams = privateProps.innerParams.get(instance);
    instance.disableInput();
    const validationPromise = Promise.resolve().then(() => asPromise(innerParams.inputValidator(inputValue, innerParams.validationMessage)));
    validationPromise.then(validationMessage => {
      instance.enableButtons();
      instance.enableInput();
      if (validationMessage) {
        instance.showValidationMessage(validationMessage);
      } else if (type === 'deny') {
        deny(instance, inputValue);
      } else {
        confirm(instance, inputValue);
      }
    });
  };

  /**
   * @param {SweetAlert} instance
   * @param {*} value
   */
  const deny = (instance, value) => {
    const innerParams = privateProps.innerParams.get(instance || undefined);
    if (innerParams.showLoaderOnDeny) {
      showLoading(getDenyButton());
    }
    if (innerParams.preDeny) {
      instance.isAwaitingPromise = true; // Flagging the instance as awaiting a promise so it's own promise's reject/resolve methods doesn't get destroyed until the result from this preDeny's promise is received
      const preDenyPromise = Promise.resolve().then(() => asPromise(innerParams.preDeny(value, innerParams.validationMessage)));
      preDenyPromise.then(preDenyValue => {
        if (preDenyValue === false) {
          instance.hideLoading();
          handleAwaitingPromise(instance);
        } else {
          instance.close(/** @type SweetAlertResult */{
            isDenied: true,
            value: typeof preDenyValue === 'undefined' ? value : preDenyValue
          });
        }
      }).catch(error => rejectWith(instance || undefined, error));
    } else {
      instance.close(/** @type SweetAlertResult */{
        isDenied: true,
        value
      });
    }
  };

  /**
   * @param {SweetAlert} instance
   * @param {*} value
   */
  const succeedWith = (instance, value) => {
    instance.close(/** @type SweetAlertResult */{
      isConfirmed: true,
      value
    });
  };

  /**
   *
   * @param {SweetAlert} instance
   * @param {string} error
   */
  const rejectWith = (instance, error) => {
    instance.rejectPromise(error);
  };

  /**
   *
   * @param {SweetAlert} instance
   * @param {*} value
   */
  const confirm = (instance, value) => {
    const innerParams = privateProps.innerParams.get(instance || undefined);
    if (innerParams.showLoaderOnConfirm) {
      showLoading();
    }
    if (innerParams.preConfirm) {
      instance.resetValidationMessage();
      instance.isAwaitingPromise = true; // Flagging the instance as awaiting a promise so it's own promise's reject/resolve methods doesn't get destroyed until the result from this preConfirm's promise is received
      const preConfirmPromise = Promise.resolve().then(() => asPromise(innerParams.preConfirm(value, innerParams.validationMessage)));
      preConfirmPromise.then(preConfirmValue => {
        if (isVisible$1(getValidationMessage()) || preConfirmValue === false) {
          instance.hideLoading();
          handleAwaitingPromise(instance);
        } else {
          succeedWith(instance, typeof preConfirmValue === 'undefined' ? value : preConfirmValue);
        }
      }).catch(error => rejectWith(instance || undefined, error));
    } else {
      succeedWith(instance, value);
    }
  };

  /**
   * Hides loader and shows back the button which was hidden by .showLoading()
   */
  function hideLoading() {
    // do nothing if popup is closed
    const innerParams = privateProps.innerParams.get(this);
    if (!innerParams) {
      return;
    }
    const domCache = privateProps.domCache.get(this);
    hide(domCache.loader);
    if (isToast()) {
      if (innerParams.icon) {
        show(getIcon());
      }
    } else {
      showRelatedButton(domCache);
    }
    removeClass([domCache.popup, domCache.actions], swalClasses.loading);
    domCache.popup.removeAttribute('aria-busy');
    domCache.popup.removeAttribute('data-loading');
    domCache.confirmButton.disabled = false;
    domCache.denyButton.disabled = false;
    domCache.cancelButton.disabled = false;
  }
  const showRelatedButton = domCache => {
    const buttonToReplace = domCache.popup.getElementsByClassName(domCache.loader.getAttribute('data-button-to-replace'));
    if (buttonToReplace.length) {
      show(buttonToReplace[0], 'inline-block');
    } else if (allButtonsAreHidden()) {
      hide(domCache.actions);
    }
  };

  /**
   * Gets the input DOM node, this method works with input parameter.
   *
   * @returns {HTMLInputElement | null}
   */
  function getInput() {
    const innerParams = privateProps.innerParams.get(this);
    const domCache = privateProps.domCache.get(this);
    if (!domCache) {
      return null;
    }
    return getInput$1(domCache.popup, innerParams.input);
  }

  /**
   * @param {SweetAlert} instance
   * @param {string[]} buttons
   * @param {boolean} disabled
   */
  function setButtonsDisabled(instance, buttons, disabled) {
    const domCache = privateProps.domCache.get(instance);
    buttons.forEach(button => {
      domCache[button].disabled = disabled;
    });
  }

  /**
   * @param {HTMLInputElement | null} input
   * @param {boolean} disabled
   */
  function setInputDisabled(input, disabled) {
    const popup = getPopup();
    if (!popup || !input) {
      return;
    }
    if (input.type === 'radio') {
      /** @type {NodeListOf<HTMLInputElement>} */
      const radios = popup.querySelectorAll(`[name="${swalClasses.radio}"]`);
      for (let i = 0; i < radios.length; i++) {
        radios[i].disabled = disabled;
      }
    } else {
      input.disabled = disabled;
    }
  }

  /**
   * Enable all the buttons
   * @this {SweetAlert}
   */
  function enableButtons() {
    setButtonsDisabled(this, ['confirmButton', 'denyButton', 'cancelButton'], false);
  }

  /**
   * Disable all the buttons
   * @this {SweetAlert}
   */
  function disableButtons() {
    setButtonsDisabled(this, ['confirmButton', 'denyButton', 'cancelButton'], true);
  }

  /**
   * Enable the input field
   * @this {SweetAlert}
   */
  function enableInput() {
    setInputDisabled(this.getInput(), false);
  }

  /**
   * Disable the input field
   * @this {SweetAlert}
   */
  function disableInput() {
    setInputDisabled(this.getInput(), true);
  }

  /**
   * Show block with validation message
   *
   * @param {string} error
   * @this {SweetAlert}
   */
  function showValidationMessage(error) {
    const domCache = privateProps.domCache.get(this);
    const params = privateProps.innerParams.get(this);
    setInnerHtml(domCache.validationMessage, error);
    domCache.validationMessage.className = swalClasses['validation-message'];
    if (params.customClass && params.customClass.validationMessage) {
      addClass(domCache.validationMessage, params.customClass.validationMessage);
    }
    show(domCache.validationMessage);
    const input = this.getInput();
    if (input) {
      input.setAttribute('aria-invalid', 'true');
      input.setAttribute('aria-describedby', swalClasses['validation-message']);
      focusInput(input);
      addClass(input, swalClasses.inputerror);
    }
  }

  /**
   * Hide block with validation message
   *
   * @this {SweetAlert}
   */
  function resetValidationMessage() {
    const domCache = privateProps.domCache.get(this);
    if (domCache.validationMessage) {
      hide(domCache.validationMessage);
    }
    const input = this.getInput();
    if (input) {
      input.removeAttribute('aria-invalid');
      input.removeAttribute('aria-describedby');
      removeClass(input, swalClasses.inputerror);
    }
  }

  const defaultParams = {
    title: '',
    titleText: '',
    text: '',
    html: '',
    footer: '',
    icon: undefined,
    iconColor: undefined,
    iconHtml: undefined,
    template: undefined,
    toast: false,
    draggable: false,
    animation: true,
    theme: 'light',
    showClass: {
      popup: 'swal2-show',
      backdrop: 'swal2-backdrop-show',
      icon: 'swal2-icon-show'
    },
    hideClass: {
      popup: 'swal2-hide',
      backdrop: 'swal2-backdrop-hide',
      icon: 'swal2-icon-hide'
    },
    customClass: {},
    target: 'body',
    color: undefined,
    backdrop: true,
    heightAuto: true,
    allowOutsideClick: true,
    allowEscapeKey: true,
    allowEnterKey: true,
    stopKeydownPropagation: true,
    keydownListenerCapture: false,
    showConfirmButton: true,
    showDenyButton: false,
    showCancelButton: false,
    preConfirm: undefined,
    preDeny: undefined,
    confirmButtonText: 'OK',
    confirmButtonAriaLabel: '',
    confirmButtonColor: undefined,
    denyButtonText: 'No',
    denyButtonAriaLabel: '',
    denyButtonColor: undefined,
    cancelButtonText: 'Cancel',
    cancelButtonAriaLabel: '',
    cancelButtonColor: undefined,
    buttonsStyling: true,
    reverseButtons: false,
    focusConfirm: true,
    focusDeny: false,
    focusCancel: false,
    returnFocus: true,
    showCloseButton: false,
    closeButtonHtml: '&times;',
    closeButtonAriaLabel: 'Close this dialog',
    loaderHtml: '',
    showLoaderOnConfirm: false,
    showLoaderOnDeny: false,
    imageUrl: undefined,
    imageWidth: undefined,
    imageHeight: undefined,
    imageAlt: '',
    timer: undefined,
    timerProgressBar: false,
    width: undefined,
    padding: undefined,
    background: undefined,
    input: undefined,
    inputPlaceholder: '',
    inputLabel: '',
    inputValue: '',
    inputOptions: {},
    inputAutoFocus: true,
    inputAutoTrim: true,
    inputAttributes: {},
    inputValidator: undefined,
    returnInputValueOnDeny: false,
    validationMessage: undefined,
    grow: false,
    position: 'center',
    progressSteps: [],
    currentProgressStep: undefined,
    progressStepsDistance: undefined,
    willOpen: undefined,
    didOpen: undefined,
    didRender: undefined,
    willClose: undefined,
    didClose: undefined,
    didDestroy: undefined,
    scrollbarPadding: true,
    topLayer: false
  };
  const updatableParams = ['allowEscapeKey', 'allowOutsideClick', 'background', 'buttonsStyling', 'cancelButtonAriaLabel', 'cancelButtonColor', 'cancelButtonText', 'closeButtonAriaLabel', 'closeButtonHtml', 'color', 'confirmButtonAriaLabel', 'confirmButtonColor', 'confirmButtonText', 'currentProgressStep', 'customClass', 'denyButtonAriaLabel', 'denyButtonColor', 'denyButtonText', 'didClose', 'didDestroy', 'draggable', 'footer', 'hideClass', 'html', 'icon', 'iconColor', 'iconHtml', 'imageAlt', 'imageHeight', 'imageUrl', 'imageWidth', 'preConfirm', 'preDeny', 'progressSteps', 'returnFocus', 'reverseButtons', 'showCancelButton', 'showCloseButton', 'showConfirmButton', 'showDenyButton', 'text', 'title', 'titleText', 'theme', 'willClose'];

  /** @type {Record<string, string | undefined>} */
  const deprecatedParams = {
    allowEnterKey: undefined
  };
  const toastIncompatibleParams = ['allowOutsideClick', 'allowEnterKey', 'backdrop', 'draggable', 'focusConfirm', 'focusDeny', 'focusCancel', 'returnFocus', 'heightAuto', 'keydownListenerCapture'];

  /**
   * Is valid parameter
   *
   * @param {string} paramName
   * @returns {boolean}
   */
  const isValidParameter = paramName => {
    return Object.prototype.hasOwnProperty.call(defaultParams, paramName);
  };

  /**
   * Is valid parameter for Swal.update() method
   *
   * @param {string} paramName
   * @returns {boolean}
   */
  const isUpdatableParameter = paramName => {
    return updatableParams.indexOf(paramName) !== -1;
  };

  /**
   * Is deprecated parameter
   *
   * @param {string} paramName
   * @returns {string | undefined}
   */
  const isDeprecatedParameter = paramName => {
    return deprecatedParams[paramName];
  };

  /**
   * @param {string} param
   */
  const checkIfParamIsValid = param => {
    if (!isValidParameter(param)) {
      warn(`Unknown parameter "${param}"`);
    }
  };

  /**
   * @param {string} param
   */
  const checkIfToastParamIsValid = param => {
    if (toastIncompatibleParams.includes(param)) {
      warn(`The parameter "${param}" is incompatible with toasts`);
    }
  };

  /**
   * @param {string} param
   */
  const checkIfParamIsDeprecated = param => {
    const isDeprecated = isDeprecatedParameter(param);
    if (isDeprecated) {
      warnAboutDeprecation(param, isDeprecated);
    }
  };

  /**
   * Show relevant warnings for given params
   *
   * @param {SweetAlertOptions} params
   */
  const showWarningsForParams = params => {
    if (params.backdrop === false && params.allowOutsideClick) {
      warn('"allowOutsideClick" parameter requires `backdrop` parameter to be set to `true`');
    }
    if (params.theme && !['light', 'dark', 'auto', 'minimal', 'borderless', 'bootstrap-4', 'bootstrap-4-light', 'bootstrap-4-dark', 'bootstrap-5', 'bootstrap-5-light', 'bootstrap-5-dark', 'material-ui', 'material-ui-light', 'material-ui-dark', 'embed-iframe', 'bulma', 'bulma-light', 'bulma-dark'].includes(params.theme)) {
      warn(`Invalid theme "${params.theme}"`);
    }
    for (const param in params) {
      checkIfParamIsValid(param);
      if (params.toast) {
        checkIfToastParamIsValid(param);
      }
      checkIfParamIsDeprecated(param);
    }
  };

  /**
   * Updates popup parameters.
   *
   * @param {SweetAlertOptions} params
   */
  function update(params) {
    const container = getContainer();
    const popup = getPopup();
    const innerParams = privateProps.innerParams.get(this);
    if (!popup || hasClass(popup, innerParams.hideClass.popup)) {
      warn(`You're trying to update the closed or closing popup, that won't work. Use the update() method in preConfirm parameter or show a new popup.`);
      return;
    }
    const validUpdatableParams = filterValidParams(params);
    const updatedParams = Object.assign({}, innerParams, validUpdatableParams);
    showWarningsForParams(updatedParams);
    container.dataset['swal2Theme'] = updatedParams.theme;
    render(this, updatedParams);
    privateProps.innerParams.set(this, updatedParams);
    Object.defineProperties(this, {
      params: {
        value: Object.assign({}, this.params, params),
        writable: false,
        enumerable: true
      }
    });
  }

  /**
   * @param {SweetAlertOptions} params
   * @returns {SweetAlertOptions}
   */
  const filterValidParams = params => {
    const validUpdatableParams = {};
    Object.keys(params).forEach(param => {
      if (isUpdatableParameter(param)) {
        validUpdatableParams[param] = params[param];
      } else {
        warn(`Invalid parameter to update: ${param}`);
      }
    });
    return validUpdatableParams;
  };

  /**
   * Dispose the current SweetAlert2 instance
   */
  function _destroy() {
    const domCache = privateProps.domCache.get(this);
    const innerParams = privateProps.innerParams.get(this);
    if (!innerParams) {
      disposeWeakMaps(this); // The WeakMaps might have been partly destroyed, we must recall it to dispose any remaining WeakMaps #2335
      return; // This instance has already been destroyed
    }

    // Check if there is another Swal closing
    if (domCache.popup && globalState.swalCloseEventFinishedCallback) {
      globalState.swalCloseEventFinishedCallback();
      delete globalState.swalCloseEventFinishedCallback;
    }
    if (typeof innerParams.didDestroy === 'function') {
      innerParams.didDestroy();
    }
    globalState.eventEmitter.emit('didDestroy');
    disposeSwal(this);
  }

  /**
   * @param {SweetAlert} instance
   */
  const disposeSwal = instance => {
    disposeWeakMaps(instance);
    // Unset this.params so GC will dispose it (#1569)
    delete instance.params;
    // Unset globalState props so GC will dispose globalState (#1569)
    delete globalState.keydownHandler;
    delete globalState.keydownTarget;
    // Unset currentInstance
    delete globalState.currentInstance;
  };

  /**
   * @param {SweetAlert} instance
   */
  const disposeWeakMaps = instance => {
    // If the current instance is awaiting a promise result, we keep the privateMethods to call them once the promise result is retrieved #2335
    if (instance.isAwaitingPromise) {
      unsetWeakMaps(privateProps, instance);
      instance.isAwaitingPromise = true;
    } else {
      unsetWeakMaps(privateMethods, instance);
      unsetWeakMaps(privateProps, instance);
      delete instance.isAwaitingPromise;
      // Unset instance methods
      delete instance.disableButtons;
      delete instance.enableButtons;
      delete instance.getInput;
      delete instance.disableInput;
      delete instance.enableInput;
      delete instance.hideLoading;
      delete instance.disableLoading;
      delete instance.showValidationMessage;
      delete instance.resetValidationMessage;
      delete instance.close;
      delete instance.closePopup;
      delete instance.closeModal;
      delete instance.closeToast;
      delete instance.rejectPromise;
      delete instance.update;
      delete instance._destroy;
    }
  };

  /**
   * @param {object} obj
   * @param {SweetAlert} instance
   */
  const unsetWeakMaps = (obj, instance) => {
    for (const i in obj) {
      obj[i].delete(instance);
    }
  };

  var instanceMethods = /*#__PURE__*/Object.freeze({
    __proto__: null,
    _destroy: _destroy,
    close: close,
    closeModal: close,
    closePopup: close,
    closeToast: close,
    disableButtons: disableButtons,
    disableInput: disableInput,
    disableLoading: hideLoading,
    enableButtons: enableButtons,
    enableInput: enableInput,
    getInput: getInput,
    handleAwaitingPromise: handleAwaitingPromise,
    hideLoading: hideLoading,
    rejectPromise: rejectPromise,
    resetValidationMessage: resetValidationMessage,
    showValidationMessage: showValidationMessage,
    update: update
  });

  /**
   * @param {SweetAlertOptions} innerParams
   * @param {DomCache} domCache
   * @param {(dismiss: DismissReason) => void} dismissWith
   */
  const handlePopupClick = (innerParams, domCache, dismissWith) => {
    if (innerParams.toast) {
      handleToastClick(innerParams, domCache, dismissWith);
    } else {
      // Ignore click events that had mousedown on the popup but mouseup on the container
      // This can happen when the user drags a slider
      handleModalMousedown(domCache);

      // Ignore click events that had mousedown on the container but mouseup on the popup
      handleContainerMousedown(domCache);
      handleModalClick(innerParams, domCache, dismissWith);
    }
  };

  /**
   * @param {SweetAlertOptions} innerParams
   * @param {DomCache} domCache
   * @param {(dismiss: DismissReason) => void} dismissWith
   */
  const handleToastClick = (innerParams, domCache, dismissWith) => {
    // Closing toast by internal click
    domCache.popup.onclick = () => {
      if (innerParams && (isAnyButtonShown(innerParams) || innerParams.timer || innerParams.input)) {
        return;
      }
      dismissWith(DismissReason.close);
    };
  };

  /**
   * @param {SweetAlertOptions} innerParams
   * @returns {boolean}
   */
  const isAnyButtonShown = innerParams => {
    return !!(innerParams.showConfirmButton || innerParams.showDenyButton || innerParams.showCancelButton || innerParams.showCloseButton);
  };
  let ignoreOutsideClick = false;

  /**
   * @param {DomCache} domCache
   */
  const handleModalMousedown = domCache => {
    domCache.popup.onmousedown = () => {
      domCache.container.onmouseup = function (e) {
        domCache.container.onmouseup = () => {};
        // We only check if the mouseup target is the container because usually it doesn't
        // have any other direct children aside of the popup
        if (e.target === domCache.container) {
          ignoreOutsideClick = true;
        }
      };
    };
  };

  /**
   * @param {DomCache} domCache
   */
  const handleContainerMousedown = domCache => {
    domCache.container.onmousedown = e => {
      // prevent the modal text from being selected on double click on the container (allowOutsideClick: false)
      if (e.target === domCache.container) {
        e.preventDefault();
      }
      domCache.popup.onmouseup = function (e) {
        domCache.popup.onmouseup = () => {};
        // We also need to check if the mouseup target is a child of the popup
        if (e.target === domCache.popup || e.target instanceof HTMLElement && domCache.popup.contains(e.target)) {
          ignoreOutsideClick = true;
        }
      };
    };
  };

  /**
   * @param {SweetAlertOptions} innerParams
   * @param {DomCache} domCache
   * @param {(dismiss: DismissReason) => void} dismissWith
   */
  const handleModalClick = (innerParams, domCache, dismissWith) => {
    domCache.container.onclick = e => {
      if (ignoreOutsideClick) {
        ignoreOutsideClick = false;
        return;
      }
      if (e.target === domCache.container && callIfFunction(innerParams.allowOutsideClick)) {
        dismissWith(DismissReason.backdrop);
      }
    };
  };

  const isJqueryElement = elem => typeof elem === 'object' && elem.jquery;
  const isElement = elem => elem instanceof Element || isJqueryElement(elem);
  const argsToParams = args => {
    const params = {};
    if (typeof args[0] === 'object' && !isElement(args[0])) {
      Object.assign(params, args[0]);
    } else {
      ['title', 'html', 'icon'].forEach((name, index) => {
        const arg = args[index];
        if (typeof arg === 'string' || isElement(arg)) {
          params[name] = arg;
        } else if (arg !== undefined) {
          error(`Unexpected type of ${name}! Expected "string" or "Element", got ${typeof arg}`);
        }
      });
    }
    return params;
  };

  /**
   * Main method to create a new SweetAlert2 popup
   *
   * @param  {...SweetAlertOptions} args
   * @returns {Promise<SweetAlertResult>}
   */
  function fire(...args) {
    return new this(...args);
  }

  /**
   * Returns an extended version of `Swal` containing `params` as defaults.
   * Useful for reusing Swal configuration.
   *
   * For example:
   *
   * Before:
   * const textPromptOptions = { input: 'text', showCancelButton: true }
   * const {value: firstName} = await Swal.fire({ ...textPromptOptions, title: 'What is your first name?' })
   * const {value: lastName} = await Swal.fire({ ...textPromptOptions, title: 'What is your last name?' })
   *
   * After:
   * const TextPrompt = Swal.mixin({ input: 'text', showCancelButton: true })
   * const {value: firstName} = await TextPrompt('What is your first name?')
   * const {value: lastName} = await TextPrompt('What is your last name?')
   *
   * @param {SweetAlertOptions} mixinParams
   * @returns {SweetAlert}
   */
  function mixin(mixinParams) {
    class MixinSwal extends this {
      _main(params, priorityMixinParams) {
        return super._main(params, Object.assign({}, mixinParams, priorityMixinParams));
      }
    }
    // @ts-ignore
    return MixinSwal;
  }

  /**
   * If `timer` parameter is set, returns number of milliseconds of timer remained.
   * Otherwise, returns undefined.
   *
   * @returns {number | undefined}
   */
  const getTimerLeft = () => {
    return globalState.timeout && globalState.timeout.getTimerLeft();
  };

  /**
   * Stop timer. Returns number of milliseconds of timer remained.
   * If `timer` parameter isn't set, returns undefined.
   *
   * @returns {number | undefined}
   */
  const stopTimer = () => {
    if (globalState.timeout) {
      stopTimerProgressBar();
      return globalState.timeout.stop();
    }
  };

  /**
   * Resume timer. Returns number of milliseconds of timer remained.
   * If `timer` parameter isn't set, returns undefined.
   *
   * @returns {number | undefined}
   */
  const resumeTimer = () => {
    if (globalState.timeout) {
      const remaining = globalState.timeout.start();
      animateTimerProgressBar(remaining);
      return remaining;
    }
  };

  /**
   * Resume timer. Returns number of milliseconds of timer remained.
   * If `timer` parameter isn't set, returns undefined.
   *
   * @returns {number | undefined}
   */
  const toggleTimer = () => {
    const timer = globalState.timeout;
    return timer && (timer.running ? stopTimer() : resumeTimer());
  };

  /**
   * Increase timer. Returns number of milliseconds of an updated timer.
   * If `timer` parameter isn't set, returns undefined.
   *
   * @param {number} ms
   * @returns {number | undefined}
   */
  const increaseTimer = ms => {
    if (globalState.timeout) {
      const remaining = globalState.timeout.increase(ms);
      animateTimerProgressBar(remaining, true);
      return remaining;
    }
  };

  /**
   * Check if timer is running. Returns true if timer is running
   * or false if timer is paused or stopped.
   * If `timer` parameter isn't set, returns undefined
   *
   * @returns {boolean}
   */
  const isTimerRunning = () => {
    return !!(globalState.timeout && globalState.timeout.isRunning());
  };

  let bodyClickListenerAdded = false;
  const clickHandlers = {};

  /**
   * @param {string} attr
   */
  function bindClickHandler(attr = 'data-swal-template') {
    clickHandlers[attr] = this;
    if (!bodyClickListenerAdded) {
      document.body.addEventListener('click', bodyClickListener);
      bodyClickListenerAdded = true;
    }
  }
  const bodyClickListener = event => {
    for (let el = event.target; el && el !== document; el = el.parentNode) {
      for (const attr in clickHandlers) {
        const template = el.getAttribute(attr);
        if (template) {
          clickHandlers[attr].fire({
            template
          });
          return;
        }
      }
    }
  };

  // Source: https://gist.github.com/mudge/5830382?permalink_comment_id=2691957#gistcomment-2691957

  class EventEmitter {
    constructor() {
      /** @type {Events} */
      this.events = {};
    }

    /**
     * @param {string} eventName
     * @returns {EventHandlers}
     */
    _getHandlersByEventName(eventName) {
      if (typeof this.events[eventName] === 'undefined') {
        // not Set because we need to keep the FIFO order
        // https://github.com/sweetalert2/sweetalert2/pull/2763#discussion_r1748990334
        this.events[eventName] = [];
      }
      return this.events[eventName];
    }

    /**
     * @param {string} eventName
     * @param {EventHandler} eventHandler
     */
    on(eventName, eventHandler) {
      const currentHandlers = this._getHandlersByEventName(eventName);
      if (!currentHandlers.includes(eventHandler)) {
        currentHandlers.push(eventHandler);
      }
    }

    /**
     * @param {string} eventName
     * @param {EventHandler} eventHandler
     */
    once(eventName, eventHandler) {
      /**
       * @param {Array} args
       */
      const onceFn = (...args) => {
        this.removeListener(eventName, onceFn);
        eventHandler.apply(this, args);
      };
      this.on(eventName, onceFn);
    }

    /**
     * @param {string} eventName
     * @param {Array} args
     */
    emit(eventName, ...args) {
      this._getHandlersByEventName(eventName).forEach(
      /**
       * @param {EventHandler} eventHandler
       */
      eventHandler => {
        try {
          eventHandler.apply(this, args);
        } catch (error) {
          console.error(error);
        }
      });
    }

    /**
     * @param {string} eventName
     * @param {EventHandler} eventHandler
     */
    removeListener(eventName, eventHandler) {
      const currentHandlers = this._getHandlersByEventName(eventName);
      const index = currentHandlers.indexOf(eventHandler);
      if (index > -1) {
        currentHandlers.splice(index, 1);
      }
    }

    /**
     * @param {string} eventName
     */
    removeAllListeners(eventName) {
      if (this.events[eventName] !== undefined) {
        // https://github.com/sweetalert2/sweetalert2/pull/2763#discussion_r1749239222
        this.events[eventName].length = 0;
      }
    }
    reset() {
      this.events = {};
    }
  }

  globalState.eventEmitter = new EventEmitter();

  /**
   * @param {string} eventName
   * @param {EventHandler} eventHandler
   */
  const on = (eventName, eventHandler) => {
    globalState.eventEmitter.on(eventName, eventHandler);
  };

  /**
   * @param {string} eventName
   * @param {EventHandler} eventHandler
   */
  const once = (eventName, eventHandler) => {
    globalState.eventEmitter.once(eventName, eventHandler);
  };

  /**
   * @param {string} [eventName]
   * @param {EventHandler} [eventHandler]
   */
  const off = (eventName, eventHandler) => {
    // Remove all handlers for all events
    if (!eventName) {
      globalState.eventEmitter.reset();
      return;
    }
    if (eventHandler) {
      // Remove a specific handler
      globalState.eventEmitter.removeListener(eventName, eventHandler);
    } else {
      // Remove all handlers for a specific event
      globalState.eventEmitter.removeAllListeners(eventName);
    }
  };

  var staticMethods = /*#__PURE__*/Object.freeze({
    __proto__: null,
    argsToParams: argsToParams,
    bindClickHandler: bindClickHandler,
    clickCancel: clickCancel,
    clickConfirm: clickConfirm,
    clickDeny: clickDeny,
    enableLoading: showLoading,
    fire: fire,
    getActions: getActions,
    getCancelButton: getCancelButton,
    getCloseButton: getCloseButton,
    getConfirmButton: getConfirmButton,
    getContainer: getContainer,
    getDenyButton: getDenyButton,
    getFocusableElements: getFocusableElements,
    getFooter: getFooter,
    getHtmlContainer: getHtmlContainer,
    getIcon: getIcon,
    getIconContent: getIconContent,
    getImage: getImage,
    getInputLabel: getInputLabel,
    getLoader: getLoader,
    getPopup: getPopup,
    getProgressSteps: getProgressSteps,
    getTimerLeft: getTimerLeft,
    getTimerProgressBar: getTimerProgressBar,
    getTitle: getTitle,
    getValidationMessage: getValidationMessage,
    increaseTimer: increaseTimer,
    isDeprecatedParameter: isDeprecatedParameter,
    isLoading: isLoading,
    isTimerRunning: isTimerRunning,
    isUpdatableParameter: isUpdatableParameter,
    isValidParameter: isValidParameter,
    isVisible: isVisible,
    mixin: mixin,
    off: off,
    on: on,
    once: once,
    resumeTimer: resumeTimer,
    showLoading: showLoading,
    stopTimer: stopTimer,
    toggleTimer: toggleTimer
  });

  class Timer {
    /**
     * @param {() => void} callback
     * @param {number} delay
     */
    constructor(callback, delay) {
      this.callback = callback;
      this.remaining = delay;
      this.running = false;
      this.start();
    }

    /**
     * @returns {number}
     */
    start() {
      if (!this.running) {
        this.running = true;
        this.started = new Date();
        this.id = setTimeout(this.callback, this.remaining);
      }
      return this.remaining;
    }

    /**
     * @returns {number}
     */
    stop() {
      if (this.started && this.running) {
        this.running = false;
        clearTimeout(this.id);
        this.remaining -= new Date().getTime() - this.started.getTime();
      }
      return this.remaining;
    }

    /**
     * @param {number} n
     * @returns {number}
     */
    increase(n) {
      const running = this.running;
      if (running) {
        this.stop();
      }
      this.remaining += n;
      if (running) {
        this.start();
      }
      return this.remaining;
    }

    /**
     * @returns {number}
     */
    getTimerLeft() {
      if (this.running) {
        this.stop();
        this.start();
      }
      return this.remaining;
    }

    /**
     * @returns {boolean}
     */
    isRunning() {
      return this.running;
    }
  }

  const swalStringParams = ['swal-title', 'swal-html', 'swal-footer'];

  /**
   * @param {SweetAlertOptions} params
   * @returns {SweetAlertOptions}
   */
  const getTemplateParams = params => {
    const template = typeof params.template === 'string' ? (/** @type {HTMLTemplateElement} */document.querySelector(params.template)) : params.template;
    if (!template) {
      return {};
    }
    /** @type {DocumentFragment} */
    const templateContent = template.content;
    showWarningsForElements(templateContent);
    const result = Object.assign(getSwalParams(templateContent), getSwalFunctionParams(templateContent), getSwalButtons(templateContent), getSwalImage(templateContent), getSwalIcon(templateContent), getSwalInput(templateContent), getSwalStringParams(templateContent, swalStringParams));
    return result;
  };

  /**
   * @param {DocumentFragment} templateContent
   * @returns {Record<string, string | boolean | number>}
   */
  const getSwalParams = templateContent => {
    /** @type {Record<string, string | boolean | number>} */
    const result = {};
    /** @type {HTMLElement[]} */
    const swalParams = Array.from(templateContent.querySelectorAll('swal-param'));
    swalParams.forEach(param => {
      showWarningsForAttributes(param, ['name', 'value']);
      const paramName = /** @type {keyof SweetAlertOptions} */param.getAttribute('name');
      const value = param.getAttribute('value');
      if (!paramName || !value) {
        return;
      }
      if (typeof defaultParams[paramName] === 'boolean') {
        result[paramName] = value !== 'false';
      } else if (typeof defaultParams[paramName] === 'object') {
        result[paramName] = JSON.parse(value);
      } else {
        result[paramName] = value;
      }
    });
    return result;
  };

  /**
   * @param {DocumentFragment} templateContent
   * @returns {Record<string, () => void>}
   */
  const getSwalFunctionParams = templateContent => {
    /** @type {Record<string, () => void>} */
    const result = {};
    /** @type {HTMLElement[]} */
    const swalFunctions = Array.from(templateContent.querySelectorAll('swal-function-param'));
    swalFunctions.forEach(param => {
      const paramName = /** @type {keyof SweetAlertOptions} */param.getAttribute('name');
      const value = param.getAttribute('value');
      if (!paramName || !value) {
        return;
      }
      result[paramName] = new Function(`return ${value}`)();
    });
    return result;
  };

  /**
   * @param {DocumentFragment} templateContent
   * @returns {Record<string, string | boolean>}
   */
  const getSwalButtons = templateContent => {
    /** @type {Record<string, string | boolean>} */
    const result = {};
    /** @type {HTMLElement[]} */
    const swalButtons = Array.from(templateContent.querySelectorAll('swal-button'));
    swalButtons.forEach(button => {
      showWarningsForAttributes(button, ['type', 'color', 'aria-label']);
      const type = button.getAttribute('type');
      if (!type || !['confirm', 'cancel', 'deny'].includes(type)) {
        return;
      }
      result[`${type}ButtonText`] = button.innerHTML;
      result[`show${capitalizeFirstLetter(type)}Button`] = true;
      if (button.hasAttribute('color')) {
        result[`${type}ButtonColor`] = button.getAttribute('color');
      }
      if (button.hasAttribute('aria-label')) {
        result[`${type}ButtonAriaLabel`] = button.getAttribute('aria-label');
      }
    });
    return result;
  };

  /**
   * @param {DocumentFragment} templateContent
   * @returns {Pick<SweetAlertOptions, 'imageUrl' | 'imageWidth' | 'imageHeight' | 'imageAlt'>}
   */
  const getSwalImage = templateContent => {
    const result = {};
    /** @type {HTMLElement | null} */
    const image = templateContent.querySelector('swal-image');
    if (image) {
      showWarningsForAttributes(image, ['src', 'width', 'height', 'alt']);
      if (image.hasAttribute('src')) {
        result.imageUrl = image.getAttribute('src') || undefined;
      }
      if (image.hasAttribute('width')) {
        result.imageWidth = image.getAttribute('width') || undefined;
      }
      if (image.hasAttribute('height')) {
        result.imageHeight = image.getAttribute('height') || undefined;
      }
      if (image.hasAttribute('alt')) {
        result.imageAlt = image.getAttribute('alt') || undefined;
      }
    }
    return result;
  };

  /**
   * @param {DocumentFragment} templateContent
   * @returns {object}
   */
  const getSwalIcon = templateContent => {
    const result = {};
    /** @type {HTMLElement | null} */
    const icon = templateContent.querySelector('swal-icon');
    if (icon) {
      showWarningsForAttributes(icon, ['type', 'color']);
      if (icon.hasAttribute('type')) {
        result.icon = icon.getAttribute('type');
      }
      if (icon.hasAttribute('color')) {
        result.iconColor = icon.getAttribute('color');
      }
      result.iconHtml = icon.innerHTML;
    }
    return result;
  };

  /**
   * @param {DocumentFragment} templateContent
   * @returns {object}
   */
  const getSwalInput = templateContent => {
    /** @type {object} */
    const result = {};
    /** @type {HTMLElement | null} */
    const input = templateContent.querySelector('swal-input');
    if (input) {
      showWarningsForAttributes(input, ['type', 'label', 'placeholder', 'value']);
      result.input = input.getAttribute('type') || 'text';
      if (input.hasAttribute('label')) {
        result.inputLabel = input.getAttribute('label');
      }
      if (input.hasAttribute('placeholder')) {
        result.inputPlaceholder = input.getAttribute('placeholder');
      }
      if (input.hasAttribute('value')) {
        result.inputValue = input.getAttribute('value');
      }
    }
    /** @type {HTMLElement[]} */
    const inputOptions = Array.from(templateContent.querySelectorAll('swal-input-option'));
    if (inputOptions.length) {
      result.inputOptions = {};
      inputOptions.forEach(option => {
        showWarningsForAttributes(option, ['value']);
        const optionValue = option.getAttribute('value');
        if (!optionValue) {
          return;
        }
        const optionName = option.innerHTML;
        result.inputOptions[optionValue] = optionName;
      });
    }
    return result;
  };

  /**
   * @param {DocumentFragment} templateContent
   * @param {string[]} paramNames
   * @returns {Record<string, string>}
   */
  const getSwalStringParams = (templateContent, paramNames) => {
    /** @type {Record<string, string>} */
    const result = {};
    for (const i in paramNames) {
      const paramName = paramNames[i];
      /** @type {HTMLElement | null} */
      const tag = templateContent.querySelector(paramName);
      if (tag) {
        showWarningsForAttributes(tag, []);
        result[paramName.replace(/^swal-/, '')] = tag.innerHTML.trim();
      }
    }
    return result;
  };

  /**
   * @param {DocumentFragment} templateContent
   */
  const showWarningsForElements = templateContent => {
    const allowedElements = swalStringParams.concat(['swal-param', 'swal-function-param', 'swal-button', 'swal-image', 'swal-icon', 'swal-input', 'swal-input-option']);
    Array.from(templateContent.children).forEach(el => {
      const tagName = el.tagName.toLowerCase();
      if (!allowedElements.includes(tagName)) {
        warn(`Unrecognized element <${tagName}>`);
      }
    });
  };

  /**
   * @param {HTMLElement} el
   * @param {string[]} allowedAttributes
   */
  const showWarningsForAttributes = (el, allowedAttributes) => {
    Array.from(el.attributes).forEach(attribute => {
      if (allowedAttributes.indexOf(attribute.name) === -1) {
        warn([`Unrecognized attribute "${attribute.name}" on <${el.tagName.toLowerCase()}>.`, `${allowedAttributes.length ? `Allowed attributes are: ${allowedAttributes.join(', ')}` : 'To set the value, use HTML within the element.'}`]);
      }
    });
  };

  const SHOW_CLASS_TIMEOUT = 10;

  /**
   * Open popup, add necessary classes and styles, fix scrollbar
   *
   * @param {SweetAlertOptions} params
   */
  const openPopup = params => {
    const container = getContainer();
    const popup = getPopup();
    if (typeof params.willOpen === 'function') {
      params.willOpen(popup);
    }
    globalState.eventEmitter.emit('willOpen', popup);
    const bodyStyles = window.getComputedStyle(document.body);
    const initialBodyOverflow = bodyStyles.overflowY;
    addClasses(container, popup, params);

    // scrolling is 'hidden' until animation is done, after that 'auto'
    setTimeout(() => {
      setScrollingVisibility(container, popup);
    }, SHOW_CLASS_TIMEOUT);
    if (isModal()) {
      fixScrollContainer(container, params.scrollbarPadding, initialBodyOverflow);
      setAriaHidden();
    }
    if (!isToast() && !globalState.previousActiveElement) {
      globalState.previousActiveElement = document.activeElement;
    }
    if (typeof params.didOpen === 'function') {
      setTimeout(() => params.didOpen(popup));
    }
    globalState.eventEmitter.emit('didOpen', popup);
  };

  /**
   * @param {AnimationEvent} event
   */
  const swalOpenAnimationFinished = event => {
    const popup = getPopup();
    if (event.target !== popup) {
      return;
    }
    const container = getContainer();
    popup.removeEventListener('animationend', swalOpenAnimationFinished);
    popup.removeEventListener('transitionend', swalOpenAnimationFinished);
    container.style.overflowY = 'auto';

    // no-transition is added in init() in case one swal is opened right after another
    removeClass(container, swalClasses['no-transition']);
  };

  /**
   * @param {HTMLElement} container
   * @param {HTMLElement} popup
   */
  const setScrollingVisibility = (container, popup) => {
    if (hasCssAnimation(popup)) {
      container.style.overflowY = 'hidden';
      popup.addEventListener('animationend', swalOpenAnimationFinished);
      popup.addEventListener('transitionend', swalOpenAnimationFinished);
    } else {
      container.style.overflowY = 'auto';
    }
  };

  /**
   * @param {HTMLElement} container
   * @param {boolean} scrollbarPadding
   * @param {string} initialBodyOverflow
   */
  const fixScrollContainer = (container, scrollbarPadding, initialBodyOverflow) => {
    iOSfix();
    if (scrollbarPadding && initialBodyOverflow !== 'hidden') {
      replaceScrollbarWithPadding(initialBodyOverflow);
    }

    // sweetalert2/issues/1247
    setTimeout(() => {
      container.scrollTop = 0;
    });
  };

  /**
   * @param {HTMLElement} container
   * @param {HTMLElement} popup
   * @param {SweetAlertOptions} params
   */
  const addClasses = (container, popup, params) => {
    addClass(container, params.showClass.backdrop);
    if (params.animation) {
      // this workaround with opacity is needed for https://github.com/sweetalert2/sweetalert2/issues/2059
      popup.style.setProperty('opacity', '0', 'important');
      show(popup, 'grid');
      setTimeout(() => {
        // Animate popup right after showing it
        addClass(popup, params.showClass.popup);
        // and remove the opacity workaround
        popup.style.removeProperty('opacity');
      }, SHOW_CLASS_TIMEOUT); // 10ms in order to fix #2062
    } else {
      show(popup, 'grid');
    }
    addClass([document.documentElement, document.body], swalClasses.shown);
    if (params.heightAuto && params.backdrop && !params.toast) {
      addClass([document.documentElement, document.body], swalClasses['height-auto']);
    }
  };

  var defaultInputValidators = {
    /**
     * @param {string} string
     * @param {string} [validationMessage]
     * @returns {Promise<string | void>}
     */
    email: (string, validationMessage) => {
      return /^[a-zA-Z0-9.+_'-]+@[a-zA-Z0-9.-]+\.[a-zA-Z0-9-]+$/.test(string) ? Promise.resolve() : Promise.resolve(validationMessage || 'Invalid email address');
    },
    /**
     * @param {string} string
     * @param {string} [validationMessage]
     * @returns {Promise<string | void>}
     */
    url: (string, validationMessage) => {
      // taken from https://stackoverflow.com/a/3809435 with a small change from #1306 and #2013
      return /^https?:\/\/(www\.)?[-a-zA-Z0-9@:%._+~#=]{1,256}\.[a-z]{2,63}\b([-a-zA-Z0-9@:%_+.~#?&/=]*)$/.test(string) ? Promise.resolve() : Promise.resolve(validationMessage || 'Invalid URL');
    }
  };

  /**
   * @param {SweetAlertOptions} params
   */
  function setDefaultInputValidators(params) {
    // Use default `inputValidator` for supported input types if not provided
    if (params.inputValidator) {
      return;
    }
    if (params.input === 'email') {
      params.inputValidator = defaultInputValidators['email'];
    }
    if (params.input === 'url') {
      params.inputValidator = defaultInputValidators['url'];
    }
  }

  /**
   * @param {SweetAlertOptions} params
   */
  function validateCustomTargetElement(params) {
    // Determine if the custom target element is valid
    if (!params.target || typeof params.target === 'string' && !document.querySelector(params.target) || typeof params.target !== 'string' && !params.target.appendChild) {
      warn('Target parameter is not valid, defaulting to "body"');
      params.target = 'body';
    }
  }

  /**
   * Set type, text and actions on popup
   *
   * @param {SweetAlertOptions} params
   */
  function setParameters(params) {
    setDefaultInputValidators(params);

    // showLoaderOnConfirm && preConfirm
    if (params.showLoaderOnConfirm && !params.preConfirm) {
      warn('showLoaderOnConfirm is set to true, but preConfirm is not defined.\n' + 'showLoaderOnConfirm should be used together with preConfirm, see usage example:\n' + 'https://sweetalert2.github.io/#ajax-request');
    }
    validateCustomTargetElement(params);

    // Replace newlines with <br> in title
    if (typeof params.title === 'string') {
      params.title = params.title.split('\n').join('<br />');
    }
    init(params);
  }

  /** @type {SweetAlert} */
  let currentInstance;
  var _promise = /*#__PURE__*/new WeakMap();
  class SweetAlert {
    /**
     * @param {...(SweetAlertOptions | string)} args
     * @this {SweetAlert}
     */
    constructor(...args) {
      /**
       * @type {Promise<SweetAlertResult>}
       */
      _classPrivateFieldInitSpec(this, _promise, void 0);
      // Prevent run in Node env
      if (typeof window === 'undefined') {
        return;
      }
      currentInstance = this;

      // @ts-ignore
      const outerParams = Object.freeze(this.constructor.argsToParams(args));

      /** @type {Readonly<SweetAlertOptions>} */
      this.params = outerParams;

      /** @type {boolean} */
      this.isAwaitingPromise = false;
      _classPrivateFieldSet2(_promise, this, this._main(currentInstance.params));
    }
    _main(userParams, mixinParams = {}) {
      showWarningsForParams(Object.assign({}, mixinParams, userParams));
      if (globalState.currentInstance) {
        const swalPromiseResolve = privateMethods.swalPromiseResolve.get(globalState.currentInstance);
        const {
          isAwaitingPromise
        } = globalState.currentInstance;
        globalState.currentInstance._destroy();
        if (!isAwaitingPromise) {
          swalPromiseResolve({
            isDismissed: true
          });
        }
        if (isModal()) {
          unsetAriaHidden();
        }
      }
      globalState.currentInstance = currentInstance;
      const innerParams = prepareParams(userParams, mixinParams);
      setParameters(innerParams);
      Object.freeze(innerParams);

      // clear the previous timer
      if (globalState.timeout) {
        globalState.timeout.stop();
        delete globalState.timeout;
      }

      // clear the restore focus timeout
      clearTimeout(globalState.restoreFocusTimeout);
      const domCache = populateDomCache(currentInstance);
      render(currentInstance, innerParams);
      privateProps.innerParams.set(currentInstance, innerParams);
      return swalPromise(currentInstance, domCache, innerParams);
    }

    // `catch` cannot be the name of a module export, so we define our thenable methods here instead
    then(onFulfilled) {
      return _classPrivateFieldGet2(_promise, this).then(onFulfilled);
    }
    finally(onFinally) {
      return _classPrivateFieldGet2(_promise, this).finally(onFinally);
    }
  }

  /**
   * @param {SweetAlert} instance
   * @param {DomCache} domCache
   * @param {SweetAlertOptions} innerParams
   * @returns {Promise}
   */
  const swalPromise = (instance, domCache, innerParams) => {
    return new Promise((resolve, reject) => {
      // functions to handle all closings/dismissals
      /**
       * @param {DismissReason} dismiss
       */
      const dismissWith = dismiss => {
        instance.close({
          isDismissed: true,
          dismiss,
          isConfirmed: false,
          isDenied: false
        });
      };
      privateMethods.swalPromiseResolve.set(instance, resolve);
      privateMethods.swalPromiseReject.set(instance, reject);
      domCache.confirmButton.onclick = () => {
        handleConfirmButtonClick(instance);
      };
      domCache.denyButton.onclick = () => {
        handleDenyButtonClick(instance);
      };
      domCache.cancelButton.onclick = () => {
        handleCancelButtonClick(instance, dismissWith);
      };
      domCache.closeButton.onclick = () => {
        dismissWith(DismissReason.close);
      };
      handlePopupClick(innerParams, domCache, dismissWith);
      addKeydownHandler(globalState, innerParams, dismissWith);
      handleInputOptionsAndValue(instance, innerParams);
      openPopup(innerParams);
      setupTimer(globalState, innerParams, dismissWith);
      initFocus(domCache, innerParams);

      // Scroll container to top on open (#1247, #1946)
      setTimeout(() => {
        domCache.container.scrollTop = 0;
      });
    });
  };

  /**
   * @param {SweetAlertOptions} userParams
   * @param {SweetAlertOptions} mixinParams
   * @returns {SweetAlertOptions}
   */
  const prepareParams = (userParams, mixinParams) => {
    const templateParams = getTemplateParams(userParams);
    const params = Object.assign({}, defaultParams, mixinParams, templateParams, userParams); // precedence is described in #2131
    params.showClass = Object.assign({}, defaultParams.showClass, params.showClass);
    params.hideClass = Object.assign({}, defaultParams.hideClass, params.hideClass);
    if (params.animation === false) {
      params.showClass = {
        backdrop: 'swal2-noanimation'
      };
      params.hideClass = {};
    }
    return params;
  };

  /**
   * @param {SweetAlert} instance
   * @returns {DomCache}
   */
  const populateDomCache = instance => {
    const domCache = {
      popup: getPopup(),
      container: getContainer(),
      actions: getActions(),
      confirmButton: getConfirmButton(),
      denyButton: getDenyButton(),
      cancelButton: getCancelButton(),
      loader: getLoader(),
      closeButton: getCloseButton(),
      validationMessage: getValidationMessage(),
      progressSteps: getProgressSteps()
    };
    privateProps.domCache.set(instance, domCache);
    return domCache;
  };

  /**
   * @param {GlobalState} globalState
   * @param {SweetAlertOptions} innerParams
   * @param {(dismiss: DismissReason) => void} dismissWith
   */
  const setupTimer = (globalState, innerParams, dismissWith) => {
    const timerProgressBar = getTimerProgressBar();
    hide(timerProgressBar);
    if (innerParams.timer) {
      globalState.timeout = new Timer(() => {
        dismissWith('timer');
        delete globalState.timeout;
      }, innerParams.timer);
      if (innerParams.timerProgressBar) {
        show(timerProgressBar);
        applyCustomClass(timerProgressBar, innerParams, 'timerProgressBar');
        setTimeout(() => {
          if (globalState.timeout && globalState.timeout.running) {
            // timer can be already stopped or unset at this point
            animateTimerProgressBar(innerParams.timer);
          }
        });
      }
    }
  };

  /**
   * Initialize focus in the popup:
   *
   * 1. If `toast` is `true`, don't steal focus from the document.
   * 2. Else if there is an [autofocus] element, focus it.
   * 3. Else if `focusConfirm` is `true` and confirm button is visible, focus it.
   * 4. Else if `focusDeny` is `true` and deny button is visible, focus it.
   * 5. Else if `focusCancel` is `true` and cancel button is visible, focus it.
   * 6. Else focus the first focusable element in a popup (if any).
   *
   * @param {DomCache} domCache
   * @param {SweetAlertOptions} innerParams
   */
  const initFocus = (domCache, innerParams) => {
    if (innerParams.toast) {
      return;
    }
    // TODO: this is dumb, remove `allowEnterKey` param in the next major version
    if (!callIfFunction(innerParams.allowEnterKey)) {
      warnAboutDeprecation('allowEnterKey');
      blurActiveElement();
      return;
    }
    if (focusAutofocus(domCache)) {
      return;
    }
    if (focusButton(domCache, innerParams)) {
      return;
    }
    setFocus(-1, 1);
  };

  /**
   * @param {DomCache} domCache
   * @returns {boolean}
   */
  const focusAutofocus = domCache => {
    const autofocusElements = Array.from(domCache.popup.querySelectorAll('[autofocus]'));
    for (const autofocusElement of autofocusElements) {
      if (autofocusElement instanceof HTMLElement && isVisible$1(autofocusElement)) {
        autofocusElement.focus();
        return true;
      }
    }
    return false;
  };

  /**
   * @param {DomCache} domCache
   * @param {SweetAlertOptions} innerParams
   * @returns {boolean}
   */
  const focusButton = (domCache, innerParams) => {
    if (innerParams.focusDeny && isVisible$1(domCache.denyButton)) {
      domCache.denyButton.focus();
      return true;
    }
    if (innerParams.focusCancel && isVisible$1(domCache.cancelButton)) {
      domCache.cancelButton.focus();
      return true;
    }
    if (innerParams.focusConfirm && isVisible$1(domCache.confirmButton)) {
      domCache.confirmButton.focus();
      return true;
    }
    return false;
  };
  const blurActiveElement = () => {
    if (document.activeElement instanceof HTMLElement && typeof document.activeElement.blur === 'function') {
      document.activeElement.blur();
    }
  };

  // Assign instance methods from src/instanceMethods/*.js to prototype
  SweetAlert.prototype.disableButtons = disableButtons;
  SweetAlert.prototype.enableButtons = enableButtons;
  SweetAlert.prototype.getInput = getInput;
  SweetAlert.prototype.disableInput = disableInput;
  SweetAlert.prototype.enableInput = enableInput;
  SweetAlert.prototype.hideLoading = hideLoading;
  SweetAlert.prototype.disableLoading = hideLoading;
  SweetAlert.prototype.showValidationMessage = showValidationMessage;
  SweetAlert.prototype.resetValidationMessage = resetValidationMessage;
  SweetAlert.prototype.close = close;
  SweetAlert.prototype.closePopup = close;
  SweetAlert.prototype.closeModal = close;
  SweetAlert.prototype.closeToast = close;
  SweetAlert.prototype.rejectPromise = rejectPromise;
  SweetAlert.prototype.update = update;
  SweetAlert.prototype._destroy = _destroy;

  // Assign static methods from src/staticMethods/*.js to constructor
  Object.assign(SweetAlert, staticMethods);

  // Proxy to instance methods to constructor, for now, for backwards compatibility
  Object.keys(instanceMethods).forEach(key => {
    /**
     * @param {...(SweetAlertOptions | string | undefined)} args
     * @returns {SweetAlertResult | Promise<SweetAlertResult> | undefined}
     */
    SweetAlert[key] = function (...args) {
      if (currentInstance && currentInstance[key]) {
        return currentInstance[key](...args);
      }
      return null;
    };
  });
  SweetAlert.DismissReason = DismissReason;
  SweetAlert.version = '11.26.3';

  const Swal = SweetAlert;
  // @ts-ignore
  Swal.default = Swal;

  return Swal;

}));
if (typeof this !== 'undefined' && this.Sweetalert2){this.swal = this.sweetAlert = this.Swal = this.SweetAlert = this.Sweetalert2}
"undefined"!=typeof document&&function(e,t){var n=e.createElement("style");if(e.getElementsByTagName("head")[0].appendChild(n),n.styleSheet)n.styleSheet.disabled||(n.styleSheet.cssText=t);else try{n.innerHTML=t}catch(e){n.innerText=t}}(document,":root{--swal2-outline: 0 0 0 3px rgba(100, 150, 200, 0.5);--swal2-container-padding: 0.625em;--swal2-backdrop: rgba(0, 0, 0, 0.4);--swal2-backdrop-transition: background-color 0.15s;--swal2-width: 32em;--swal2-padding: 0 0 1.25em;--swal2-border: none;--swal2-border-radius: 0.3125rem;--swal2-background: white;--swal2-color: #545454;--swal2-show-animation: swal2-show 0.3s;--swal2-hide-animation: swal2-hide 0.15s forwards;--swal2-icon-zoom: 1;--swal2-icon-animations: true;--swal2-title-padding: 0.8em 1em 0;--swal2-html-container-padding: 1em 1.6em 0.3em;--swal2-input-border: 1px solid #d9d9d9;--swal2-input-border-radius: 0.1875em;--swal2-input-box-shadow: inset 0 1px 1px rgba(0, 0, 0, 0.06), 0 0 0 3px transparent;--swal2-input-background: transparent;--swal2-input-transition: border-color 0.2s, box-shadow 0.2s;--swal2-input-hover-box-shadow: inset 0 1px 1px rgba(0, 0, 0, 0.06), 0 0 0 3px transparent;--swal2-input-focus-border: 1px solid #b4dbed;--swal2-input-focus-box-shadow: inset 0 1px 1px rgba(0, 0, 0, 0.06), 0 0 0 3px rgba(100, 150, 200, 0.5);--swal2-progress-step-background: #add8e6;--swal2-validation-message-background: #f0f0f0;--swal2-validation-message-color: #666;--swal2-footer-border-color: #eee;--swal2-footer-background: transparent;--swal2-footer-color: inherit;--swal2-timer-progress-bar-background: rgba(0, 0, 0, 0.3);--swal2-close-button-position: initial;--swal2-close-button-inset: auto;--swal2-close-button-font-size: 2.5em;--swal2-close-button-color: #ccc;--swal2-close-button-transition: color 0.2s, box-shadow 0.2s;--swal2-close-button-outline: initial;--swal2-close-button-box-shadow: inset 0 0 0 3px transparent;--swal2-close-button-focus-box-shadow: inset var(--swal2-outline);--swal2-close-button-hover-transform: none;--swal2-actions-justify-content: center;--swal2-actions-width: auto;--swal2-actions-margin: 1.25em auto 0;--swal2-actions-padding: 0;--swal2-actions-border-radius: 0;--swal2-actions-background: transparent;--swal2-action-button-transition: background-color 0.2s, box-shadow 0.2s;--swal2-action-button-hover: black 10%;--swal2-action-button-active: black 10%;--swal2-confirm-button-box-shadow: none;--swal2-confirm-button-border-radius: 0.25em;--swal2-confirm-button-background-color: #7066e0;--swal2-confirm-button-color: #fff;--swal2-deny-button-box-shadow: none;--swal2-deny-button-border-radius: 0.25em;--swal2-deny-button-background-color: #dc3741;--swal2-deny-button-color: #fff;--swal2-cancel-button-box-shadow: none;--swal2-cancel-button-border-radius: 0.25em;--swal2-cancel-button-background-color: #6e7881;--swal2-cancel-button-color: #fff;--swal2-toast-show-animation: swal2-toast-show 0.5s;--swal2-toast-hide-animation: swal2-toast-hide 0.1s forwards;--swal2-toast-border: none;--swal2-toast-box-shadow: 0 0 1px hsl(0deg 0% 0% / 0.075), 0 1px 2px hsl(0deg 0% 0% / 0.075), 1px 2px 4px hsl(0deg 0% 0% / 0.075), 1px 3px 8px hsl(0deg 0% 0% / 0.075), 2px 4px 16px hsl(0deg 0% 0% / 0.075)}[data-swal2-theme=dark]{--swal2-dark-theme-black: #19191a;--swal2-dark-theme-white: #e1e1e1;--swal2-background: var(--swal2-dark-theme-black);--swal2-color: var(--swal2-dark-theme-white);--swal2-footer-border-color: #555;--swal2-input-background: color-mix(in srgb, var(--swal2-dark-theme-black), var(--swal2-dark-theme-white) 10%);--swal2-validation-message-background: color-mix( in srgb, var(--swal2-dark-theme-black), var(--swal2-dark-theme-white) 10% );--swal2-validation-message-color: var(--swal2-dark-theme-white);--swal2-timer-progress-bar-background: rgba(255, 255, 255, 0.7)}@media(prefers-color-scheme: dark){[data-swal2-theme=auto]{--swal2-dark-theme-black: #19191a;--swal2-dark-theme-white: #e1e1e1;--swal2-background: var(--swal2-dark-theme-black);--swal2-color: var(--swal2-dark-theme-white);--swal2-footer-border-color: #555;--swal2-input-background: color-mix(in srgb, var(--swal2-dark-theme-black), var(--swal2-dark-theme-white) 10%);--swal2-validation-message-background: color-mix( in srgb, var(--swal2-dark-theme-black), var(--swal2-dark-theme-white) 10% );--swal2-validation-message-color: var(--swal2-dark-theme-white);--swal2-timer-progress-bar-background: rgba(255, 255, 255, 0.7)}}body.swal2-shown:not(.swal2-no-backdrop,.swal2-toast-shown){overflow:hidden}body.swal2-height-auto{height:auto !important}body.swal2-no-backdrop .swal2-container{background-color:rgba(0,0,0,0) !important;pointer-events:none}body.swal2-no-backdrop .swal2-container .swal2-popup{pointer-events:all}body.swal2-no-backdrop .swal2-container .swal2-modal{box-shadow:0 0 10px var(--swal2-backdrop)}body.swal2-toast-shown .swal2-container{box-sizing:border-box;width:360px;max-width:100%;background-color:rgba(0,0,0,0);pointer-events:none}body.swal2-toast-shown .swal2-container.swal2-top{inset:0 auto auto 50%;transform:translateX(-50%)}body.swal2-toast-shown .swal2-container.swal2-top-end,body.swal2-toast-shown .swal2-container.swal2-top-right{inset:0 0 auto auto}body.swal2-toast-shown .swal2-container.swal2-top-start,body.swal2-toast-shown .swal2-container.swal2-top-left{inset:0 auto auto 0}body.swal2-toast-shown .swal2-container.swal2-center-start,body.swal2-toast-shown .swal2-container.swal2-center-left{inset:50% auto auto 0;transform:translateY(-50%)}body.swal2-toast-shown .swal2-container.swal2-center{inset:50% auto auto 50%;transform:translate(-50%, -50%)}body.swal2-toast-shown .swal2-container.swal2-center-end,body.swal2-toast-shown .swal2-container.swal2-center-right{inset:50% 0 auto auto;transform:translateY(-50%)}body.swal2-toast-shown .swal2-container.swal2-bottom-start,body.swal2-toast-shown .swal2-container.swal2-bottom-left{inset:auto auto 0 0}body.swal2-toast-shown .swal2-container.swal2-bottom{inset:auto auto 0 50%;transform:translateX(-50%)}body.swal2-toast-shown .swal2-container.swal2-bottom-end,body.swal2-toast-shown .swal2-container.swal2-bottom-right{inset:auto 0 0 auto}@media print{body.swal2-shown:not(.swal2-no-backdrop,.swal2-toast-shown){overflow-y:scroll !important}body.swal2-shown:not(.swal2-no-backdrop,.swal2-toast-shown)>[aria-hidden=true]{display:none}body.swal2-shown:not(.swal2-no-backdrop,.swal2-toast-shown) .swal2-container{position:static !important}}div:where(.swal2-container){display:grid;position:fixed;z-index:1060;inset:0;box-sizing:border-box;grid-template-areas:\"top-start     top            top-end\" \"center-start  center         center-end\" \"bottom-start  bottom-center  bottom-end\";grid-template-rows:minmax(min-content, auto) minmax(min-content, auto) minmax(min-content, auto);height:100%;padding:var(--swal2-container-padding);overflow-x:hidden;transition:var(--swal2-backdrop-transition);-webkit-overflow-scrolling:touch}div:where(.swal2-container).swal2-backdrop-show,div:where(.swal2-container).swal2-noanimation{background:var(--swal2-backdrop)}div:where(.swal2-container).swal2-backdrop-hide{background:rgba(0,0,0,0) !important}div:where(.swal2-container).swal2-top-start,div:where(.swal2-container).swal2-center-start,div:where(.swal2-container).swal2-bottom-start{grid-template-columns:minmax(0, 1fr) auto auto}div:where(.swal2-container).swal2-top,div:where(.swal2-container).swal2-center,div:where(.swal2-container).swal2-bottom{grid-template-columns:auto minmax(0, 1fr) auto}div:where(.swal2-container).swal2-top-end,div:where(.swal2-container).swal2-center-end,div:where(.swal2-container).swal2-bottom-end{grid-template-columns:auto auto minmax(0, 1fr)}div:where(.swal2-container).swal2-top-start>.swal2-popup{align-self:start}div:where(.swal2-container).swal2-top>.swal2-popup{grid-column:2;place-self:start center}div:where(.swal2-container).swal2-top-end>.swal2-popup,div:where(.swal2-container).swal2-top-right>.swal2-popup{grid-column:3;place-self:start end}div:where(.swal2-container).swal2-center-start>.swal2-popup,div:where(.swal2-container).swal2-center-left>.swal2-popup{grid-row:2;align-self:center}div:where(.swal2-container).swal2-center>.swal2-popup{grid-column:2;grid-row:2;place-self:center center}div:where(.swal2-container).swal2-center-end>.swal2-popup,div:where(.swal2-container).swal2-center-right>.swal2-popup{grid-column:3;grid-row:2;place-self:center end}div:where(.swal2-container).swal2-bottom-start>.swal2-popup,div:where(.swal2-container).swal2-bottom-left>.swal2-popup{grid-column:1;grid-row:3;align-self:end}div:where(.swal2-container).swal2-bottom>.swal2-popup{grid-column:2;grid-row:3;place-self:end center}div:where(.swal2-container).swal2-bottom-end>.swal2-popup,div:where(.swal2-container).swal2-bottom-right>.swal2-popup{grid-column:3;grid-row:3;place-self:end end}div:where(.swal2-container).swal2-grow-row>.swal2-popup,div:where(.swal2-container).swal2-grow-fullscreen>.swal2-popup{grid-column:1/4;width:100%}div:where(.swal2-container).swal2-grow-column>.swal2-popup,div:where(.swal2-container).swal2-grow-fullscreen>.swal2-popup{grid-row:1/4;align-self:stretch}div:where(.swal2-container).swal2-no-transition{transition:none !important}div:where(.swal2-container)[popover]{width:auto;border:0}div:where(.swal2-container) div:where(.swal2-popup){display:none;position:relative;box-sizing:border-box;grid-template-columns:minmax(0, 100%);width:var(--swal2-width);max-width:100%;padding:var(--swal2-padding);border:var(--swal2-border);border-radius:var(--swal2-border-radius);background:var(--swal2-background);color:var(--swal2-color);font-family:inherit;font-size:1rem;container-name:swal2-popup}div:where(.swal2-container) div:where(.swal2-popup):focus{outline:none}div:where(.swal2-container) div:where(.swal2-popup).swal2-loading{overflow-y:hidden}div:where(.swal2-container) div:where(.swal2-popup).swal2-draggable{cursor:grab}div:where(.swal2-container) div:where(.swal2-popup).swal2-draggable div:where(.swal2-icon){cursor:grab}div:where(.swal2-container) div:where(.swal2-popup).swal2-dragging{cursor:grabbing}div:where(.swal2-container) div:where(.swal2-popup).swal2-dragging div:where(.swal2-icon){cursor:grabbing}div:where(.swal2-container) h2:where(.swal2-title){position:relative;max-width:100%;margin:0;padding:var(--swal2-title-padding);color:inherit;font-size:1.875em;font-weight:600;text-align:center;text-transform:none;overflow-wrap:break-word;cursor:initial}div:where(.swal2-container) div:where(.swal2-actions){display:flex;z-index:1;box-sizing:border-box;flex-wrap:wrap;align-items:center;justify-content:var(--swal2-actions-justify-content);width:var(--swal2-actions-width);margin:var(--swal2-actions-margin);padding:var(--swal2-actions-padding);border-radius:var(--swal2-actions-border-radius);background:var(--swal2-actions-background)}div:where(.swal2-container) div:where(.swal2-loader){display:none;align-items:center;justify-content:center;width:2.2em;height:2.2em;margin:0 1.875em;animation:swal2-rotate-loading 1.5s linear 0s infinite normal;border-width:.25em;border-style:solid;border-radius:100%;border-color:#2778c4 rgba(0,0,0,0) #2778c4 rgba(0,0,0,0)}div:where(.swal2-container) button:where(.swal2-styled){margin:.3125em;padding:.625em 1.1em;transition:var(--swal2-action-button-transition);border:none;box-shadow:0 0 0 3px rgba(0,0,0,0);font-weight:500}div:where(.swal2-container) button:where(.swal2-styled):not([disabled]){cursor:pointer}div:where(.swal2-container) button:where(.swal2-styled):where(.swal2-confirm){border-radius:var(--swal2-confirm-button-border-radius);background:initial;background-color:var(--swal2-confirm-button-background-color);box-shadow:var(--swal2-confirm-button-box-shadow);color:var(--swal2-confirm-button-color);font-size:1em}div:where(.swal2-container) button:where(.swal2-styled):where(.swal2-confirm):hover{background-color:color-mix(in srgb, var(--swal2-confirm-button-background-color), var(--swal2-action-button-hover))}div:where(.swal2-container) button:where(.swal2-styled):where(.swal2-confirm):active{background-color:color-mix(in srgb, var(--swal2-confirm-button-background-color), var(--swal2-action-button-active))}div:where(.swal2-container) button:where(.swal2-styled):where(.swal2-deny){border-radius:var(--swal2-deny-button-border-radius);background:initial;background-color:var(--swal2-deny-button-background-color);box-shadow:var(--swal2-deny-button-box-shadow);color:var(--swal2-deny-button-color);font-size:1em}div:where(.swal2-container) button:where(.swal2-styled):where(.swal2-deny):hover{background-color:color-mix(in srgb, var(--swal2-deny-button-background-color), var(--swal2-action-button-hover))}div:where(.swal2-container) button:where(.swal2-styled):where(.swal2-deny):active{background-color:color-mix(in srgb, var(--swal2-deny-button-background-color), var(--swal2-action-button-active))}div:where(.swal2-container) button:where(.swal2-styled):where(.swal2-cancel){border-radius:var(--swal2-cancel-button-border-radius);background:initial;background-color:var(--swal2-cancel-button-background-color);box-shadow:var(--swal2-cancel-button-box-shadow);color:var(--swal2-cancel-button-color);font-size:1em}div:where(.swal2-container) button:where(.swal2-styled):where(.swal2-cancel):hover{background-color:color-mix(in srgb, var(--swal2-cancel-button-background-color), var(--swal2-action-button-hover))}div:where(.swal2-container) button:where(.swal2-styled):where(.swal2-cancel):active{background-color:color-mix(in srgb, var(--swal2-cancel-button-background-color), var(--swal2-action-button-active))}div:where(.swal2-container) button:where(.swal2-styled):focus-visible{outline:none;box-shadow:var(--swal2-action-button-focus-box-shadow)}div:where(.swal2-container) button:where(.swal2-styled)[disabled]:not(.swal2-loading){opacity:.4}div:where(.swal2-container) button:where(.swal2-styled)::-moz-focus-inner{border:0}div:where(.swal2-container) div:where(.swal2-footer){margin:1em 0 0;padding:1em 1em 0;border-top:1px solid var(--swal2-footer-border-color);background:var(--swal2-footer-background);color:var(--swal2-footer-color);font-size:1em;text-align:center;cursor:initial}div:where(.swal2-container) .swal2-timer-progress-bar-container{position:absolute;right:0;bottom:0;left:0;grid-column:auto !important;overflow:hidden;border-bottom-right-radius:var(--swal2-border-radius);border-bottom-left-radius:var(--swal2-border-radius)}div:where(.swal2-container) div:where(.swal2-timer-progress-bar){width:100%;height:.25em;background:var(--swal2-timer-progress-bar-background)}div:where(.swal2-container) img:where(.swal2-image){max-width:100%;margin:2em auto 1em;cursor:initial}div:where(.swal2-container) button:where(.swal2-close){position:var(--swal2-close-button-position);inset:var(--swal2-close-button-inset);z-index:2;align-items:center;justify-content:center;width:1.2em;height:1.2em;margin-top:0;margin-right:0;margin-bottom:-1.2em;padding:0;overflow:hidden;transition:var(--swal2-close-button-transition);border:none;border-radius:var(--swal2-border-radius);outline:var(--swal2-close-button-outline);background:rgba(0,0,0,0);color:var(--swal2-close-button-color);font-family:monospace;font-size:var(--swal2-close-button-font-size);cursor:pointer;justify-self:end}div:where(.swal2-container) button:where(.swal2-close):hover{transform:var(--swal2-close-button-hover-transform);background:rgba(0,0,0,0);color:#f27474}div:where(.swal2-container) button:where(.swal2-close):focus-visible{outline:none;box-shadow:var(--swal2-close-button-focus-box-shadow)}div:where(.swal2-container) button:where(.swal2-close)::-moz-focus-inner{border:0}div:where(.swal2-container) div:where(.swal2-html-container){z-index:1;justify-content:center;margin:0;padding:var(--swal2-html-container-padding);overflow:auto;color:inherit;font-size:1.125em;font-weight:normal;line-height:normal;text-align:center;overflow-wrap:break-word;word-break:break-word;cursor:initial}div:where(.swal2-container) input:where(.swal2-input),div:where(.swal2-container) input:where(.swal2-file),div:where(.swal2-container) textarea:where(.swal2-textarea),div:where(.swal2-container) select:where(.swal2-select),div:where(.swal2-container) div:where(.swal2-radio),div:where(.swal2-container) label:where(.swal2-checkbox){margin:1em 2em 3px}div:where(.swal2-container) input:where(.swal2-input),div:where(.swal2-container) input:where(.swal2-file),div:where(.swal2-container) textarea:where(.swal2-textarea){box-sizing:border-box;width:auto;transition:var(--swal2-input-transition);border:var(--swal2-input-border);border-radius:var(--swal2-input-border-radius);background:var(--swal2-input-background);box-shadow:var(--swal2-input-box-shadow);color:inherit;font-size:1.125em}div:where(.swal2-container) input:where(.swal2-input).swal2-inputerror,div:where(.swal2-container) input:where(.swal2-file).swal2-inputerror,div:where(.swal2-container) textarea:where(.swal2-textarea).swal2-inputerror{border-color:#f27474 !important;box-shadow:0 0 2px #f27474 !important}div:where(.swal2-container) input:where(.swal2-input):hover,div:where(.swal2-container) input:where(.swal2-file):hover,div:where(.swal2-container) textarea:where(.swal2-textarea):hover{box-shadow:var(--swal2-input-hover-box-shadow)}div:where(.swal2-container) input:where(.swal2-input):focus,div:where(.swal2-container) input:where(.swal2-file):focus,div:where(.swal2-container) textarea:where(.swal2-textarea):focus{border:var(--swal2-input-focus-border);outline:none;box-shadow:var(--swal2-input-focus-box-shadow)}div:where(.swal2-container) input:where(.swal2-input)::placeholder,div:where(.swal2-container) input:where(.swal2-file)::placeholder,div:where(.swal2-container) textarea:where(.swal2-textarea)::placeholder{color:#ccc}div:where(.swal2-container) .swal2-range{margin:1em 2em 3px;background:var(--swal2-background)}div:where(.swal2-container) .swal2-range input{width:80%}div:where(.swal2-container) .swal2-range output{width:20%;color:inherit;font-weight:600;text-align:center}div:where(.swal2-container) .swal2-range input,div:where(.swal2-container) .swal2-range output{height:2.625em;padding:0;font-size:1.125em;line-height:2.625em}div:where(.swal2-container) .swal2-input{height:2.625em;padding:0 .75em}div:where(.swal2-container) .swal2-file{width:75%;margin-right:auto;margin-left:auto;background:var(--swal2-input-background);font-size:1.125em}div:where(.swal2-container) .swal2-textarea{height:6.75em;padding:.75em}div:where(.swal2-container) .swal2-select{min-width:50%;max-width:100%;padding:.375em .625em;background:var(--swal2-input-background);color:inherit;font-size:1.125em}div:where(.swal2-container) .swal2-radio,div:where(.swal2-container) .swal2-checkbox{align-items:center;justify-content:center;background:var(--swal2-background);color:inherit}div:where(.swal2-container) .swal2-radio label,div:where(.swal2-container) .swal2-checkbox label{margin:0 .6em;font-size:1.125em}div:where(.swal2-container) .swal2-radio input,div:where(.swal2-container) .swal2-checkbox input{flex-shrink:0;margin:0 .4em}div:where(.swal2-container) label:where(.swal2-input-label){display:flex;justify-content:center;margin:1em auto 0}div:where(.swal2-container) div:where(.swal2-validation-message){align-items:center;justify-content:center;margin:1em 0 0;padding:.625em;overflow:hidden;background:var(--swal2-validation-message-background);color:var(--swal2-validation-message-color);font-size:1em;font-weight:300}div:where(.swal2-container) div:where(.swal2-validation-message)::before{content:\"!\";display:inline-block;width:1.5em;min-width:1.5em;height:1.5em;margin:0 .625em;border-radius:50%;background-color:#f27474;color:#fff;font-weight:600;line-height:1.5em;text-align:center}div:where(.swal2-container) .swal2-progress-steps{flex-wrap:wrap;align-items:center;max-width:100%;margin:1.25em auto;padding:0;background:rgba(0,0,0,0);font-weight:600}div:where(.swal2-container) .swal2-progress-steps li{display:inline-block;position:relative}div:where(.swal2-container) .swal2-progress-steps .swal2-progress-step{z-index:20;flex-shrink:0;width:2em;height:2em;border-radius:2em;background:#2778c4;color:#fff;line-height:2em;text-align:center}div:where(.swal2-container) .swal2-progress-steps .swal2-progress-step.swal2-active-progress-step{background:#2778c4}div:where(.swal2-container) .swal2-progress-steps .swal2-progress-step.swal2-active-progress-step~.swal2-progress-step{background:var(--swal2-progress-step-background);color:#fff}div:where(.swal2-container) .swal2-progress-steps .swal2-progress-step.swal2-active-progress-step~.swal2-progress-step-line{background:var(--swal2-progress-step-background)}div:where(.swal2-container) .swal2-progress-steps .swal2-progress-step-line{z-index:10;flex-shrink:0;width:2.5em;height:.4em;margin:0 -1px;background:#2778c4}div:where(.swal2-icon){position:relative;box-sizing:content-box;justify-content:center;width:5em;height:5em;margin:2.5em auto .6em;zoom:var(--swal2-icon-zoom);border:.25em solid rgba(0,0,0,0);border-radius:50%;border-color:#000;font-family:inherit;line-height:5em;cursor:default;user-select:none}div:where(.swal2-icon) .swal2-icon-content{display:flex;align-items:center;font-size:3.75em}div:where(.swal2-icon).swal2-error{border-color:#f27474;color:#f27474}div:where(.swal2-icon).swal2-error .swal2-x-mark{position:relative;flex-grow:1}div:where(.swal2-icon).swal2-error [class^=swal2-x-mark-line]{display:block;position:absolute;top:2.3125em;width:2.9375em;height:.3125em;border-radius:.125em;background-color:#f27474}div:where(.swal2-icon).swal2-error [class^=swal2-x-mark-line][class$=left]{left:1.0625em;transform:rotate(45deg)}div:where(.swal2-icon).swal2-error [class^=swal2-x-mark-line][class$=right]{right:1em;transform:rotate(-45deg)}@container swal2-popup style(--swal2-icon-animations:true){div:where(.swal2-icon).swal2-error.swal2-icon-show{animation:swal2-animate-error-icon .5s}div:where(.swal2-icon).swal2-error.swal2-icon-show .swal2-x-mark{animation:swal2-animate-error-x-mark .5s}}div:where(.swal2-icon).swal2-warning{border-color:#f8bb86;color:#f8bb86}@container swal2-popup style(--swal2-icon-animations:true){div:where(.swal2-icon).swal2-warning.swal2-icon-show{animation:swal2-animate-error-icon .5s}div:where(.swal2-icon).swal2-warning.swal2-icon-show .swal2-icon-content{animation:swal2-animate-i-mark .5s}}div:where(.swal2-icon).swal2-info{border-color:#3fc3ee;color:#3fc3ee}@container swal2-popup style(--swal2-icon-animations:true){div:where(.swal2-icon).swal2-info.swal2-icon-show{animation:swal2-animate-error-icon .5s}div:where(.swal2-icon).swal2-info.swal2-icon-show .swal2-icon-content{animation:swal2-animate-i-mark .8s}}div:where(.swal2-icon).swal2-question{border-color:#87adbd;color:#87adbd}@container swal2-popup style(--swal2-icon-animations:true){div:where(.swal2-icon).swal2-question.swal2-icon-show{animation:swal2-animate-error-icon .5s}div:where(.swal2-icon).swal2-question.swal2-icon-show .swal2-icon-content{animation:swal2-animate-question-mark .8s}}div:where(.swal2-icon).swal2-success{border-color:#a5dc86;color:#a5dc86}div:where(.swal2-icon).swal2-success [class^=swal2-success-circular-line]{position:absolute;width:3.75em;height:7.5em;border-radius:50%}div:where(.swal2-icon).swal2-success [class^=swal2-success-circular-line][class$=left]{top:-0.4375em;left:-2.0635em;transform:rotate(-45deg);transform-origin:3.75em 3.75em;border-radius:7.5em 0 0 7.5em}div:where(.swal2-icon).swal2-success [class^=swal2-success-circular-line][class$=right]{top:-0.6875em;left:1.875em;transform:rotate(-45deg);transform-origin:0 3.75em;border-radius:0 7.5em 7.5em 0}div:where(.swal2-icon).swal2-success .swal2-success-ring{position:absolute;z-index:2;top:-0.25em;left:-0.25em;box-sizing:content-box;width:100%;height:100%;border:.25em solid rgba(165,220,134,.3);border-radius:50%}div:where(.swal2-icon).swal2-success .swal2-success-fix{position:absolute;z-index:1;top:.5em;left:1.625em;width:.4375em;height:5.625em;transform:rotate(-45deg)}div:where(.swal2-icon).swal2-success [class^=swal2-success-line]{display:block;position:absolute;z-index:2;height:.3125em;border-radius:.125em;background-color:#a5dc86}div:where(.swal2-icon).swal2-success [class^=swal2-success-line][class$=tip]{top:2.875em;left:.8125em;width:1.5625em;transform:rotate(45deg)}div:where(.swal2-icon).swal2-success [class^=swal2-success-line][class$=long]{top:2.375em;right:.5em;width:2.9375em;transform:rotate(-45deg)}@container swal2-popup style(--swal2-icon-animations:true){div:where(.swal2-icon).swal2-success.swal2-icon-show .swal2-success-line-tip{animation:swal2-animate-success-line-tip .75s}div:where(.swal2-icon).swal2-success.swal2-icon-show .swal2-success-line-long{animation:swal2-animate-success-line-long .75s}div:where(.swal2-icon).swal2-success.swal2-icon-show .swal2-success-circular-line-right{animation:swal2-rotate-success-circular-line 4.25s ease-in}}[class^=swal2]{-webkit-tap-highlight-color:rgba(0,0,0,0)}.swal2-show{animation:var(--swal2-show-animation)}.swal2-hide{animation:var(--swal2-hide-animation)}.swal2-noanimation{transition:none}.swal2-scrollbar-measure{position:absolute;top:-9999px;width:50px;height:50px;overflow:scroll}.swal2-rtl .swal2-close{margin-right:initial;margin-left:0}.swal2-rtl .swal2-timer-progress-bar{right:0;left:auto}.swal2-toast{box-sizing:border-box;grid-column:1/4 !important;grid-row:1/4 !important;grid-template-columns:min-content auto min-content;padding:1em;overflow-y:hidden;border:var(--swal2-toast-border);background:var(--swal2-background);box-shadow:var(--swal2-toast-box-shadow);pointer-events:all}.swal2-toast>*{grid-column:2}.swal2-toast h2:where(.swal2-title){margin:.5em 1em;padding:0;font-size:1em;text-align:initial}.swal2-toast .swal2-loading{justify-content:center}.swal2-toast input:where(.swal2-input){height:2em;margin:.5em;font-size:1em}.swal2-toast .swal2-validation-message{font-size:1em}.swal2-toast div:where(.swal2-footer){margin:.5em 0 0;padding:.5em 0 0;font-size:.8em}.swal2-toast button:where(.swal2-close){grid-column:3/3;grid-row:1/99;align-self:center;width:.8em;height:.8em;margin:0;font-size:2em}.swal2-toast div:where(.swal2-html-container){margin:.5em 1em;padding:0;overflow:initial;font-size:1em;text-align:initial}.swal2-toast div:where(.swal2-html-container):empty{padding:0}.swal2-toast .swal2-loader{grid-column:1;grid-row:1/99;align-self:center;width:2em;height:2em;margin:.25em}.swal2-toast .swal2-icon{grid-column:1;grid-row:1/99;align-self:center;width:2em;min-width:2em;height:2em;margin:0 .5em 0 0}.swal2-toast .swal2-icon .swal2-icon-content{display:flex;align-items:center;font-size:1.8em;font-weight:bold}.swal2-toast .swal2-icon.swal2-success .swal2-success-ring{width:2em;height:2em}.swal2-toast .swal2-icon.swal2-error [class^=swal2-x-mark-line]{top:.875em;width:1.375em}.swal2-toast .swal2-icon.swal2-error [class^=swal2-x-mark-line][class$=left]{left:.3125em}.swal2-toast .swal2-icon.swal2-error [class^=swal2-x-mark-line][class$=right]{right:.3125em}.swal2-toast div:where(.swal2-actions){justify-content:flex-start;height:auto;margin:0;margin-top:.5em;padding:0 .5em}.swal2-toast button:where(.swal2-styled){margin:.25em .5em;padding:.4em .6em;font-size:1em}.swal2-toast .swal2-success{border-color:#a5dc86}.swal2-toast .swal2-success [class^=swal2-success-circular-line]{position:absolute;width:1.6em;height:3em;border-radius:50%}.swal2-toast .swal2-success [class^=swal2-success-circular-line][class$=left]{top:-0.8em;left:-0.5em;transform:rotate(-45deg);transform-origin:2em 2em;border-radius:4em 0 0 4em}.swal2-toast .swal2-success [class^=swal2-success-circular-line][class$=right]{top:-0.25em;left:.9375em;transform-origin:0 1.5em;border-radius:0 4em 4em 0}.swal2-toast .swal2-success .swal2-success-ring{width:2em;height:2em}.swal2-toast .swal2-success .swal2-success-fix{top:0;left:.4375em;width:.4375em;height:2.6875em}.swal2-toast .swal2-success [class^=swal2-success-line]{height:.3125em}.swal2-toast .swal2-success [class^=swal2-success-line][class$=tip]{top:1.125em;left:.1875em;width:.75em}.swal2-toast .swal2-success [class^=swal2-success-line][class$=long]{top:.9375em;right:.1875em;width:1.375em}@container swal2-popup style(--swal2-icon-animations:true){.swal2-toast .swal2-success.swal2-icon-show .swal2-success-line-tip{animation:swal2-toast-animate-success-line-tip .75s}.swal2-toast .swal2-success.swal2-icon-show .swal2-success-line-long{animation:swal2-toast-animate-success-line-long .75s}}.swal2-toast.swal2-show{animation:var(--swal2-toast-show-animation)}.swal2-toast.swal2-hide{animation:var(--swal2-toast-hide-animation)}@keyframes swal2-show{0%{transform:translate3d(0, -50px, 0) scale(0.9);opacity:0}100%{transform:translate3d(0, 0, 0) scale(1);opacity:1}}@keyframes swal2-hide{0%{transform:translate3d(0, 0, 0) scale(1);opacity:1}100%{transform:translate3d(0, -50px, 0) scale(0.9);opacity:0}}@keyframes swal2-animate-success-line-tip{0%{top:1.1875em;left:.0625em;width:0}54%{top:1.0625em;left:.125em;width:0}70%{top:2.1875em;left:-0.375em;width:3.125em}84%{top:3em;left:1.3125em;width:1.0625em}100%{top:2.8125em;left:.8125em;width:1.5625em}}@keyframes swal2-animate-success-line-long{0%{top:3.375em;right:2.875em;width:0}65%{top:3.375em;right:2.875em;width:0}84%{top:2.1875em;right:0;width:3.4375em}100%{top:2.375em;right:.5em;width:2.9375em}}@keyframes swal2-rotate-success-circular-line{0%{transform:rotate(-45deg)}5%{transform:rotate(-45deg)}12%{transform:rotate(-405deg)}100%{transform:rotate(-405deg)}}@keyframes swal2-animate-error-x-mark{0%{margin-top:1.625em;transform:scale(0.4);opacity:0}50%{margin-top:1.625em;transform:scale(0.4);opacity:0}80%{margin-top:-0.375em;transform:scale(1.15)}100%{margin-top:0;transform:scale(1);opacity:1}}@keyframes swal2-animate-error-icon{0%{transform:rotateX(100deg);opacity:0}100%{transform:rotateX(0deg);opacity:1}}@keyframes swal2-rotate-loading{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}@keyframes swal2-animate-question-mark{0%{transform:rotateY(-360deg)}100%{transform:rotateY(0)}}@keyframes swal2-animate-i-mark{0%{transform:rotateZ(45deg);opacity:0}25%{transform:rotateZ(-25deg);opacity:.4}50%{transform:rotateZ(15deg);opacity:.8}75%{transform:rotateZ(-5deg);opacity:1}100%{transform:rotateX(0);opacity:1}}@keyframes swal2-toast-show{0%{transform:translateY(-0.625em) rotateZ(2deg)}33%{transform:translateY(0) rotateZ(-2deg)}66%{transform:translateY(0.3125em) rotateZ(2deg)}100%{transform:translateY(0) rotateZ(0deg)}}@keyframes swal2-toast-hide{100%{transform:rotateZ(1deg);opacity:0}}@keyframes swal2-toast-animate-success-line-tip{0%{top:.5625em;left:.0625em;width:0}54%{top:.125em;left:.125em;width:0}70%{top:.625em;left:-0.25em;width:1.625em}84%{top:1.0625em;left:.75em;width:.5em}100%{top:1.125em;left:.1875em;width:.75em}}@keyframes swal2-toast-animate-success-line-long{0%{top:1.625em;right:1.375em;width:0}65%{top:1.25em;right:.9375em;width:0}84%{top:.9375em;right:0;width:1.125em}100%{top:.9375em;right:.1875em;width:1.375em}}");

/***/ }),
/* 1 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
// ESM COMPAT FLAG
__webpack_require__.r(__webpack_exports__);

// EXPORTS
__webpack_require__.d(__webpack_exports__, "default", function() { return /* reexport */ core_Drawflow; });

// CONCATENATED MODULE: ./src/core/lifecycle/base.ts
function start(context) {
    var container = context.container;
    container.classList.add('parent-drawflow');
    container.tabIndex = 0;
    context.precanvas = document.createElement('div');
    context.precanvas.classList.add('drawflow');
    container.appendChild(context.precanvas);
    container.addEventListener('mouseup', context.dragEnd);
    container.addEventListener('mousemove', context.position);
    container.addEventListener('mousedown', context.click);
    container.addEventListener('touchend', context.dragEnd);
    container.addEventListener('touchmove', context.position);
    container.addEventListener('touchstart', context.click);
    container.addEventListener('contextmenu', context.contextmenu);
    container.addEventListener('keydown', context.key);
    container.addEventListener('wheel', context.zoom_enter);
    container.addEventListener('input', context.updateNodeValue);
    container.addEventListener('dblclick', context.dblclick);
    container.onpointerdown = context.pointerdown_handler;
    container.onpointermove = context.pointermove_handler;
    container.onpointerup = context.pointerup_handler;
    container.onpointercancel = context.pointerup_handler;
    container.onpointerout = context.pointerup_handler;
    container.onpointerleave = context.pointerup_handler;
    context.load();
}
function load(context) {
    var moduleData = context.drawflow.drawflow[context.module];
    if (!moduleData) {
        return;
    }
    Object.keys(moduleData.data).forEach(function (key) {
        context.addNodeImport(moduleData.data[key], context.precanvas);
    });
    if (context.reroute) {
        Object.keys(moduleData.data).forEach(function (key) {
            context.addRerouteImport(moduleData.data[key]);
        });
    }
    Object.keys(moduleData.data).forEach(function (key) {
        context.updateConnectionNodes("node-".concat(key));
    });
    var editor = context.drawflow.drawflow;
    var number = 1;
    Object.keys(editor).forEach(function (moduleName) {
        Object.keys(editor[moduleName].data).forEach(function (id) {
            var numericId = parseInt(id, 10);
            if (numericId >= number) {
                number = numericId + 1;
            }
        });
    });
    context.nodeId = number;
}
function removeReouteConnectionSelected(context) {
    var _a;
    context.dispatch('connectionUnselected', true);
    if (context.reroute_fix_curvature && context.connection_selected) {
        (_a = context.connection_selected.parentElement) === null || _a === void 0 ? void 0 : _a.querySelectorAll('.main-path').forEach(function (item) {
            item.classList.remove('selected');
        });
    }
}

// CONCATENATED MODULE: ./src/core/lifecycle/pointer.ts
function pointerdown_handler(context, ev) {
    context.evCache.push(ev);
}
function pointermove_handler(context, ev) {
    for (var i = 0; i < context.evCache.length; i += 1) {
        if (ev.pointerId === context.evCache[i].pointerId) {
            context.evCache[i] = ev;
            break;
        }
    }
    if (context.evCache.length === 2) {
        var curDiff = Math.abs(context.evCache[0].clientX - context.evCache[1].clientX);
        if (context.prevDiff > 100) {
            if (curDiff > context.prevDiff) {
                context.zoom_in();
            }
            if (curDiff < context.prevDiff) {
                context.zoom_out();
            }
        }
        context.prevDiff = curDiff;
    }
}
function pointerup_handler(context, ev) {
    remove_event(context, ev);
    if (context.evCache.length < 2) {
        context.prevDiff = -1;
    }
}
function remove_event(context, ev) {
    for (var i = 0; i < context.evCache.length; i += 1) {
        if (context.evCache[i].pointerId === ev.pointerId) {
            context.evCache.splice(i, 1);
            break;
        }
    }
}

// CONCATENATED MODULE: ./src/core/utils/canvas.ts
function applyCanvasTranslation(context, x, y) {
    if (context.precanvas) {
        context.precanvas.style.transform = "translate(".concat(x, "px, ").concat(y, "px) scale(").concat(context.zoom, ")");
    }
    context.container.style.backgroundPosition = "".concat(x, "px ").concat(y, "px");
}
function applyStoredCanvasTranslation(context) {
    applyCanvasTranslation(context, context.canvas_x, context.canvas_y);
}
function setCanvasTranslation(context, x, y) {
    context.canvas_x = x;
    context.canvas_y = y;
    context.dispatch('translate', { x: x, y: y });
    applyCanvasTranslation(context, x, y);
}
function resetCanvasTransform(context) {
    if (context.precanvas) {
        context.precanvas.style.transform = '';
    }
    context.container.style.backgroundPosition = '0px 0px';
}

// CONCATENATED MODULE: ./src/core/utils/classNames.ts
var ensureNodeDomId = function (nodeId) { return (nodeId.startsWith('node-') ? nodeId : "node-".concat(nodeId)); };
var stripClassPrefix = function (className, prefix) {
    return (className.startsWith(prefix) ? className.slice(prefix.length) : className);
};
var findClassWithPrefix = function (classList, prefix) {
    return Array.from(classList).find(function (className) { return className.startsWith(prefix); });
};
var extractConnectionClassInfo = function (classList) {
    var outputNodeClass = findClassWithPrefix(classList, 'node_out_');
    var inputNodeClass = findClassWithPrefix(classList, 'node_in_');
    var outputPortClass = findClassWithPrefix(classList, 'output_');
    var inputPortClass = findClassWithPrefix(classList, 'input_');
    if (!outputNodeClass || !inputNodeClass || !outputPortClass || !inputPortClass) {
        return null;
    }
    var outputNodeDomId = ensureNodeDomId(stripClassPrefix(outputNodeClass, 'node_out_'));
    var inputNodeDomId = ensureNodeDomId(stripClassPrefix(inputNodeClass, 'node_in_'));
    return {
        outputNodeDomId: outputNodeDomId,
        inputNodeDomId: inputNodeDomId,
        outputNodeId: outputNodeDomId.slice(5),
        inputNodeId: inputNodeDomId.slice(5),
        outputPortClass: outputPortClass,
        inputPortClass: inputPortClass,
    };
};

// EXTERNAL MODULE: ./node_modules/sweetalert2/dist/sweetalert2.all.js
var sweetalert2_all = __webpack_require__(0);
var sweetalert2_all_default = /*#__PURE__*/__webpack_require__.n(sweetalert2_all);

// CONCATENATED MODULE: ./src/core/wireless.ts
var __awaiter = (undefined && undefined.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (undefined && undefined.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};

var LABEL_CLASS = 'df-wireless-label';
var LABEL_CONNECTED_CLASS = 'df-wireless-label--connected';
var WIRELESS_PORT_CLASS = 'has-wireless';
var LABEL_ID_PREFIX = 'df-wireless-label-';
var WIRELESS_CONNECTED_ATTRIBUTE = 'data-wireless-connected';
var MOVE_THRESHOLD = 5;
var hasWindow = typeof window !== 'undefined';
var isSwalLike = function (candidate) {
    return !!candidate && typeof candidate.fire === 'function';
};
var cachedImportedSwal;
var resolveImportedSwal = function () {
    if (cachedImportedSwal !== undefined) {
        return cachedImportedSwal;
    }
    var directCandidate = sweetalert2_all_default.a;
    if (isSwalLike(directCandidate)) {
        cachedImportedSwal = directCandidate;
        return cachedImportedSwal;
    }
    var defaultCandidate = directCandidate.default;
    if (isSwalLike(defaultCandidate)) {
        cachedImportedSwal = defaultCandidate;
        return cachedImportedSwal;
    }
    cachedImportedSwal = null;
    return cachedImportedSwal;
};
var getSwal = function () { return __awaiter(void 0, void 0, void 0, function () {
    var existing, resolved;
    return __generator(this, function (_a) {
        if (!hasWindow) {
            return [2 /*return*/, null];
        }
        existing = window.Swal;
        if (isSwalLike(existing)) {
            return [2 /*return*/, existing];
        }
        resolved = resolveImportedSwal();
        if (resolved) {
            window.Swal = resolved;
        }
        return [2 /*return*/, resolved];
    });
}); };
var escapeHtml = function (value) {
    return value
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;')
        .replace(/'/g, '&#39;');
};
function getModuleData(context, nodeId) {
    var _a, _b;
    var moduleName = context.getModuleFromNodeId(nodeId);
    if (!moduleName) {
        return null;
    }
    return (_b = (_a = context.drawflow.drawflow[moduleName]) === null || _a === void 0 ? void 0 : _a.data) !== null && _b !== void 0 ? _b : null;
}
function getPortData(context, ref) {
    var moduleData = getModuleData(context, ref.nodeId);
    if (!moduleData) {
        return null;
    }
    var node = moduleData[ref.nodeId];
    if (!node) {
        return null;
    }
    var collection = ref.type === 'input' ? node.inputs : node.outputs;
    var port = collection === null || collection === void 0 ? void 0 : collection[ref.portClass];
    if (!port) {
        return null;
    }
    return port;
}
function resolvePortWirelessName(port) {
    var storedWireless = port.wireless;
    if (typeof storedWireless === 'string' && storedWireless.trim() !== '') {
        return storedWireless.trim();
    }
    var connectionWithSignal = port.connections.find(function (connection) {
        return typeof connection.signal === 'string' && connection.signal.trim() !== '';
    });
    return connectionWithSignal ? connectionWithSignal.signal.trim() : null;
}
function applySignalValue(connection, normalized) {
    if (normalized !== '') {
        connection.signal = normalized;
    }
    else if ('signal' in connection) {
        delete connection.signal;
    }
}
function isSamePort(ref, candidateNodeId, candidatePortClass, candidateType) {
    if (ref.type !== candidateType) {
        return false;
    }
    if (candidateType === 'input' || candidateType === 'output') {
        return ref.nodeId === candidateNodeId.toString() && ref.portClass === candidatePortClass;
    }
    return false;
}
function isWirelessNameUsedByAnotherOutput(context, ref, name) {
    var moduleData = getModuleData(context, ref.nodeId);
    if (!moduleData) {
        return false;
    }
    var normalized = name.trim();
    if (normalized === '') {
        return false;
    }
    return Object.values(moduleData).some(function (node) {
        var _a;
        var outputs = (_a = node.outputs) !== null && _a !== void 0 ? _a : {};
        return Object.entries(outputs).some(function (_a) {
            var portClass = _a[0], portData = _a[1];
            if (isSamePort(ref, node.id, portClass, 'output')) {
                return false;
            }
            var portName = resolvePortWirelessName(portData);
            return portName === normalized;
        });
    });
}
function listWirelessNamePool(context, ref, existingName) {
    var moduleData = getModuleData(context, ref.nodeId);
    if (!moduleData) {
        return [];
    }
    var seen = new Set();
    var result = [];
    var considerName = function (name) {
        if (!name) {
            return;
        }
        var trimmed = name.trim();
        if (trimmed === '') {
            return;
        }
        if (seen.has(trimmed)) {
            return;
        }
        seen.add(trimmed);
        if (ref.type === 'output' && isWirelessNameUsedByAnotherOutput(context, ref, trimmed)) {
            return;
        }
        result.push(trimmed);
    };
    Object.values(moduleData).forEach(function (node) {
        var _a, _b;
        var inputs = (_a = node.inputs) !== null && _a !== void 0 ? _a : {};
        Object.values(inputs).forEach(function (port) {
            considerName(resolvePortWirelessName(port));
        });
        var outputs = (_b = node.outputs) !== null && _b !== void 0 ? _b : {};
        Object.values(outputs).forEach(function (port) {
            considerName(resolvePortWirelessName(port));
        });
    });
    if (existingName && existingName.trim() !== '') {
        var trimmedExisting = existingName.trim();
        if (!seen.has(trimmedExisting)) {
            seen.add(trimmedExisting);
            result.push(trimmedExisting);
        }
    }
    return result;
}
function getPortWirelessName(context, ref) {
    var port = getPortData(context, ref);
    if (!port) {
        return null;
    }
    return resolvePortWirelessName(port);
}
function isPortEligibleForWireless(context, ref) {
    var port = getPortData(context, ref);
    if (!port) {
        return false;
    }
    if (port.connections.length === 0) {
        return true;
    }
    return port.connections.every(function (connection) { return typeof connection.signal === 'string' && connection.signal.trim() !== ''; });
}
function getPortElement(context, ref) {
    var nodeElement = context.container.querySelector("#node-".concat(ref.nodeId));
    if (!nodeElement) {
        return null;
    }
    var selector = ref.type === 'input' ? ".inputs .".concat(ref.portClass) : ".outputs .".concat(ref.portClass);
    return nodeElement.querySelector(selector);
}
function ensureLabelElement(portElement) {
    var label = portElement.querySelector(".".concat(LABEL_CLASS));
    if (!label) {
        label = document.createElement('span');
        label.classList.add(LABEL_CLASS);
        label.id = "".concat(LABEL_ID_PREFIX).concat(Math.random().toString(36).slice(2));
        portElement.appendChild(label);
    }
    return label;
}
function clearLabelElement(portElement) {
    var label = portElement.querySelector(".".concat(LABEL_CLASS));
    if (label) {
        label.remove();
    }
}
function hasActiveWirelessConnection(port, signal) {
    if (!port) {
        return false;
    }
    var expectedSignal = signal === null || signal === void 0 ? void 0 : signal.trim();
    return port.connections.some(function (connection) {
        if (typeof connection.signal !== 'string') {
            return false;
        }
        var candidate = connection.signal.trim();
        if (candidate === '') {
            return false;
        }
        if (expectedSignal) {
            return candidate === expectedSignal;
        }
        return true;
    });
}
function applyConnectionState(portElement, label, port, signal) {
    var isConnected = hasActiveWirelessConnection(port, signal);
    label.classList.toggle(LABEL_CONNECTED_CLASS, isConnected);
    if (isConnected) {
        portElement.setAttribute(WIRELESS_CONNECTED_ATTRIBUTE, 'true');
    }
    else {
        portElement.removeAttribute(WIRELESS_CONNECTED_ATTRIBUTE);
    }
}
function setPortWirelessName(context, ref, name) {
    var port = getPortData(context, ref);
    var normalized = typeof name === 'string' ? name.trim() : '';
    if (port) {
        if (normalized !== '') {
            port.wireless = normalized;
        }
        else if ('wireless' in port) {
            delete port.wireless;
        }
        var moduleData_1 = getModuleData(context, ref.nodeId);
        if (!moduleData_1) {
            port.connections.forEach(function (connection) {
                applySignalValue(connection, normalized);
            });
        }
        else {
            port.connections.forEach(function (connection) {
                var _a, _b;
                applySignalValue(connection, normalized);
                if (ref.type === 'output') {
                    var targetNode = moduleData_1[connection.node];
                    var targetPort = (_a = targetNode === null || targetNode === void 0 ? void 0 : targetNode.inputs) === null || _a === void 0 ? void 0 : _a[connection.output];
                    var counterpart = targetPort === null || targetPort === void 0 ? void 0 : targetPort.connections.find(function (candidate) {
                        return candidate.node === ref.nodeId && candidate.input === ref.portClass;
                    });
                    if (counterpart) {
                        applySignalValue(counterpart, normalized);
                    }
                }
                else {
                    var targetNode = moduleData_1[connection.node];
                    var targetPort = (_b = targetNode === null || targetNode === void 0 ? void 0 : targetNode.outputs) === null || _b === void 0 ? void 0 : _b[connection.input];
                    var counterpart = targetPort === null || targetPort === void 0 ? void 0 : targetPort.connections.find(function (candidate) {
                        return candidate.node === ref.nodeId && candidate.output === ref.portClass;
                    });
                    if (counterpart) {
                        applySignalValue(counterpart, normalized);
                    }
                }
            });
        }
    }
    var element = getPortElement(context, ref);
    if (!element) {
        return;
    }
    if (normalized !== '') {
        var label = ensureLabelElement(element);
        label.addEventListener('click', function () {
            void openWirelessDialog(context, ref).catch(function (error) { return console.error(error); });
        });
        label.textContent = normalized;
        label.setAttribute('title', normalized);
        element.classList.add(WIRELESS_PORT_CLASS);
        element.setAttribute('data-wireless', normalized);
        applyConnectionState(element, label, port, normalized);
    }
    else {
        element.classList.remove(WIRELESS_PORT_CLASS);
        element.removeAttribute('data-wireless');
        element.removeAttribute(WIRELESS_CONNECTED_ATTRIBUTE);
        clearLabelElement(element);
    }
}
function buildWirelessConnections(context, ref) {
    var port = getPortData(context, ref);
    if (!port) {
        return [];
    }
    var descriptors = [];
    port.connections.forEach(function (connection) {
        if (typeof connection.signal !== 'string' || connection.signal.trim() === '') {
            return;
        }
        var signal = connection.signal.trim();
        if (ref.type === 'output') {
            descriptors.push({
                outputNodeId: ref.nodeId,
                inputNodeId: connection.node,
                outputPortClass: ref.portClass,
                inputPortClass: connection.output,
                signal: signal,
            });
        }
        else {
            descriptors.push({
                outputNodeId: connection.node,
                inputNodeId: ref.nodeId,
                outputPortClass: connection.input,
                inputPortClass: ref.portClass,
                signal: signal,
            });
        }
    });
    return descriptors;
}
function removeWirelessConnections(context, ref) {
    var descriptors = buildWirelessConnections(context, ref);
    descriptors.forEach(function (descriptor) {
        context.removeSingleConnection(descriptor.outputNodeId, descriptor.inputNodeId, descriptor.outputPortClass, descriptor.inputPortClass);
    });
}
function listAvailableOppositeEndpoints(context, ref) {
    var moduleData = getModuleData(context, ref.nodeId);
    if (!moduleData) {
        return [];
    }
    var oppositeType = ref.type === 'input' ? 'output' : 'input';
    var options = [];
    Object.values(moduleData).forEach(function (node) {
        var _a;
        var ports = (_a = (oppositeType === 'input' ? node.inputs : node.outputs)) !== null && _a !== void 0 ? _a : {};
        Object.entries(ports).forEach(function (_a) {
            var portClass = _a[0], portData = _a[1];
            var name = resolvePortWirelessName(portData);
            if (!name) {
                return;
            }
            if (oppositeType === 'input' && portData.connections.length > 0) {
                return;
            }
            if (ref.type === 'output' && isWirelessNameUsedByAnotherOutput(context, ref, name)) {
                return;
            }
            options.push({
                nodeId: node.id.toString(),
                portClass: portClass,
                type: oppositeType,
                name: name,
            });
        });
    });
    return options;
}
var encodeNameValue = function (name) {
    return "name|".concat(encodeURIComponent(name));
};
var findSelectOption = function (options, identifier) {
    if (!identifier) {
        return undefined;
    }
    return options.find(function (option) { return option.value === identifier; });
};
function buildDialogSelectOptions(context, ref, existingName, endpoints) {
    var optionMap = new Map();
    var registerOption = function (name) {
        var existing = optionMap.get(name);
        if (existing) {
            return existing;
        }
        var created = {
            value: encodeNameValue(name),
            label: name,
            name: name,
            endpoints: [],
        };
        optionMap.set(name, created);
        return created;
    };
    var pool = listWirelessNamePool(context, ref, existingName);
    pool.forEach(function (name) {
        registerOption(name);
    });
    endpoints.forEach(function (endpoint) {
        var normalized = endpoint.name.trim();
        if (normalized === '') {
            return;
        }
        var option = registerOption(normalized);
        var alreadyTracked = option.endpoints.some(function (tracked) {
            return (tracked.nodeId === endpoint.nodeId &&
                tracked.portClass === endpoint.portClass &&
                tracked.type === endpoint.type);
        });
        if (!alreadyTracked) {
            option.endpoints.push(endpoint);
        }
    });
    return Array.from(optionMap.values()).sort(function (a, b) { return a.name.localeCompare(b.name); });
}
function connectWirelessPorts(context, origin, target, signal) {
    var trimmedSignal = signal.trim();
    if (origin.type === 'output' && target.type === 'input') {
        context.addConnection(origin.nodeId, target.nodeId, origin.portClass, target.portClass, { signal: trimmedSignal, skipDom: true });
    }
    else if (origin.type === 'input' && target.type === 'output') {
        context.addConnection(target.nodeId, origin.nodeId, target.portClass, origin.portClass, { signal: trimmedSignal, skipDom: true });
    }
    setPortWirelessName(context, origin, trimmedSignal);
    setPortWirelessName(context, target, trimmedSignal);
}
function autoConnectByName(context, ref, name) {
    var trimmed = name.trim();
    if (trimmed === '') {
        setPortWirelessName(context, ref, null);
        return;
    }
    var candidates = listAvailableOppositeEndpoints(context, ref).filter(function (option) {
        return option.name === trimmed && !(option.nodeId === ref.nodeId && option.portClass === ref.portClass);
    });
    setPortWirelessName(context, ref, trimmed);
    candidates.forEach(function (candidate) {
        connectWirelessPorts(context, ref, candidate, trimmed);
    });
}
var listAttributeValue = 'drawflow-wireless-dialog__input-data';
function buildDialogHtml(existingName, options) {
    var escapedValue = existingName ? escapeHtml(existingName) : '';
    var hasSelectOptions = options.length > 0;
    var selectOptions = hasSelectOptions
        ? options
            .map(function (option) {
            return "<option value=\"".concat(escapeHtml(option.value), "\">").concat(escapeHtml(option.label), "</option>");
        })
            .join('')
        : '';
    var selectOptionsData = hasSelectOptions
        ? options
            .map(function (option) {
            return "<option>".concat(escapeHtml(option.label), "</option>");
        })
            .join('')
        : '';
    return "\n    <div class=\"drawflow-wireless-dialog\">\n      <label class=\"drawflow-wireless-dialog__label\" for=\"df-wireless-name-input\">Signal name</label>\n      <input\n        id=\"df-wireless-name-input\"\n        class=\"swal2-input\"\n        placeholder=\"Enter new signal name\"\n        value=\"".concat(escapedValue, "\"\n        autocomplete=\"off\"\n      >\n      ").concat(hasSelectOptions
        ? "\n                  <datalist id=\"".concat(listAttributeValue, "\">").concat(selectOptionsData, "</datalist>\n          <label class=\"drawflow-wireless-dialog__label\" for=\"df-wireless-name-select\">Connect to existing</label>\n                  <select id=\"df-wireless-name-select\" class=\"swal2-input swal2-select\">\n                        <option value=\"\">(none)</option>\n                        ").concat(selectOptions, "\n                  </select>\n                  ")
        : '', "\n    </div>\n  ");
}
function readDialogSelection() {
    var input = document.getElementById('df-wireless-name-input');
    var select = document.getElementById('df-wireless-name-select');
    return {
        name: input ? input.value.trim() : undefined,
        selectedId: select ? select.value : undefined,
    };
}
function shouldShowRemoveButton(context, ref, existingName) {
    if (existingName) {
        return true;
    }
    return buildWirelessConnections(context, ref).length > 0;
}
function openDialog(context, ref, existingName, selectOptions) {
    return __awaiter(this, void 0, void 0, function () {
        var modal, result;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0: return [4 /*yield*/, getSwal()];
                case 1:
                    modal = _a.sent();
                    if (!modal) {
                        return [2 /*return*/, null];
                    }
                    return [4 /*yield*/, modal.fire({
                            title: ref.type === 'input' ? 'Configure input signal' : 'Configure output signal',
                            html: buildDialogHtml(existingName, selectOptions),
                            focusConfirm: false,
                            didOpen: function () {
                                var input = document.getElementById('df-wireless-name-input');
                                var select = document.getElementById('df-wireless-name-select');
                                if (select) {
                                    //input?.blur();
                                    //select.focus();
                                    input.setAttribute('list', listAttributeValue);
                                }
                                else {
                                    input === null || input === void 0 ? void 0 : input.focus();
                                }
                            },
                            showCancelButton: true,
                            confirmButtonText: 'Save',
                            showDenyButton: shouldShowRemoveButton(context, ref, existingName),
                            denyButtonText: 'Remove name',
                            preConfirm: function () {
                                var _a, _b, _c, _d;
                                var selection = readDialogSelection();
                                var selectedOption = findSelectOption(selectOptions, selection.selectedId);
                                if (selectedOption) {
                                    selection.name = selectedOption.name;
                                }
                                var candidateName = (_b = (_a = selection.name) === null || _a === void 0 ? void 0 : _a.trim()) !== null && _b !== void 0 ? _b : '';
                                if (!selection.selectedId && candidateName === '') {
                                    (_c = modal.showValidationMessage) === null || _c === void 0 ? void 0 : _c.call(modal, 'Signal name cannot be empty.');
                                    return false;
                                }
                                if (ref.type === 'output' && candidateName !== '' && isWirelessNameUsedByAnotherOutput(context, ref, candidateName)) {
                                    (_d = modal.showValidationMessage) === null || _d === void 0 ? void 0 : _d.call(modal, 'Signal name is already used by another output.');
                                    return false;
                                }
                                selection.name = candidateName;
                                if (selection.selectedId && candidateName !== '') {
                                    return selection;
                                }
                                if (selection.selectedId || candidateName !== '') {
                                    return selection;
                                }
                                return { selectedId: '', name: existingName !== null && existingName !== void 0 ? existingName : '' };
                            },
                        })];
                case 2:
                    result = _a.sent();
                    if (result.isDismissed) {
                        return [2 /*return*/, null];
                    }
                    if (result.isDenied) {
                        return [2 /*return*/, 'remove'];
                    }
                    return [2 /*return*/, result.value];
            }
        });
    });
}
function applySelection(context, ref, selection, selectOptions) {
    var _a, _b;
    if (!selection) {
        return;
    }
    if (selection === 'remove') {
        removeWirelessConnections(context, ref);
        setPortWirelessName(context, ref, null);
        return;
    }
    var trimmedName = (_b = (_a = selection.name) === null || _a === void 0 ? void 0 : _a.trim()) !== null && _b !== void 0 ? _b : '';
    var option = findSelectOption(selectOptions, selection.selectedId);
    if (option) {
        removeWirelessConnections(context, ref);
        setPortWirelessName(context, ref, option.name);
        if (option.endpoints.length > 0) {
            option.endpoints.forEach(function (endpoint) {
                connectWirelessPorts(context, ref, endpoint, option.name);
            });
        }
        else {
            autoConnectByName(context, ref, option.name);
        }
        return;
    }
    if (trimmedName !== '') {
        removeWirelessConnections(context, ref);
        autoConnectByName(context, ref, trimmedName);
    }
}
function openWirelessDialog(context, ref) {
    return __awaiter(this, void 0, void 0, function () {
        var existingName, optionsSnapshot, selectOptions, selection;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    existingName = getPortWirelessName(context, ref);
                    optionsSnapshot = listAvailableOppositeEndpoints(context, ref);
                    selectOptions = buildDialogSelectOptions(context, ref, existingName, optionsSnapshot);
                    return [4 /*yield*/, openDialog(context, ref, existingName, selectOptions)];
                case 1:
                    selection = _a.sent();
                    applySelection(context, ref, selection, selectOptions);
                    return [2 /*return*/];
            }
        });
    });
}
function movementExceedsThreshold(context, currentX, currentY) {
    var deltaX = Math.abs(context.pos_x_start - currentX);
    var deltaY = Math.abs(context.pos_y_start - currentY);
    return deltaX > MOVE_THRESHOLD || deltaY > MOVE_THRESHOLD;
}
function refreshWirelessPorts(context) {
    var _a, _b;
    var modules = (_b = (_a = context.drawflow) === null || _a === void 0 ? void 0 : _a.drawflow) !== null && _b !== void 0 ? _b : {};
    Object.values(modules).forEach(function (module) {
        var _a;
        var nodes = (_a = module === null || module === void 0 ? void 0 : module.data) !== null && _a !== void 0 ? _a : {};
        Object.values(nodes).forEach(function (node) {
            var _a, _b;
            var nodeId = node.id.toString();
            var inputs = (_a = node.inputs) !== null && _a !== void 0 ? _a : {};
            Object.keys(inputs).forEach(function (inputClass) {
                var _a;
                var portData = inputs[inputClass];
                var name = (_a = resolvePortWirelessName(portData)) !== null && _a !== void 0 ? _a : null;
                setPortWirelessName(context, { nodeId: nodeId, portClass: inputClass, type: 'input' }, name);
            });
            var outputs = (_b = node.outputs) !== null && _b !== void 0 ? _b : {};
            Object.keys(outputs).forEach(function (outputClass) {
                var _a;
                var portData = outputs[outputClass];
                var name = (_a = resolvePortWirelessName(portData)) !== null && _a !== void 0 ? _a : null;
                setPortWirelessName(context, { nodeId: nodeId, portClass: outputClass, type: 'output' }, name);
            });
        });
    });
}

// CONCATENATED MODULE: ./src/core/lifecycle/interactions.ts



var interactions_hasWindow = typeof window !== 'undefined';
var requestFrame = interactions_hasWindow && typeof window.requestAnimationFrame === 'function'
    ? window.requestAnimationFrame.bind(window)
    : function (callback) { return setTimeout(function () { return callback(Date.now()); }, 16); };
var cancelFrame = interactions_hasWindow && typeof window.cancelAnimationFrame === 'function'
    ? window.cancelAnimationFrame.bind(window)
    : function (id) { return clearTimeout(id); };
function click(context, e) {
    context.dispatch('click', e);
    if (document.activeElement !== context.container) {
        context.container.focus({ preventScroll: true });
    }
    var target = e.target;
    if (!target) {
        return;
    }
    if (context.editor_mode === 'fixed') {
        if (target.classList.contains('parent-drawflow') || target.classList.contains('drawflow')) {
            context.ele_selected = target.closest('.parent-drawflow');
            e.preventDefault();
        }
        else {
            return;
        }
    }
    else if (context.editor_mode === 'view') {
        if (target.closest('.drawflow') != null || target.matches('.parent-drawflow')) {
            context.ele_selected = target.closest('.parent-drawflow');
            e.preventDefault();
        }
    }
    else {
        context.first_click = target;
        context.ele_selected = target;
        if ('button' in e && e.button === 0) {
            context.contextmenuDel();
        }
        var contentNode = target.closest('.drawflow_content_node');
        if (contentNode) {
            context.ele_selected = contentNode.parentElement;
        }
    }
    if (!context.ele_selected) {
        return;
    }
    var selectedElement = context.ele_selected;
    var selectedClasses = selectedElement.classList;
    var isNode = selectedClasses.contains('drawflow-node');
    var isOutput = selectedClasses.contains('output');
    var isInput = selectedClasses.contains('input');
    var isEditor = selectedClasses.contains('parent-drawflow') || selectedClasses.contains('drawflow');
    var isMainPath = selectedClasses.contains('main-path');
    var isPoint = selectedClasses.contains('point');
    var isDeleteAction = selectedClasses.contains('drawflow-delete');
    if (isNode) {
        handleNodeSelected(context);
    }
    else if (isOutput || isInput) {
        handlePortSelected(context, selectedElement);
    }
    else if (isEditor) {
        handleEditorSelected(context);
    }
    else if (isMainPath) {
        handleConnectionSelected(context);
    }
    else if (isPoint) {
        context.drag_point = true;
        selectedElement.classList.add('selected');
    }
    else if (isDeleteAction) {
        if (context.node_selected) {
            context.removeNodeId(context.node_selected.id);
        }
        if (context.connection_selected) {
            context.removeConnection();
        }
        if (context.node_selected) {
            context.node_selected.classList.remove('selected');
            context.node_selected = null;
            context.dispatch('nodeUnselected', true);
        }
        if (context.connection_selected) {
            context.connection_selected.classList.remove('selected');
            context.removeReouteConnectionSelected();
            context.connection_selected = null;
        }
    }
    if (e.type === 'touchstart') {
        var touchEvent = e;
        context.pos_x = touchEvent.touches[0].clientX;
        context.pos_x_start = touchEvent.touches[0].clientX;
        context.pos_y = touchEvent.touches[0].clientY;
        context.pos_y_start = touchEvent.touches[0].clientY;
        context.mouse_x = touchEvent.touches[0].clientX;
        context.mouse_y = touchEvent.touches[0].clientY;
    }
    else {
        var mouseEvent = e;
        context.pos_x = mouseEvent.clientX;
        context.pos_x_start = mouseEvent.clientX;
        context.pos_y = mouseEvent.clientY;
        context.pos_y_start = mouseEvent.clientY;
    }
    if (isInput || isOutput || isMainPath) {
        e.preventDefault();
    }
    context.dispatch('clickEnd', e);
}
function handleNodeSelected(context) {
    if (context.node_selected) {
        context.node_selected.classList.remove('selected');
        if (context.node_selected !== context.ele_selected) {
            context.dispatch('nodeUnselected', true);
        }
    }
    if (context.connection_selected) {
        context.connection_selected.classList.remove('selected');
        context.removeReouteConnectionSelected();
        context.connection_selected = null;
    }
    if (context.node_selected !== context.ele_selected) {
        context.dispatch('nodeSelected', context.ele_selected.id.slice(5));
    }
    context.node_selected = context.ele_selected;
    context.node_selected.classList.add('selected');
    var target = context.first_click;
    var isEditableTarget = target && (['INPUT', 'TEXTAREA', 'SELECT'].includes(target.tagName) || target.hasAttribute('contenteditable'));
    if (!context.draggable_inputs) {
        if (!isEditableTarget) {
            context.drag = true;
        }
    }
    else if (!target || target.tagName !== 'SELECT') {
        context.drag = true;
    }
}
function handlePortSelected(context, element) {
    var portReference = getPortReferenceFromElement(element);
    if (portReference && isPortEligibleForWireless(context, portReference)) {
        context.pending_wireless = portReference;
    }
    else {
        context.pending_wireless = null;
    }
    context.connection = true;
    if (context.node_selected) {
        context.node_selected.classList.remove('selected');
        context.node_selected = null;
        context.dispatch('nodeUnselected', true);
    }
    if (context.connection_selected) {
        context.connection_selected.classList.remove('selected');
        context.removeReouteConnectionSelected();
        context.connection_selected = null;
    }
    context.drawConnection(element);
}
function getPortReferenceFromElement(element) {
    var _a;
    var isInput = element.classList.contains('input');
    var isOutput = element.classList.contains('output');
    if (!isInput && !isOutput) {
        return null;
    }
    var nodeElement = (_a = element.parentElement) === null || _a === void 0 ? void 0 : _a.parentElement;
    if (!nodeElement || !nodeElement.id.startsWith('node-')) {
        return null;
    }
    var portClass = findClassWithPrefix(element.classList, isInput ? 'input_' : 'output_');
    if (!portClass) {
        return null;
    }
    return {
        nodeId: nodeElement.id.slice(5),
        portClass: portClass,
        type: isInput ? 'input' : 'output',
    };
}
function handleEditorSelected(context) {
    if (context.node_selected) {
        context.node_selected.classList.remove('selected');
        context.node_selected = null;
        context.dispatch('nodeUnselected', true);
    }
    if (context.connection_selected) {
        context.connection_selected.classList.remove('selected');
        context.removeReouteConnectionSelected();
        context.connection_selected = null;
    }
    context.editor_selected = true;
}
function handleConnectionSelected(context) {
    if (context.node_selected) {
        context.node_selected.classList.remove('selected');
        context.node_selected = null;
        context.dispatch('nodeUnselected', true);
    }
    if (context.connection_selected) {
        context.connection_selected.classList.remove('selected');
        context.removeReouteConnectionSelected();
        context.connection_selected = null;
    }
    context.connection_selected = context.ele_selected;
    context.connection_selected.classList.add('selected');
    var connectionElement = context.connection_selected.parentElement;
    if (!connectionElement) {
        return;
    }
    var connectionInfo = extractConnectionClassInfo(connectionElement.classList);
    if (connectionInfo) {
        context.dispatch('connectionSelected', {
            output_id: connectionInfo.outputNodeId,
            input_id: connectionInfo.inputNodeId,
            output_class: connectionInfo.outputPortClass,
            input_class: connectionInfo.inputPortClass,
        });
        if (context.reroute_fix_curvature) {
            connectionElement.querySelectorAll('.main-path').forEach(function (item) {
                item.classList.add('selected');
            });
        }
    }
}
function position(context, e) {
    var e_pos_x;
    var e_pos_y;
    if (e.type === 'touchmove') {
        var touchEvent = e;
        e_pos_x = touchEvent.touches[0].clientX;
        e_pos_y = touchEvent.touches[0].clientY;
    }
    else {
        var mouseEvent = e;
        e_pos_x = mouseEvent.clientX;
        e_pos_y = mouseEvent.clientY;
    }
    if (context.connection) {
        context.updateConnection(e_pos_x, e_pos_y);
        if (context.pending_wireless && movementExceedsThreshold(context, e_pos_x, e_pos_y)) {
            context.pending_wireless = null;
        }
        handleConnectionAutoPan(context, e_pos_x, e_pos_y);
    }
    else if (context.drag && context.ele_selected) {
        handleNodeAutoPan(context, e_pos_x, e_pos_y);
    }
    else {
        stopAutoPan(context);
    }
    if (context.editor_selected) {
        var x = context.canvas_x + (-(context.pos_x - e_pos_x));
        var y = context.canvas_y + (-(context.pos_y - e_pos_y));
        context.dispatch('translate', { x: x, y: y });
        applyCanvasTranslation(context, x, y);
    }
    if (context.drag && context.ele_selected) {
        e.preventDefault();
        var x = (context.pos_x - e_pos_x) * context.precanvas.clientWidth / (context.precanvas.clientWidth * context.zoom);
        var y = (context.pos_y - e_pos_y) * context.precanvas.clientHeight / (context.precanvas.clientHeight * context.zoom);
        context.pos_x = e_pos_x;
        context.pos_y = e_pos_y;
        context.ele_selected.style.top = "".concat(context.ele_selected.offsetTop - y, "px");
        context.ele_selected.style.left = "".concat(context.ele_selected.offsetLeft - x, "px");
        var nodeId = context.ele_selected.id.slice(5);
        context.drawflow.drawflow[context.module].data[nodeId].pos_x = context.ele_selected.offsetLeft - x;
        context.drawflow.drawflow[context.module].data[nodeId].pos_y = context.ele_selected.offsetTop - y;
        context.updateConnectionNodes(context.ele_selected.id);
    }
    if (context.drag_point && context.ele_selected) {
        var x = (context.pos_x - e_pos_x) * context.precanvas.clientWidth / (context.precanvas.clientWidth * context.zoom);
        var y = (context.pos_y - e_pos_y) * context.precanvas.clientHeight / (context.precanvas.clientHeight * context.zoom);
        context.pos_x = e_pos_x;
        context.pos_y = e_pos_y;
        var pos_x = context.pos_x * (context.precanvas.clientWidth / (context.precanvas.clientWidth * context.zoom)) -
            (context.precanvas.getBoundingClientRect().x * (context.precanvas.clientWidth / (context.precanvas.clientWidth * context.zoom)));
        var pos_y = context.pos_y * (context.precanvas.clientHeight / (context.precanvas.clientHeight * context.zoom)) -
            (context.precanvas.getBoundingClientRect().y * (context.precanvas.clientHeight / (context.precanvas.clientHeight * context.zoom)));
        context.ele_selected.setAttributeNS(null, 'cx', pos_x.toString());
        context.ele_selected.setAttributeNS(null, 'cy', pos_y.toString());
        var connectionElement = context.ele_selected.parentElement;
        if (!connectionElement) {
            return;
        }
        var connectionInfo = extractConnectionClassInfo(connectionElement.classList);
        if (!connectionInfo) {
            return;
        }
        var outputNodeDomId = connectionInfo.outputNodeDomId, outputNodeId = connectionInfo.outputNodeId, inputNodeId_1 = connectionInfo.inputNodeId, outputPortClass = connectionInfo.outputPortClass, inputPortClass_1 = connectionInfo.inputPortClass;
        var numberPointPosition = Array.from(connectionElement.children).indexOf(context.ele_selected) - 1;
        if (context.reroute_fix_curvature) {
            var numberMainPath = connectionElement.querySelectorAll('.main-path').length - 1;
            numberPointPosition -= numberMainPath;
            if (numberPointPosition < 0) {
                numberPointPosition = 0;
            }
        }
        var nodeId = outputNodeId;
        var searchConnection = context.drawflow.drawflow[context.module].data[nodeId].outputs[outputPortClass].connections.findIndex(function (item) {
            return item.node === inputNodeId_1 && item.output === inputPortClass_1;
        });
        context.drawflow.drawflow[context.module].data[nodeId].outputs[outputPortClass].connections[searchConnection].points[numberPointPosition] = {
            pos_x: pos_x,
            pos_y: pos_y
        };
        context.updateConnectionNodes(outputNodeDomId);
    }
    if (e.type === 'touchmove') {
        var touchEvent = e;
        context.mouse_x = touchEvent.touches[0].clientX;
        context.mouse_y = touchEvent.touches[0].clientY;
    }
    context.dispatch('mouseMove', { x: e_pos_x, y: e_pos_y });
}
function dragEnd(context, e) {
    var e_pos_x;
    var e_pos_y;
    var ele_last = null;
    if (e.type === 'touchend') {
        e_pos_x = context.mouse_x;
        e_pos_y = context.mouse_y;
        ele_last = document.elementFromPoint(e_pos_x, e_pos_y);
    }
    else {
        var mouseEvent = e;
        e_pos_x = mouseEvent.clientX;
        e_pos_y = mouseEvent.clientY;
        ele_last = mouseEvent.target;
    }
    if (context.drag && context.ele_selected) {
        if (context.pos_x_start !== e_pos_x || context.pos_y_start !== e_pos_y) {
            context.dispatch('nodeMoved', context.ele_selected.id.slice(5));
        }
    }
    if (context.drag_point && context.ele_selected) {
        context.ele_selected.classList.remove('selected');
        if (context.pos_x_start !== e_pos_x || context.pos_y_start !== e_pos_y) {
            var connectionElement = context.ele_selected.parentElement;
            var connectionInfo = connectionElement ? extractConnectionClassInfo(connectionElement.classList) : null;
            if (connectionInfo) {
                context.dispatch('rerouteMoved', connectionInfo.outputNodeId);
            }
        }
    }
    if (context.editor_selected) {
        context.canvas_x = context.canvas_x + (-(context.pos_x - e_pos_x));
        context.canvas_y = context.canvas_y + (-(context.pos_y - e_pos_y));
        context.editor_selected = false;
    }
    if (context.connection === true && context.ele_selected) {
        handleConnectionDrop(context, ele_last);
        context.connection_ele = null;
    }
    stopAutoPan(context);
    context.drag = false;
    context.drag_point = false;
    context.connection = false;
    context.ele_selected = null;
    context.editor_selected = false;
    context.dispatch('mouseUp', e);
    var pendingWireless = context.pending_wireless;
    context.pending_wireless = null;
    if (pendingWireless && context.editor_mode === 'edit') {
        void openWirelessDialog(context, pendingWireless).catch(function (error) { return console.error(error); });
    }
}
function handleConnectionDrop(context, ele_last) {
    var _a, _b, _c;
    if (!ele_last || !context.connection_ele) {
        context.dispatch('connectionCancel', true);
        (_a = context.connection_ele) === null || _a === void 0 ? void 0 : _a.remove();
        return;
    }
    var targetIsOutput = ele_last.classList.contains('output');
    var targetIsInput = ele_last.classList.contains('input');
    var targetIsNode = ele_last.classList.contains('drawflow-node');
    var sourceIsInput = context.ele_selected.classList.contains('input');
    if (targetIsInput || targetIsOutput || ele_last.closest('.drawflow_content_node') != null || targetIsNode) {
        var input_id_1;
        var input_class_1;
        var output_id_1;
        var output_class_1;
        if (sourceIsInput && targetIsOutput) {
            input_id_1 = context.ele_selected.parentElement.parentElement.id;
            input_class_1 = findClassWithPrefix(context.ele_selected.classList, 'input_');
            output_id_1 = ele_last.parentElement.parentElement.id;
            output_class_1 = findClassWithPrefix(ele_last.classList, 'output_');
        }
        else if (!sourceIsInput && (targetIsInput || ele_last.closest('.drawflow_content_node'))) {
            var container = ele_last.closest('.drawflow_content_node');
            input_id_1 = container ? container.parentElement.id : ele_last.parentElement.parentElement.id;
            input_class_1 = targetIsInput ? findClassWithPrefix(ele_last.classList, 'input_') : 'input_1';
            output_id_1 = context.ele_selected.parentElement.parentElement.id;
            output_class_1 = findClassWithPrefix(context.ele_selected.classList, 'output_');
        }
        if (input_id_1 && output_id_1 && input_id_1 !== output_id_1 && input_class_1 && output_class_1) {
            var outputNode = context.drawflow.drawflow[context.module].data[output_id_1.slice(5)];
            var inputNode = context.drawflow.drawflow[context.module].data[input_id_1.slice(5)];
            var connectionExists = false;
            var inputPort = (_b = inputNode === null || inputNode === void 0 ? void 0 : inputNode.inputs) === null || _b === void 0 ? void 0 : _b[input_class_1];
            var inputHasWirelessName = (_c = inputPort === null || inputPort === void 0 ? void 0 : inputPort.connections.some(function (conn) {
                return typeof conn.signal === 'string' && conn.signal.trim() !== '';
            })) !== null && _c !== void 0 ? _c : false;
            if (inputHasWirelessName) {
                context.dispatch('connectionCancel', true);
                context.connection_ele.remove();
                context.pending_wireless = null;
                return;
            }
            if (outputNode && outputNode.outputs[output_class_1]) {
                connectionExists = outputNode.outputs[output_class_1].connections.some(function (conn) {
                    return conn.node === input_id_1.slice(5) && conn.output === input_class_1;
                });
            }
            if (!connectionExists && inputNode && inputNode.inputs[input_class_1]) {
                connectionExists = inputNode.inputs[input_class_1].connections.some(function (conn) {
                    return conn.node === output_id_1.slice(5) && conn.input === output_class_1;
                });
            }
            if (!connectionExists) {
                context.connection_ele.classList.add("node_in_".concat(input_id_1));
                context.connection_ele.classList.add("node_out_".concat(output_id_1));
                context.connection_ele.classList.add(output_class_1);
                context.connection_ele.classList.add(input_class_1);
                context.drawflow.drawflow[context.module].data[output_id_1.slice(5)].outputs[output_class_1].connections.push({
                    node: input_id_1.slice(5),
                    output: input_class_1
                });
                context.drawflow.drawflow[context.module].data[input_id_1.slice(5)].inputs[input_class_1].connections.push({
                    node: output_id_1.slice(5),
                    input: output_class_1
                });
                context.updateConnectionNodes("node-".concat(output_id_1.slice(5)));
                context.updateConnectionNodes("node-".concat(input_id_1.slice(5)));
                context.dispatch('connectionCreated', {
                    output_id: output_id_1.slice(5),
                    input_id: input_id_1.slice(5),
                    output_class: output_class_1,
                    input_class: input_class_1
                });
                context.pending_wireless = null;
            }
            else {
                context.dispatch('connectionCancel', true);
                context.connection_ele.remove();
            }
        }
        else {
            context.dispatch('connectionCancel', true);
            context.connection_ele.remove();
        }
    }
    else {
        context.dispatch('connectionCancel', true);
        context.connection_ele.remove();
    }
}
function handleConnectionAutoPan(context, pointerX, pointerY) {
    handleAutoPan(context, 'connection', pointerX, pointerY);
}
function handleNodeAutoPan(context, pointerX, pointerY) {
    if (!context.drag || !context.ele_selected) {
        stopAutoPan(context);
        return;
    }
    handleAutoPan(context, 'node', pointerX, pointerY);
}
function handleAutoPan(context, mode, pointerX, pointerY) {
    if (!context.precanvas) {
        return;
    }
    context.autoPanPointerX = pointerX;
    context.autoPanPointerY = pointerY;
    context.autoPanMode = mode;
    if (!shouldAutoPan(context, pointerX, pointerY)) {
        stopAutoPan(context);
        return;
    }
    scheduleAutoPan(context);
}
function performAutoPan(context) {
    if (!context.autoPanMode || !context.precanvas) {
        stopAutoPan(context);
        return;
    }
    var pointerX = context.autoPanPointerX;
    var pointerY = context.autoPanPointerY;
    if (!shouldAutoPan(context, pointerX, pointerY)) {
        stopAutoPan(context);
        return;
    }
    var margin = Math.max(context.autoPanEdgeMargin, 0);
    var speed = context.autoPanSpeed;
    if (margin === 0 || speed === 0) {
        stopAutoPan(context);
        return;
    }
    var rect = context.container.getBoundingClientRect();
    var distanceLeft = pointerX - rect.left;
    var distanceRight = rect.right - pointerX;
    var distanceTop = pointerY - rect.top;
    var distanceBottom = rect.bottom - pointerY;
    var stepLeft = calculateAutoPanStep(distanceLeft, margin, speed, 1);
    var stepRight = calculateAutoPanStep(distanceRight, margin, speed, -1);
    var stepTop = calculateAutoPanStep(distanceTop, margin, speed, 1);
    var stepBottom = calculateAutoPanStep(distanceBottom, margin, speed, -1);
    var stepX = stepLeft !== 0 ? stepLeft : stepRight;
    var stepY = stepTop !== 0 ? stepTop : stepBottom;
    if (stepX === 0 && stepY === 0) {
        stopAutoPan(context);
        return;
    }
    context.canvas_x += stepX;
    context.canvas_y += stepY;
    applyStoredCanvasTranslation(context);
    if (context.autoPanMode === 'node') {
        adjustDraggedNodeDuringAutoPan(context, stepX, stepY);
    }
    else if (context.autoPanMode === 'connection') {
        context.updateConnection(pointerX, pointerY);
    }
    scheduleAutoPan(context);
}
function adjustDraggedNodeDuringAutoPan(context, stepX, stepY) {
    if (!context.drag || !context.ele_selected) {
        return;
    }
    var zoom = context.zoom || 1;
    var deltaX = -stepX / zoom;
    var deltaY = -stepY / zoom;
    if (deltaX === 0 && deltaY === 0) {
        return;
    }
    var node = context.ele_selected;
    var currentLeft = getPreciseNodePosition(node.style.left, node.offsetLeft);
    var currentTop = getPreciseNodePosition(node.style.top, node.offsetTop);
    var nextLeft = currentLeft + deltaX;
    var nextTop = currentTop + deltaY;
    node.style.left = "".concat(nextLeft, "px");
    node.style.top = "".concat(nextTop, "px");
    var nodeId = node.id.slice(5);
    var moduleData = context.drawflow.drawflow[context.module].data[nodeId];
    if (moduleData) {
        moduleData.pos_x = nextLeft;
        moduleData.pos_y = nextTop;
    }
    context.updateConnectionNodes(node.id);
}
function getPreciseNodePosition(styleValue, fallback) {
    var parsed = parseFloat(styleValue);
    return Number.isNaN(parsed) ? fallback : parsed;
}
function calculateAutoPanStep(distance, margin, speed, direction) {
    if (distance >= margin) {
        return 0;
    }
    var normalizedDistance = Math.max(distance, 0);
    var intensity = (margin - normalizedDistance) / margin;
    return direction * intensity * speed;
}
function shouldAutoPan(context, pointerX, pointerY) {
    var margin = Math.max(context.autoPanEdgeMargin, 0);
    if (margin === 0) {
        return false;
    }
    var rect = context.container.getBoundingClientRect();
    return (pointerX <= rect.left + margin ||
        pointerX >= rect.right - margin ||
        pointerY <= rect.top + margin ||
        pointerY >= rect.bottom - margin);
}
function scheduleAutoPan(context) {
    if (context.autoPanFrame != null) {
        return;
    }
    context.autoPanFrame = requestFrame(function () {
        context.autoPanFrame = null;
        performAutoPan(context);
    });
}
function stopAutoPan(context) {
    if (context.autoPanFrame != null) {
        cancelFrame(context.autoPanFrame);
        context.autoPanFrame = null;
    }
    context.autoPanMode = null;
}
function contextmenu(context, e) {
    context.dispatch('contextmenu', e);
    e.preventDefault();
    if (context.editor_mode === 'fixed' || context.editor_mode === 'view') {
        return;
    }
    if (context.precanvas.getElementsByClassName('drawflow-delete').length) {
        context.precanvas.getElementsByClassName('drawflow-delete')[0].remove();
    }
    if (context.node_selected || context.connection_selected) {
        var deletebox = document.createElement('div');
        deletebox.classList.add('drawflow-delete');
        deletebox.innerHTML = 'x';
        if (context.node_selected) {
            context.node_selected.appendChild(deletebox);
        }
        if (context.connection_selected && context.connection_selected.parentElement && context.connection_selected.parentElement.classList.length > 1) {
            deletebox.style.top = "".concat(e.clientY * (context.precanvas.clientHeight / (context.precanvas.clientHeight * context.zoom)) -
                (context.precanvas.getBoundingClientRect().y * (context.precanvas.clientHeight / (context.precanvas.clientHeight * context.zoom))), "px");
            deletebox.style.left = "".concat(e.clientX * (context.precanvas.clientWidth / (context.precanvas.clientWidth * context.zoom)) -
                (context.precanvas.getBoundingClientRect().x * (context.precanvas.clientWidth / (context.precanvas.clientWidth * context.zoom))), "px");
            context.precanvas.appendChild(deletebox);
        }
    }
}
function contextmenuDel(context) {
    if (context.precanvas.getElementsByClassName('drawflow-delete').length) {
        context.precanvas.getElementsByClassName('drawflow-delete')[0].remove();
    }
}
function key(context, e) {
    var _a, _b;
    context.dispatch('keydown', e);
    if (context.editor_mode === 'fixed' || context.editor_mode === 'view') {
        return;
    }
    if (e.key === 'Delete' || (e.key === 'Backspace' && e.metaKey)) {
        if (context.node_selected) {
            var tagName = (_a = context.first_click) === null || _a === void 0 ? void 0 : _a.tagName;
            var editable = (_b = context.first_click) === null || _b === void 0 ? void 0 : _b.hasAttribute('contenteditable');
            if (tagName !== 'INPUT' && tagName !== 'TEXTAREA' && editable !== true) {
                context.removeNodeId(context.node_selected.id);
            }
        }
        if (context.connection_selected) {
            context.removeConnection();
        }
    }
}

// CONCATENATED MODULE: ./src/core/lifecycle/index.ts




// CONCATENATED MODULE: ./src/core/connections.ts
var __assign = (undefined && undefined.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __spreadArray = (undefined && undefined.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};


function createCurvature(context, start_pos_x, start_pos_y, end_pos_x, end_pos_y, curvature_value, type) {
    var line_x = start_pos_x;
    var line_y = start_pos_y;
    var x = end_pos_x;
    var y = end_pos_y;
    var curvature = curvature_value;
    switch (type) {
        case 'open':
            if (start_pos_x >= end_pos_x) {
                var hx1 = line_x + Math.abs(x - line_x) * curvature;
                var hx2 = x - Math.abs(x - line_x) * (curvature * -1);
                return " M ".concat(line_x, " ").concat(line_y, " C ").concat(hx1, " ").concat(line_y, " ").concat(hx2, " ").concat(y, " ").concat(x, "  ").concat(y);
            }
            return " M ".concat(line_x, " ").concat(line_y, " C ").concat(line_x + Math.abs(x - line_x) * curvature, " ").concat(line_y, " ").concat(x - Math.abs(x - line_x) * curvature, " ").concat(y, " ").concat(x, "  ").concat(y);
        case 'close':
            if (start_pos_x >= end_pos_x) {
                var hx1 = line_x + Math.abs(x - line_x) * (curvature * -1);
                var hx2 = x - Math.abs(x - line_x) * curvature;
                return " M ".concat(line_x, " ").concat(line_y, " C ").concat(hx1, " ").concat(line_y, " ").concat(hx2, " ").concat(y, " ").concat(x, "  ").concat(y);
            }
            return " M ".concat(line_x, " ").concat(line_y, " C ").concat(line_x + Math.abs(x - line_x) * curvature, " ").concat(line_y, " ").concat(x - Math.abs(x - line_x) * curvature, " ").concat(y, " ").concat(x, "  ").concat(y);
        case 'other':
            if (start_pos_x >= end_pos_x) {
                var hx1 = line_x + Math.abs(x - line_x) * (curvature * -1);
                var hx2 = x - Math.abs(x - line_x) * (curvature * -1);
                return " M ".concat(line_x, " ").concat(line_y, " C ").concat(hx1, " ").concat(line_y, " ").concat(hx2, " ").concat(y, " ").concat(x, "  ").concat(y);
            }
            return " M ".concat(line_x, " ").concat(line_y, " C ").concat(line_x + Math.abs(x - line_x) * curvature, " ").concat(line_y, " ").concat(x - Math.abs(x - line_x) * curvature, " ").concat(y, " ").concat(x, "  ").concat(y);
        default: {
            var hx1 = line_x + Math.abs(x - line_x) * curvature;
            var hx2 = x - Math.abs(x - line_x) * curvature;
            return " M ".concat(line_x, " ").concat(line_y, " C ").concat(hx1, " ").concat(line_y, " ").concat(hx2, " ").concat(y, " ").concat(x, "  ").concat(y);
        }
    }
}
function drawConnection(context, ele) {
    var connection = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
    context.connection_ele = connection;
    var path = document.createElementNS('http://www.w3.org/2000/svg', 'path');
    path.classList.add('main-path');
    path.setAttributeNS(null, 'd', '');
    connection.classList.add('connection');
    connection.appendChild(path);
    context.precanvas.appendChild(connection);
    if (ele.classList.contains('input')) {
        var id_input = ele.parentElement.parentElement.id.slice(5);
        var inputClass = findClassWithPrefix(ele.classList, 'input_');
        if (!inputClass) {
            return;
        }
        context.dispatch('connectionStart', { input_id: id_input, input_class: inputClass });
    }
    else {
        var id_output = ele.parentElement.parentElement.id.slice(5);
        var outputClass = findClassWithPrefix(ele.classList, 'output_');
        if (!outputClass) {
            return;
        }
        context.dispatch('connectionStart', { output_id: id_output, output_class: outputClass });
    }
}
function updateConnection(context, eX, eY) {
    var precanvas = context.precanvas;
    var zoom = context.zoom;
    var precanvasWidthZoom = precanvas.clientWidth / (precanvas.clientWidth * zoom);
    precanvasWidthZoom = precanvasWidthZoom || 0;
    var precanvasHeightZoom = precanvas.clientHeight / (precanvas.clientHeight * zoom);
    precanvasHeightZoom = precanvasHeightZoom || 0;
    var path = context.connection_ele.children[0];
    var line_x = context.ele_selected.offsetWidth / 2 +
        (context.ele_selected.getBoundingClientRect().x - precanvas.getBoundingClientRect().x) * precanvasWidthZoom;
    var line_y = context.ele_selected.offsetHeight / 2 +
        (context.ele_selected.getBoundingClientRect().y - precanvas.getBoundingClientRect().y) * precanvasHeightZoom;
    var x = eX * (precanvas.clientWidth / (precanvas.clientWidth * zoom)) -
        (precanvas.getBoundingClientRect().x * (precanvas.clientWidth / (precanvas.clientWidth * zoom)));
    var y = eY * (precanvas.clientHeight / (precanvas.clientHeight * zoom)) -
        (precanvas.getBoundingClientRect().y * (precanvas.clientHeight / (precanvas.clientHeight * zoom)));
    var curvature = context.curvature;
    var lineCurve = context.ele_selected.classList.contains('input')
        ? createCurvature(context, x, y, line_x, line_y, curvature, 'close')
        : createCurvature(context, line_x, line_y, x, y, curvature, 'openclose');
    path.setAttributeNS(null, 'd', lineCurve);
}
function addConnection(context, id_output, id_input, output_class, input_class, options) {
    if (options === void 0) { options = {}; }
    var nodeOneModule = context.getModuleFromNodeId(id_output);
    var nodeTwoModule = context.getModuleFromNodeId(id_input);
    if (nodeOneModule !== nodeTwoModule) {
        return;
    }
    var dataNode = context.getNodeFromId(id_output);
    if (!dataNode) {
        return;
    }
    var exist = false;
    for (var _i = 0, _a = dataNode.outputs[output_class].connections; _i < _a.length; _i++) {
        var connectionSearch = _a[_i];
        if (connectionSearch.node === id_input && connectionSearch.output === input_class) {
            exist = true;
            break;
        }
    }
    if (!exist) {
        var targetNode = context.getNodeFromId(id_input);
        if (targetNode) {
            for (var _b = 0, _c = targetNode.inputs[input_class].connections; _b < _c.length; _b++) {
                var connectionSearch = _c[_b];
                if (connectionSearch.node === id_output && connectionSearch.input === output_class) {
                    exist = true;
                    break;
                }
            }
        }
    }
    if (exist) {
        return;
    }
    var normalizedSignal = typeof options.signal === 'string' && options.signal.trim() !== '' ? options.signal.trim() : undefined;
    var outputConnection = {
        node: id_input.toString(),
        output: input_class,
    };
    if (normalizedSignal) {
        outputConnection.signal = normalizedSignal;
    }
    context.drawflow.drawflow[nodeOneModule].data[id_output].outputs[output_class].connections.push(outputConnection);
    var inputConnection = {
        node: id_output.toString(),
        input: output_class,
    };
    if (normalizedSignal) {
        inputConnection.signal = normalizedSignal;
    }
    context.drawflow.drawflow[nodeOneModule].data[id_input].inputs[input_class].connections.push(inputConnection);
    var shouldSkipDom = Boolean(options.skipDom);
    if (normalizedSignal) {
        setPortWirelessName(context, { nodeId: id_output, portClass: output_class, type: 'output' }, normalizedSignal);
        setPortWirelessName(context, { nodeId: id_input, portClass: input_class, type: 'input' }, normalizedSignal);
    }
    if (!shouldSkipDom && context.module === nodeOneModule) {
        var connection = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
        var path = document.createElementNS('http://www.w3.org/2000/svg', 'path');
        path.classList.add('main-path');
        path.setAttributeNS(null, 'd', '');
        connection.classList.add('connection');
        connection.classList.add("node_in_node-".concat(id_input));
        connection.classList.add("node_out_node-".concat(id_output));
        connection.classList.add(output_class);
        connection.classList.add(input_class);
        connection.appendChild(path);
        context.precanvas.appendChild(connection);
        context.updateConnectionNodes("node-".concat(id_output));
        context.updateConnectionNodes("node-".concat(id_input));
    }
    context.dispatch('connectionCreated', __assign({ output_id: id_output, input_id: id_input, output_class: output_class, input_class: input_class }, (normalizedSignal ? { signal: normalizedSignal } : {})));
}
function updateConnectionNodes(context, id) {
    var idSearch = "node_in_".concat(id);
    var idSearchOut = "node_out_".concat(id);
    var container = context.container, precanvas = context.precanvas;
    var curvature = context.curvature;
    var reroute_curvature = context.reroute_curvature;
    var reroute_curvature_start_end = context.reroute_curvature_start_end;
    var reroute_fix_curvature = context.reroute_fix_curvature;
    var rerouteWidth = context.reroute_width;
    var zoom = context.zoom;
    var precanvasWidthZoom = precanvas.clientWidth / (precanvas.clientWidth * zoom) || 0;
    var precanvasHeightZoom = precanvas.clientHeight / (precanvas.clientHeight * zoom) || 0;
    var elemsOut = container.querySelectorAll(".".concat(idSearchOut));
    Object.keys(elemsOut).forEach(function (item) {
        var element = elemsOut[item];
        if (!element) {
            return;
        }
        var outputPortClass = findClassWithPrefix(element.classList, 'output_');
        var inputPortClass = findClassWithPrefix(element.classList, 'input_');
        var targetNodeDomId = getInputNodeIdFromClassListSafe(element.classList);
        if (!outputPortClass || !inputPortClass || !targetNodeDomId) {
            removeDanglingConnectionElement(context, element);
            return;
        }
        if (element.querySelector('.point') === null) {
            var sourceNodeElement = container.querySelector("#".concat(id));
            var targetNodeElement = container.querySelector("#".concat(targetNodeDomId));
            if (!sourceNodeElement || !targetNodeElement) {
                removeDanglingConnectionElement(context, element);
                return;
            }
            var targetPort = targetNodeElement.querySelector(".".concat(inputPortClass));
            var sourcePort = sourceNodeElement.querySelector(".".concat(outputPortClass));
            if (!targetPort || !sourcePort) {
                removeDanglingConnectionElement(context, element);
                return;
            }
            var eX = targetPort.offsetWidth / 2 + (targetPort.getBoundingClientRect().x - precanvas.getBoundingClientRect().x) * precanvasWidthZoom;
            var eY = targetPort.offsetHeight / 2 + (targetPort.getBoundingClientRect().y - precanvas.getBoundingClientRect().y) * precanvasHeightZoom;
            var line_x = sourcePort.offsetWidth / 2 + (sourcePort.getBoundingClientRect().x - precanvas.getBoundingClientRect().x) * precanvasWidthZoom;
            var line_y = sourcePort.offsetHeight / 2 + (sourcePort.getBoundingClientRect().y - precanvas.getBoundingClientRect().y) * precanvasHeightZoom;
            var lineCurve = createCurvature(context, line_x, line_y, eX, eY, curvature, 'openclose');
            element.children[0].setAttributeNS(null, 'd', lineCurve);
        }
        else {
            var outputNodeDomId = getOutputNodeIdFromClassListSafe(element.classList);
            var inputNodeDomId = targetNodeDomId;
            if (!outputNodeDomId || !inputNodeDomId) {
                removeDanglingConnectionElement(context, element);
                return;
            }
            var updated = updateConnectionWithPoints({
                context: context,
                element: element,
                outputNodeId: outputNodeDomId,
                inputNodeId: inputNodeDomId,
                precanvasWidthZoom: precanvasWidthZoom,
                precanvasHeightZoom: precanvasHeightZoom,
                rerouteWidth: rerouteWidth,
                reroute_curvature: reroute_curvature,
                reroute_curvature_start_end: reroute_curvature_start_end,
                reroute_fix_curvature: reroute_fix_curvature,
            });
            if (!updated) {
                removeDanglingConnectionElement(context, element);
            }
        }
    });
    var elems = container.querySelectorAll(".".concat(idSearch));
    Object.keys(elems).forEach(function (item) {
        var element = elems[item];
        if (!element) {
            return;
        }
        var outputPortClass = findClassWithPrefix(element.classList, 'output_');
        var inputPortClass = findClassWithPrefix(element.classList, 'input_');
        var sourceNodeDomId = getOutputNodeIdFromClassListSafe(element.classList);
        if (!outputPortClass || !inputPortClass || !sourceNodeDomId) {
            removeDanglingConnectionElement(context, element);
            return;
        }
        if (element.querySelector('.point') === null) {
            var targetNodeElement = container.querySelector("#".concat(id));
            var sourceNodeElement = container.querySelector("#".concat(sourceNodeDomId));
            if (!targetNodeElement || !sourceNodeElement) {
                removeDanglingConnectionElement(context, element);
                return;
            }
            var sourcePort = sourceNodeElement.querySelector(".".concat(outputPortClass));
            var targetPort = targetNodeElement.querySelector(".".concat(inputPortClass));
            if (!sourcePort || !targetPort) {
                removeDanglingConnectionElement(context, element);
                return;
            }
            var line_x = sourcePort.offsetWidth / 2 + (sourcePort.getBoundingClientRect().x - precanvas.getBoundingClientRect().x) * precanvasWidthZoom;
            var line_y = sourcePort.offsetHeight / 2 + (sourcePort.getBoundingClientRect().y - precanvas.getBoundingClientRect().y) * precanvasHeightZoom;
            var x = targetPort.offsetWidth / 2 + (targetPort.getBoundingClientRect().x - precanvas.getBoundingClientRect().x) * precanvasWidthZoom;
            var y = targetPort.offsetHeight / 2 + (targetPort.getBoundingClientRect().y - precanvas.getBoundingClientRect().y) * precanvasHeightZoom;
            var lineCurve = createCurvature(context, line_x, line_y, x, y, curvature, 'openclose');
            element.children[0].setAttributeNS(null, 'd', lineCurve);
        }
        else {
            var outputNodeDomId = sourceNodeDomId;
            var inputNodeDomId = getInputNodeIdFromClassListSafe(element.classList);
            if (!outputNodeDomId || !inputNodeDomId) {
                removeDanglingConnectionElement(context, element);
                return;
            }
            var updated = updateConnectionWithPoints({
                context: context,
                element: element,
                outputNodeId: outputNodeDomId,
                inputNodeId: inputNodeDomId,
                precanvasWidthZoom: precanvasWidthZoom,
                precanvasHeightZoom: precanvasHeightZoom,
                rerouteWidth: rerouteWidth,
                reroute_curvature: reroute_curvature,
                reroute_curvature_start_end: reroute_curvature_start_end,
                reroute_fix_curvature: reroute_fix_curvature,
            });
            if (!updated) {
                removeDanglingConnectionElement(context, element);
            }
        }
    });
}
var getOutputNodeIdFromClass = function (className) { return ensureNodeDomId(stripClassPrefix(className, 'node_out_')); };
var getInputNodeIdFromClass = function (className) { return ensureNodeDomId(stripClassPrefix(className, 'node_in_')); };
var getOutputNodeIdFromClassList = function (classList) {
    var className = findClassWithPrefix(classList, 'node_out_');
    if (!className) {
        throw new Error('Connection element is missing a node_out_ class');
    }
    return getOutputNodeIdFromClass(className);
};
var getInputNodeIdFromClassList = function (classList) {
    var className = findClassWithPrefix(classList, 'node_in_');
    if (!className) {
        throw new Error('Connection element is missing a node_in_ class');
    }
    return getInputNodeIdFromClass(className);
};
var getOutputNodeIdFromClassListSafe = function (classList) {
    var className = findClassWithPrefix(classList, 'node_out_');
    return className ? getOutputNodeIdFromClass(className) : undefined;
};
var getInputNodeIdFromClassListSafe = function (classList) {
    var className = findClassWithPrefix(classList, 'node_in_');
    return className ? getInputNodeIdFromClass(className) : undefined;
};
var removeDanglingConnectionElement = function (context, element) {
    var _a, _b, _c, _d, _e, _f;
    var connectionInfo = extractConnectionClassInfo(element.classList);
    if (!connectionInfo) {
        element.remove();
        return;
    }
    var outputNodeId = connectionInfo.outputNodeId, inputNodeId = connectionInfo.inputNodeId, outputPortClass = connectionInfo.outputPortClass, inputPortClass = connectionInfo.inputPortClass;
    var outputModuleName = context.getModuleFromNodeId(outputNodeId);
    var inputModuleName = context.getModuleFromNodeId(inputNodeId);
    var removedFromData = false;
    if (outputModuleName) {
        var outputModule = context.drawflow.drawflow[outputModuleName];
        var outputNode = (_a = outputModule === null || outputModule === void 0 ? void 0 : outputModule.data) === null || _a === void 0 ? void 0 : _a[outputNodeId];
        var outputConnections = (_c = (_b = outputNode === null || outputNode === void 0 ? void 0 : outputNode.outputs) === null || _b === void 0 ? void 0 : _b[outputPortClass]) === null || _c === void 0 ? void 0 : _c.connections;
        if (outputConnections) {
            var index = outputConnections.findIndex(function (connection) {
                return connection.node === inputNodeId && connection.output === inputPortClass;
            });
            if (index > -1) {
                outputConnections.splice(index, 1);
                removedFromData = true;
            }
        }
    }
    if (inputModuleName) {
        var inputModule = context.drawflow.drawflow[inputModuleName];
        var inputNode = (_d = inputModule === null || inputModule === void 0 ? void 0 : inputModule.data) === null || _d === void 0 ? void 0 : _d[inputNodeId];
        var inputConnections = (_f = (_e = inputNode === null || inputNode === void 0 ? void 0 : inputNode.inputs) === null || _e === void 0 ? void 0 : _e[inputPortClass]) === null || _f === void 0 ? void 0 : _f.connections;
        if (inputConnections) {
            var index = inputConnections.findIndex(function (connection) {
                return connection.node === outputNodeId && connection.input === outputPortClass;
            });
            if (index > -1) {
                inputConnections.splice(index, 1);
                removedFromData = true;
            }
        }
    }
    element.remove();
    if (removedFromData) {
        context.dispatch('connectionRemoved', {
            output_id: outputNodeId,
            input_id: inputNodeId,
            output_class: outputPortClass,
            input_class: inputPortClass
        });
    }
};
function updateConnectionWithPoints(args) {
    var context = args.context, element = args.element, outputNodeId = args.outputNodeId, inputNodeId = args.inputNodeId, precanvasWidthZoom = args.precanvasWidthZoom, precanvasHeightZoom = args.precanvasHeightZoom, rerouteWidth = args.rerouteWidth, reroute_curvature = args.reroute_curvature, reroute_curvature_start_end = args.reroute_curvature_start_end, reroute_fix_curvature = args.reroute_fix_curvature;
    var points = element.querySelectorAll('.point');
    var normalizedOutputNodeId = ensureNodeDomId(outputNodeId);
    var normalizedInputNodeId = ensureNodeDomId(inputNodeId);
    var reroutePath = buildReroutePath({
        context: context,
        element: element,
        points: points,
        precanvasWidthZoom: precanvasWidthZoom,
        precanvasHeightZoom: precanvasHeightZoom,
        rerouteWidth: rerouteWidth,
        reroute_curvature: reroute_curvature,
        reroute_curvature_start_end: reroute_curvature_start_end,
        outputNodeId: normalizedOutputNodeId,
        inputNodeId: normalizedInputNodeId
    });
    if (!reroutePath) {
        return false;
    }
    if (reroute_fix_curvature) {
        reroutePath.segmentPaths.forEach(function (segmentPath, index) {
            var pathElement = element.children[index];
            if (pathElement) {
                pathElement.setAttributeNS(null, 'd', segmentPath);
            }
        });
    }
    else {
        element.children[0].setAttributeNS(null, 'd', reroutePath.fullPath);
    }
    return true;
}
function buildReroutePath(args) {
    var context = args.context, element = args.element, points = args.points, precanvasWidthZoom = args.precanvasWidthZoom, precanvasHeightZoom = args.precanvasHeightZoom, rerouteWidth = args.rerouteWidth, reroute_curvature = args.reroute_curvature, reroute_curvature_start_end = args.reroute_curvature_start_end, outputNodeId = args.outputNodeId, inputNodeId = args.inputNodeId;
    var precanvas = context.precanvas;
    if (!precanvas) {
        return null;
    }
    var precanvasRect = precanvas.getBoundingClientRect();
    var outputNode = context.container.querySelector("#".concat(outputNodeId));
    var inputNode = context.container.querySelector("#".concat(inputNodeId));
    if (!outputNode || !inputNode) {
        return null;
    }
    var outputPortClass = findClassWithPrefix(element.classList, 'output_');
    var inputPortClass = findClassWithPrefix(element.classList, 'input_');
    if (!outputPortClass || !inputPortClass) {
        return null;
    }
    var outputPort = outputNode.querySelector(".".concat(outputPortClass));
    var inputPort = inputNode.querySelector(".".concat(inputPortClass));
    if (!outputPort || !inputPort) {
        return null;
    }
    var pathPoints = [];
    pathPoints.push(getPortCenter(outputPort, precanvasRect, precanvasWidthZoom, precanvasHeightZoom));
    points.forEach(function (point) {
        pathPoints.push(getReroutePointCenter(point, precanvasRect, precanvasWidthZoom, precanvasHeightZoom, rerouteWidth));
    });
    pathPoints.push(getPortCenter(inputPort, precanvasRect, precanvasWidthZoom, precanvasHeightZoom));
    if (pathPoints.length < 2) {
        return null;
    }
    var _a = createSmoothPath(pathPoints, reroute_curvature_start_end, reroute_curvature), fullPath = _a.fullPath, segmentPaths = _a.segmentPaths;
    return { fullPath: fullPath, segmentPaths: segmentPaths };
}
function getPortCenter(port, precanvasRect, precanvasWidthZoom, precanvasHeightZoom) {
    var portRect = port.getBoundingClientRect();
    return {
        x: port.offsetWidth / 2 + (portRect.x - precanvasRect.x) * precanvasWidthZoom,
        y: port.offsetHeight / 2 + (portRect.y - precanvasRect.y) * precanvasHeightZoom,
    };
}
function getReroutePointCenter(point, precanvasRect, precanvasWidthZoom, precanvasHeightZoom, rerouteWidth) {
    var pointRect = point.getBoundingClientRect();
    return {
        x: (pointRect.x - precanvasRect.x) * precanvasWidthZoom + rerouteWidth,
        y: (pointRect.y - precanvasRect.y) * precanvasHeightZoom + rerouteWidth,
    };
}
function createSmoothPath(points, startEndCurvature, middleCurvature) {
    if (points.length === 0) {
        return { fullPath: '', segmentPaths: [] };
    }
    var fullPath = " M ".concat(points[0].x, " ").concat(points[0].y);
    var segmentPaths = [];
    for (var index = 0; index < points.length - 1; index += 1) {
        var start = points[index];
        var end = points[index + 1];
        var curvature = (index === 0 || index === points.length - 2) ? startEndCurvature : middleCurvature;
        var _a = computeControlPoints(points, index, curvature), cp1 = _a.cp1, cp2 = _a.cp2;
        fullPath += " C ".concat(cp1.x, " ").concat(cp1.y, " ").concat(cp2.x, " ").concat(cp2.y, " ").concat(end.x, " ").concat(end.y);
        segmentPaths.push(" M ".concat(start.x, " ").concat(start.y, " C ").concat(cp1.x, " ").concat(cp1.y, " ").concat(cp2.x, " ").concat(cp2.y, " ").concat(end.x, " ").concat(end.y));
    }
    return { fullPath: fullPath, segmentPaths: segmentPaths };
}
function computeControlPoints(points, index, curvature) {
    var _a, _b;
    var p0 = (_a = points[index - 1]) !== null && _a !== void 0 ? _a : points[index];
    var p1 = points[index];
    var p2 = points[index + 1];
    var p3 = (_b = points[index + 2]) !== null && _b !== void 0 ? _b : points[index + 1];
    var tangentScale = curvature * 0.75;
    var tangent1 = scalePoint({ x: p2.x - p0.x, y: p2.y - p0.y }, tangentScale);
    var tangent2 = scalePoint({ x: p3.x - p1.x, y: p3.y - p1.y }, tangentScale);
    var cp1 = { x: p1.x + tangent1.x / 3, y: p1.y + tangent1.y / 3 };
    var cp2 = { x: p2.x - tangent2.x / 3, y: p2.y - tangent2.y / 3 };
    return { cp1: cp1, cp2: cp2 };
}
function scalePoint(point, scale) {
    return { x: point.x * scale, y: point.y * scale };
}
function dblclick(context, e) {
    if (context.connection_selected && context.reroute) {
        context.createReroutePoint(context.connection_selected);
    }
    var target = e.target;
    if (target && target.classList.contains('point')) {
        context.removeReroutePoint(target);
    }
}
function createReroutePoint(context, ele) {
    var _a;
    var selectedConnection = context.connection_selected;
    if (!selectedConnection) {
        return;
    }
    selectedConnection.classList.remove('selected');
    var connectionElement = selectedConnection.parentElement;
    if (!connectionElement) {
        context.connection_selected = null;
        return;
    }
    var connectionInfo = extractConnectionClassInfo(connectionElement.classList);
    if (!connectionInfo) {
        removeDanglingConnectionElement(context, connectionElement);
        context.connection_selected = null;
        return;
    }
    var outputNodeDomId = connectionInfo.outputNodeDomId, inputNodeDomId = connectionInfo.inputNodeDomId, outputNodeId = connectionInfo.outputNodeId, inputNodeId = connectionInfo.inputNodeId, outputPortClass = connectionInfo.outputPortClass, inputPortClass = connectionInfo.inputPortClass;
    context.connection_selected = null;
    var point = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
    point.classList.add('point');
    var pos_x = context.pos_x * (context.precanvas.clientWidth / (context.precanvas.clientWidth * context.zoom)) -
        (context.precanvas.getBoundingClientRect().x * (context.precanvas.clientWidth / (context.precanvas.clientWidth * context.zoom)));
    var pos_y = context.pos_y * (context.precanvas.clientHeight / (context.precanvas.clientHeight * context.zoom)) -
        (context.precanvas.getBoundingClientRect().y * (context.precanvas.clientHeight / (context.precanvas.clientHeight * context.zoom)));
    point.setAttributeNS(null, 'cx', pos_x.toString());
    point.setAttributeNS(null, 'cy', pos_y.toString());
    point.setAttributeNS(null, 'r', context.reroute_width.toString());
    var nodeId = outputNodeId;
    var searchConnection = context.drawflow.drawflow[context.module].data[nodeId].outputs[outputPortClass].connections.findIndex(function (item) {
        return item.node === inputNodeId && item.output === inputPortClass;
    });
    var connection = context.drawflow.drawflow[context.module].data[nodeId].outputs[outputPortClass].connections[searchConnection];
    if (!connection.points) {
        connection.points = [];
    }
    var insertIndex = connection.points.length;
    if (context.reroute_fix_curvature) {
        var numberPoints = connectionElement.querySelectorAll('.main-path').length;
        var path = document.createElementNS('http://www.w3.org/2000/svg', 'path');
        path.classList.add('main-path');
        path.setAttributeNS(null, 'd', '');
        connectionElement.insertBefore(path, connectionElement.children[numberPoints]);
        if (numberPoints === 1) {
            insertIndex = connection.points.length;
            connectionElement.appendChild(point);
        }
        else {
            var search_point = Array.from(connectionElement.children).indexOf(ele);
            insertIndex = Math.min(search_point, connection.points.length);
            connectionElement.insertBefore(point, connectionElement.children[search_point + numberPoints + 1]);
        }
    }
    else {
        var calculateInsertIndex = function () {
            var _a, _b;
            var precanvas = context.precanvas;
            if (!precanvas) {
                return connection.points.length;
            }
            var outputNodeElement = context.container.querySelector("#".concat(outputNodeDomId));
            var inputNodeElement = context.container.querySelector("#".concat(inputNodeDomId));
            var outputElement = (_a = outputNodeElement === null || outputNodeElement === void 0 ? void 0 : outputNodeElement.querySelector(".".concat(outputPortClass))) !== null && _a !== void 0 ? _a : null;
            var inputElement = (_b = inputNodeElement === null || inputNodeElement === void 0 ? void 0 : inputNodeElement.querySelector(".".concat(inputPortClass))) !== null && _b !== void 0 ? _b : null;
            if (!outputElement || !inputElement) {
                return connection.points.length;
            }
            var zoom = context.zoom;
            var precanvasRect = precanvas.getBoundingClientRect();
            var precanvasWidthZoom = precanvas.clientWidth / (precanvas.clientWidth * zoom) || 0;
            var precanvasHeightZoom = precanvas.clientHeight / (precanvas.clientHeight * zoom) || 0;
            var toCanvasX = function (element) {
                return element.offsetWidth / 2 + (element.getBoundingClientRect().x - precanvasRect.x) * precanvasWidthZoom;
            };
            var toCanvasY = function (element) {
                return element.offsetHeight / 2 + (element.getBoundingClientRect().y - precanvasRect.y) * precanvasHeightZoom;
            };
            var startPoint = { x: toCanvasX(outputElement), y: toCanvasY(outputElement) };
            var endPoint = { x: toCanvasX(inputElement), y: toCanvasY(inputElement) };
            var existingPoints = connection.points.map(function (pt) { return ({ x: pt.pos_x, y: pt.pos_y }); });
            var routePoints = __spreadArray(__spreadArray([startPoint], existingPoints, true), [endPoint], false);
            var newPoint = { x: pos_x, y: pos_y };
            var bestIndex = connection.points.length;
            var bestDistanceSq = Number.POSITIVE_INFINITY;
            var bestAlongDistance = Number.POSITIVE_INFINITY;
            var accumulatedDistance = 0;
            var EPSILON = 1e-6;
            for (var i = 0; i < routePoints.length - 1; i += 1) {
                var segmentStart = routePoints[i];
                var segmentEnd = routePoints[i + 1];
                var dx = segmentEnd.x - segmentStart.x;
                var dy = segmentEnd.y - segmentStart.y;
                var lengthSq = dx * dx + dy * dy;
                var t = 0;
                if (lengthSq > 0) {
                    t = ((newPoint.x - segmentStart.x) * dx + (newPoint.y - segmentStart.y) * dy) / lengthSq;
                    t = Math.max(0, Math.min(1, t));
                }
                var projX = segmentStart.x + dx * t;
                var projY = segmentStart.y + dy * t;
                var distSq = Math.pow((newPoint.x - projX), 2) + Math.pow((newPoint.y - projY), 2);
                var alongDistance = accumulatedDistance + Math.hypot(projX - segmentStart.x, projY - segmentStart.y);
                if ((distSq + EPSILON) < bestDistanceSq ||
                    (Math.abs(distSq - bestDistanceSq) <= EPSILON && alongDistance < bestAlongDistance)) {
                    bestDistanceSq = distSq;
                    bestAlongDistance = alongDistance;
                    bestIndex = Math.min(i, connection.points.length);
                }
                accumulatedDistance += Math.hypot(dx, dy);
            }
            return bestIndex;
        };
        insertIndex = calculateInsertIndex();
        var domPoints = Array.from(connectionElement.querySelectorAll('.point'));
        var referencePoint = (_a = domPoints[insertIndex]) !== null && _a !== void 0 ? _a : null;
        if (referencePoint) {
            connectionElement.insertBefore(point, referencePoint);
        }
        else {
            connectionElement.appendChild(point);
        }
    }
    connection.points.splice(insertIndex, 0, { pos_x: pos_x, pos_y: pos_y });
    if (context.reroute_fix_curvature) {
        connectionElement.querySelectorAll('.main-path').forEach(function (item) {
            item.classList.remove('selected');
        });
    }
    context.dispatch('addReroute', nodeId);
    context.updateConnectionNodes(outputNodeDomId);
}
function removeReroutePoint(context, ele) {
    var connectionElement = ele.parentElement;
    if (!connectionElement) {
        return;
    }
    var connectionInfo = extractConnectionClassInfo(connectionElement.classList);
    if (!connectionInfo) {
        removeDanglingConnectionElement(context, connectionElement);
        return;
    }
    var outputNodeDomId = connectionInfo.outputNodeDomId, outputNodeId = connectionInfo.outputNodeId, inputNodeId = connectionInfo.inputNodeId, outputPortClass = connectionInfo.outputPortClass, inputPortClass = connectionInfo.inputPortClass;
    var numberPointPosition = Array.from(connectionElement.children).indexOf(ele);
    var nodeId = outputNodeId;
    var searchConnection = context.drawflow.drawflow[context.module].data[nodeId].outputs[outputPortClass].connections.findIndex(function (item) {
        return item.node === inputNodeId && item.output === inputPortClass;
    });
    if (context.reroute_fix_curvature) {
        var numberMainPath = connectionElement.querySelectorAll('.main-path').length;
        connectionElement.children[numberMainPath - 1].remove();
        numberPointPosition -= numberMainPath;
        if (numberPointPosition < 0) {
            numberPointPosition = 0;
        }
    }
    else {
        numberPointPosition -= 1;
    }
    context.drawflow.drawflow[context.module].data[nodeId].outputs[outputPortClass].connections[searchConnection].points.splice(numberPointPosition, 1);
    ele.remove();
    context.dispatch('removeReroute', nodeId);
    context.updateConnectionNodes(outputNodeDomId);
}
function addRerouteImport(context, dataNode) {
    var reroute_width = context.reroute_width;
    var reroute_fix_curvature = context.reroute_fix_curvature;
    var container = context.container;
    Object.keys(dataNode.outputs).forEach(function (output_item) {
        Object.keys(dataNode.outputs[output_item].connections).forEach(function (input_item) {
            var points = dataNode.outputs[output_item].connections[input_item].points;
            if (!points) {
                return;
            }
            points.forEach(function (item, i) {
                var input_id = dataNode.outputs[output_item].connections[input_item].node;
                var input_class = dataNode.outputs[output_item].connections[input_item].output;
                var ele = container.querySelector(".connection.node_in_node-".concat(input_id, ".node_out_node-").concat(dataNode.id, ".").concat(output_item, ".").concat(input_class));
                if (!ele) {
                    return;
                }
                if (reroute_fix_curvature && i === 0) {
                    for (var z = 0; z < points.length; z += 1) {
                        var path = document.createElementNS('http://www.w3.org/2000/svg', 'path');
                        path.classList.add('main-path');
                        path.setAttributeNS(null, 'd', '');
                        ele.appendChild(path);
                    }
                }
                var point = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
                point.classList.add('point');
                point.setAttributeNS(null, 'cx', item.pos_x.toString());
                point.setAttributeNS(null, 'cy', item.pos_y.toString());
                point.setAttributeNS(null, 'r', reroute_width.toString());
                ele.appendChild(point);
            });
        });
    });
}
function removeConnection(context) {
    var _a, _b, _c, _d, _e, _f;
    var selectedConnectionPath = context.connection_selected;
    if (!selectedConnectionPath) {
        return;
    }
    var connectionElement = selectedConnectionPath.parentElement;
    if (!connectionElement) {
        context.connection_selected = null;
        return;
    }
    var connectionInfo = extractConnectionClassInfo(connectionElement.classList);
    connectionElement.remove();
    context.connection_selected = null;
    if (!connectionInfo) {
        return;
    }
    var outputNodeId = connectionInfo.outputNodeId, inputNodeId = connectionInfo.inputNodeId, outputPortClass = connectionInfo.outputPortClass, inputPortClass = connectionInfo.inputPortClass;
    var moduleData = context.drawflow.drawflow[context.module].data;
    var removed = false;
    var outputConnections = (_c = (_b = (_a = moduleData[outputNodeId]) === null || _a === void 0 ? void 0 : _a.outputs) === null || _b === void 0 ? void 0 : _b[outputPortClass]) === null || _c === void 0 ? void 0 : _c.connections;
    if (outputConnections) {
        var indexOut = outputConnections.findIndex(function (item) { return item.node === inputNodeId && item.output === inputPortClass; });
        if (indexOut > -1) {
            outputConnections.splice(indexOut, 1);
            removed = true;
        }
    }
    var inputConnections = (_f = (_e = (_d = moduleData[inputNodeId]) === null || _d === void 0 ? void 0 : _d.inputs) === null || _e === void 0 ? void 0 : _e[inputPortClass]) === null || _f === void 0 ? void 0 : _f.connections;
    if (inputConnections) {
        var indexIn = inputConnections.findIndex(function (item) { return item.node === outputNodeId && item.input === outputPortClass; });
        if (indexIn > -1) {
            inputConnections.splice(indexIn, 1);
            removed = true;
        }
    }
    if (removed) {
        context.dispatch('connectionRemoved', {
            output_id: outputNodeId,
            input_id: inputNodeId,
            output_class: outputPortClass,
            input_class: inputPortClass,
        });
    }
}
function removeSingleConnection(context, id_output, id_input, output_class, input_class) {
    var _a;
    var nodeOneModule = context.getModuleFromNodeId(id_output);
    var nodeTwoModule = context.getModuleFromNodeId(id_input);
    if (nodeOneModule !== nodeTwoModule) {
        return false;
    }
    var exists = context.drawflow.drawflow[nodeOneModule].data[id_output].outputs[output_class].connections.findIndex(function (item) {
        return item.node === id_input && item.output === input_class;
    });
    if (exists <= -1) {
        return false;
    }
    if (context.module === nodeOneModule) {
        (_a = context.container.querySelector(".connection.node_in_node-".concat(id_input, ".node_out_node-").concat(id_output, ".").concat(output_class, ".").concat(input_class))) === null || _a === void 0 ? void 0 : _a.remove();
    }
    var index_out = context.drawflow.drawflow[nodeOneModule].data[id_output].outputs[output_class].connections.findIndex(function (item) {
        return item.node === id_input && item.output === input_class;
    });
    context.drawflow.drawflow[nodeOneModule].data[id_output].outputs[output_class].connections.splice(index_out, 1);
    var index_in = context.drawflow.drawflow[nodeOneModule].data[id_input].inputs[input_class].connections.findIndex(function (item) {
        return item.node === id_output && item.input === output_class;
    });
    context.drawflow.drawflow[nodeOneModule].data[id_input].inputs[input_class].connections.splice(index_in, 1);
    context.dispatch('connectionRemoved', { output_id: id_output, input_id: id_input, output_class: output_class, input_class: input_class });
    var outputRef = { nodeId: id_output, portClass: output_class, type: 'output' };
    var inputRef = { nodeId: id_input, portClass: input_class, type: 'input' };
    setPortWirelessName(context, outputRef, getPortWirelessName(context, outputRef));
    setPortWirelessName(context, inputRef, getPortWirelessName(context, inputRef));
    return true;
}
function removeConnectionNodeId(context, id) {
    var targetNodeDomId = ensureNodeDomId(id);
    var connections = Array.from(context.container.querySelectorAll('.connection'));
    connections.forEach(function (element) {
        var info = extractConnectionClassInfo(element.classList);
        if (!info) {
            element.remove();
            return;
        }
        if (info.outputNodeDomId === targetNodeDomId || info.inputNodeDomId === targetNodeDomId) {
            removeDanglingConnectionElement(context, element);
        }
    });
    removeWirelessConnectionsForNode(context, id);
}
function removeWirelessConnectionsForNode(context, nodeId) {
    var _a;
    var moduleName = context.getModuleFromNodeId(nodeId);
    if (!moduleName) {
        return;
    }
    var moduleData = (_a = context.drawflow.drawflow[moduleName]) === null || _a === void 0 ? void 0 : _a.data;
    if (!moduleData) {
        return;
    }
    var removals = new Map();
    Object.entries(moduleData).forEach(function (_a) {
        var currentNodeId = _a[0], nodeData = _a[1];
        Object.entries(nodeData.outputs).forEach(function (_a) {
            var outputClass = _a[0], portData = _a[1];
            portData.connections.forEach(function (connection) {
                if (!connection.signal || connection.signal.trim() === '') {
                    return;
                }
                if (currentNodeId !== nodeId && connection.node !== nodeId) {
                    return;
                }
                var removalKey = "".concat(currentNodeId, "->").concat(connection.node, ":").concat(outputClass, ":").concat(connection.output);
                if (!removals.has(removalKey)) {
                    removals.set(removalKey, {
                        outputNode: currentNodeId,
                        inputNode: connection.node,
                        outputClass: outputClass,
                        inputClass: connection.output,
                    });
                }
            });
        });
    });
    removals.forEach(function (removal) {
        context.removeSingleConnection(removal.outputNode, removal.inputNode, removal.outputClass, removal.inputClass);
    });
}

// CONCATENATED MODULE: ./src/core/nodes.ts
var nodes_assign = (undefined && undefined.__assign) || function () {
    nodes_assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return nodes_assign.apply(this, arguments);
};


function registerNode(context, name, html, props, options) {
    if (props === void 0) { props = null; }
    if (options === void 0) { options = null; }
    context.noderegister[name] = { html: html, props: props, options: options };
}
function getNodeFromId(context, id) {
    var moduleName = context.getModuleFromNodeId(id);
    if (moduleName && context.drawflow.drawflow[moduleName] && context.drawflow.drawflow[moduleName].data[id]) {
        return JSON.parse(JSON.stringify(context.drawflow.drawflow[moduleName].data[id]));
    }
    return null;
}
function getNodesFromName(context, name) {
    var nodes = [];
    var editor = context.drawflow.drawflow;
    Object.keys(editor).forEach(function (moduleName) {
        Object.keys(editor[moduleName].data).forEach(function (node) {
            if (editor[moduleName].data[node].name === name) {
                nodes.push(editor[moduleName].data[node].id.toString());
            }
        });
    });
    return nodes;
}
function addNode(context, name, num_in, num_out, ele_pos_x, ele_pos_y, classoverride, data, html, typenode) {
    var _a;
    if (typenode === void 0) { typenode = false; }
    var newNodeId = context.useuuid ? context.getUuid() : context.nodeId;
    var parent = document.createElement('div');
    parent.classList.add('parent-node');
    var node = document.createElement('div');
    node.innerHTML = '';
    node.setAttribute('id', "node-".concat(newNodeId));
    node.classList.add('drawflow-node');
    if (classoverride !== '') {
        (_a = node.classList).add.apply(_a, classoverride.split(' '));
    }
    var inputs = document.createElement('div');
    inputs.classList.add('inputs');
    var outputs = document.createElement('div');
    outputs.classList.add('outputs');
    var json_inputs = {};
    for (var x = 0; x < num_in; x += 1) {
        var input = document.createElement('div');
        input.classList.add('input');
        input.classList.add("input_".concat(x + 1));
        json_inputs["input_".concat(x + 1)] = { connections: [] };
        inputs.appendChild(input);
    }
    var json_outputs = {};
    for (var x = 0; x < num_out; x += 1) {
        var output = document.createElement('div');
        output.classList.add('output');
        output.classList.add("output_".concat(x + 1));
        json_outputs["output_".concat(x + 1)] = { connections: [] };
        outputs.appendChild(output);
    }
    var content = document.createElement('div');
    content.classList.add('drawflow_content_node');
    if (typenode === false) {
        content.innerHTML = html;
    }
    else if (typenode === true) {
        content.appendChild(context.noderegister[html].html.cloneNode(true));
    }
    else if (context.render) {
        if (parseInt(context.render.version, 10) === 3) {
            var wrapper = context.render.h(context.noderegister[html].html, context.noderegister[html].props, context.noderegister[html].options);
            wrapper.appContext = context.parent;
            context.render.render(wrapper, content);
        }
        else {
            var wrapper = new context.render(nodes_assign({ parent: context.parent, render: function (h) { return h(context.noderegister[html].html, { props: context.noderegister[html].props }); } }, context.noderegister[html].options)).$mount();
            content.appendChild(wrapper.$el);
        }
    }
    applyDataBindings(content, data);
    node.appendChild(inputs);
    node.appendChild(content);
    node.appendChild(outputs);
    node.style.top = "".concat(ele_pos_y, "px");
    node.style.left = "".concat(ele_pos_x, "px");
    parent.appendChild(node);
    context.precanvas.appendChild(parent);
    var json = {
        id: newNodeId,
        name: name,
        data: data,
        class: classoverride,
        html: html,
        typenode: typenode,
        inputs: json_inputs,
        outputs: json_outputs,
        pos_x: ele_pos_x,
        pos_y: ele_pos_y
    };
    context.drawflow.drawflow[context.module].data[String(newNodeId)] = json;
    context.dispatch('nodeCreated', newNodeId);
    if (!context.useuuid) {
        context.nodeId += 1;
    }
    return newNodeId;
}
function addNodeImport(context, dataNode, precanvas) {
    var _a;
    var parent = document.createElement('div');
    parent.classList.add('parent-node');
    var node = document.createElement('div');
    node.innerHTML = '';
    node.setAttribute('id', "node-".concat(dataNode.id));
    node.classList.add('drawflow-node');
    if (dataNode.class !== '') {
        (_a = node.classList).add.apply(_a, dataNode.class.split(' '));
    }
    var inputs = document.createElement('div');
    inputs.classList.add('inputs');
    var outputs = document.createElement('div');
    outputs.classList.add('outputs');
    Object.keys(dataNode.inputs).forEach(function (input_item) {
        var input = document.createElement('div');
        input.classList.add('input');
        input.classList.add(input_item);
        inputs.appendChild(input);
        var inputPortData = dataNode.inputs[input_item];
        Object.keys(inputPortData.connections).forEach(function (output_item) {
            var connectionData = inputPortData.connections[output_item];
            if (!connectionData) {
                return;
            }
            if (typeof connectionData.signal === 'string' && connectionData.signal.trim() !== '') {
                return;
            }
            var connection = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
            var path = document.createElementNS('http://www.w3.org/2000/svg', 'path');
            path.classList.add('main-path');
            path.setAttributeNS(null, 'd', '');
            connection.classList.add('connection');
            connection.classList.add("node_in_node-".concat(dataNode.id));
            connection.classList.add("node_out_node-".concat(connectionData.node));
            connection.classList.add(connectionData.input);
            connection.classList.add(input_item);
            connection.appendChild(path);
            precanvas.appendChild(connection);
        });
    });
    Object.keys(dataNode.outputs).forEach(function (output_item) {
        var output = document.createElement('div');
        output.classList.add('output');
        output.classList.add(output_item);
        outputs.appendChild(output);
        var outputPortData = dataNode.outputs[output_item];
    });
    var content = document.createElement('div');
    content.classList.add('drawflow_content_node');
    if (dataNode.typenode === false) {
        content.innerHTML = dataNode.html;
    }
    else if (dataNode.typenode === true) {
        content.appendChild(context.noderegister[dataNode.html].html.cloneNode(true));
    }
    else if (context.render) {
        if (parseInt(context.render.version, 10) === 3) {
            var wrapper = context.render.h(context.noderegister[dataNode.html].html, context.noderegister[dataNode.html].props, context.noderegister[dataNode.html].options);
            wrapper.appContext = context.parent;
            context.render.render(wrapper, content);
        }
        else {
            var wrapper = new context.render(nodes_assign({ parent: context.parent, render: function (h) { return h(context.noderegister[dataNode.html].html, { props: context.noderegister[dataNode.html].props }); } }, context.noderegister[dataNode.html].options)).$mount();
            content.appendChild(wrapper.$el);
        }
    }
    applyDataBindings(content, dataNode.data);
    node.appendChild(inputs);
    node.appendChild(content);
    node.appendChild(outputs);
    node.style.top = "".concat(dataNode.pos_y, "px");
    node.style.left = "".concat(dataNode.pos_x, "px");
    parent.appendChild(node);
    context.precanvas.appendChild(parent);
    Object.entries(dataNode.inputs).forEach(function (_a) {
        var inputClass = _a[0], portData = _a[1];
        var existingSignal = portData.connections.find(function (connection) { return typeof connection.signal === 'string' && connection.signal.trim() !== ''; });
        var name = typeof (existingSignal === null || existingSignal === void 0 ? void 0 : existingSignal.signal) === 'string' && existingSignal.signal.trim() !== '' ? existingSignal.signal.trim() : null;
        if (name) {
            setPortWirelessName(context, { nodeId: dataNode.id.toString(), portClass: inputClass, type: 'input' }, name);
        }
    });
    Object.entries(dataNode.outputs).forEach(function (_a) {
        var outputClass = _a[0], portData = _a[1];
        var existingSignal = portData.connections.find(function (connection) { return typeof connection.signal === 'string' && connection.signal.trim() !== ''; });
        var name = typeof (existingSignal === null || existingSignal === void 0 ? void 0 : existingSignal.signal) === 'string' && existingSignal.signal.trim() !== '' ? existingSignal.signal.trim() : null;
        if (name) {
            setPortWirelessName(context, { nodeId: dataNode.id.toString(), portClass: outputClass, type: 'output' }, name);
        }
    });
}
function applyDataBindings(content, data) {
    var insertObjectkeys = function (object, name, completname) {
        var current = object === null ? data[name] : object[name];
        if (current !== null && current !== undefined) {
            Object.entries(current).forEach(function (_a) {
                var key = _a[0], value = _a[1];
                if (typeof value === 'object') {
                    insertObjectkeys(current, key, "".concat(completname, "-").concat(key));
                }
                else {
                    var elems = content.querySelectorAll("[df-".concat(completname, "-").concat(key, "]"));
                    elems.forEach(function (elem) {
                        elem.value = value;
                        if (elem.isContentEditable) {
                            elem.innerText = value;
                        }
                    });
                }
            });
        }
    };
    Object.entries(data).forEach(function (_a) {
        var key = _a[0], value = _a[1];
        if (typeof value === 'object') {
            insertObjectkeys(null, key, key);
        }
        else {
            var elems = content.querySelectorAll("[df-".concat(key, "]"));
            elems.forEach(function (elem) {
                elem.value = value;
                if (elem.isContentEditable) {
                    elem.innerText = value;
                }
            });
        }
    });
}
function updateNodeValue(context, event) {
    var target = event.target;
    if (!target) {
        return;
    }
    var attr = target.attributes;
    for (var i = 0; i < attr.length; i += 1) {
        if (attr[i].nodeName.startsWith('df-')) {
            var keys = attr[i].nodeName.slice(3).split('-');
            var nodeId = target.closest('.drawflow_content_node').parentElement.id.slice(5);
            var dataTarget = context.drawflow.drawflow[context.module].data[nodeId].data;
            for (var index = 0; index < keys.length - 1; index += 1) {
                if (dataTarget[keys[index]] == null) {
                    dataTarget[keys[index]] = {};
                }
                dataTarget = dataTarget[keys[index]];
            }
            var lastKey = keys[keys.length - 1];
            dataTarget[lastKey] = target.isContentEditable ? target.innerText : target.value;
            context.dispatch('nodeDataChanged', nodeId);
        }
    }
}
function updateNodeDataFromId(context, id, data) {
    var moduleName = context.getModuleFromNodeId(id);
    context.drawflow.drawflow[moduleName].data[id].data = data;
    if (context.module === moduleName) {
        var content = context.container.querySelector("#node-".concat(id));
        applyDataBindings(content.querySelector('.drawflow_content_node'), data);
    }
}
function addNodeInput(context, id) {
    var moduleName = context.getModuleFromNodeId(id);
    var infoNode = context.getNodeFromId(id);
    var numInputs = Object.keys(infoNode.inputs).length;
    if (context.module === moduleName) {
        var input = document.createElement('div');
        input.classList.add('input');
        input.classList.add("input_".concat(numInputs + 1));
        var parent_1 = context.container.querySelector("#node-".concat(id, " .inputs"));
        parent_1.appendChild(input);
        context.updateConnectionNodes("node-".concat(id));
    }
    context.drawflow.drawflow[moduleName].data[id].inputs["input_".concat(numInputs + 1)] = { connections: [] };
}
function addNodeOutput(context, id) {
    var moduleName = context.getModuleFromNodeId(id);
    var infoNode = context.getNodeFromId(id);
    var numOutputs = Object.keys(infoNode.outputs).length;
    if (context.module === moduleName) {
        var output = document.createElement('div');
        output.classList.add('output');
        output.classList.add("output_".concat(numOutputs + 1));
        var parent_2 = context.container.querySelector("#node-".concat(id, " .outputs"));
        parent_2.appendChild(output);
        context.updateConnectionNodes("node-".concat(id));
    }
    context.drawflow.drawflow[moduleName].data[id].outputs["output_".concat(numOutputs + 1)] = { connections: [] };
}
function removeNodeInput(context, id, input_class) {
    var _a;
    var moduleName = context.getModuleFromNodeId(id);
    var infoNode = context.getNodeFromId(id);
    if (context.module === moduleName) {
        (_a = context.container.querySelector("#node-".concat(id, " .inputs .input.").concat(input_class))) === null || _a === void 0 ? void 0 : _a.remove();
    }
    var removeInputs = [];
    infoNode.inputs[input_class].connections.forEach(function (connection) {
        removeInputs.push({
            id_output: connection.node,
            id: id,
            output_class: connection.input,
            input_class: input_class
        });
    });
    removeInputs.forEach(function (item) {
        context.removeSingleConnection(item.id_output, item.id, item.output_class, item.input_class);
    });
    delete context.drawflow.drawflow[moduleName].data[id].inputs[input_class];
    var connections = Object.values(context.drawflow.drawflow[moduleName].data[id].inputs);
    context.drawflow.drawflow[moduleName].data[id].inputs = {};
    var input_class_id = parseInt(input_class.slice(6), 10);
    var nodeUpdates = [];
    connections.forEach(function (item, index) {
        item.connections.forEach(function (conn) {
            nodeUpdates.push(conn);
        });
        context.drawflow.drawflow[moduleName].data[id].inputs["input_".concat(index + 1)] = item;
    });
    nodeUpdates = Array.from(new Set(nodeUpdates.map(function (e) { return JSON.stringify(e); }))).map(function (e) { return JSON.parse(e); });
    if (context.module === moduleName) {
        var eles = context.container.querySelectorAll("#node-".concat(id, " .inputs .input"));
        eles.forEach(function (item) {
            var inputClassName = findClassWithPrefix(item.classList, 'input_');
            if (!inputClassName) {
                return;
            }
            var id_class = parseInt(inputClassName.slice(6), 10);
            if (input_class_id < id_class) {
                item.classList.remove("input_".concat(id_class));
                item.classList.add("input_".concat(id_class - 1));
            }
        });
    }
    nodeUpdates.forEach(function (itemx) {
        context.drawflow.drawflow[moduleName].data[itemx.node].outputs[itemx.input].connections.forEach(function (itemz, index) {
            if (itemz.node === id) {
                var output_id = parseInt(itemz.output.slice(6), 10);
                if (input_class_id < output_id) {
                    if (context.module === moduleName) {
                        var ele = context.container.querySelector(".connection.node_in_node-".concat(id, ".node_out_node-").concat(itemx.node, ".").concat(itemx.input, ".input_").concat(output_id));
                        ele === null || ele === void 0 ? void 0 : ele.classList.remove("input_".concat(output_id));
                        ele === null || ele === void 0 ? void 0 : ele.classList.add("input_".concat(output_id - 1));
                    }
                    if (itemz.points) {
                        context.drawflow.drawflow[moduleName].data[itemx.node].outputs[itemx.input].connections[index] = {
                            node: itemz.node,
                            output: "input_".concat(output_id - 1),
                            points: itemz.points
                        };
                    }
                    else {
                        context.drawflow.drawflow[moduleName].data[itemx.node].outputs[itemx.input].connections[index] = {
                            node: itemz.node,
                            output: "input_".concat(output_id - 1)
                        };
                    }
                }
            }
        });
    });
    context.updateConnectionNodes("node-".concat(id));
}
function removeNodeOutput(context, id, output_class) {
    var _a;
    var moduleName = context.getModuleFromNodeId(id);
    var infoNode = context.getNodeFromId(id);
    if (context.module === moduleName) {
        (_a = context.container.querySelector("#node-".concat(id, " .outputs .output.").concat(output_class))) === null || _a === void 0 ? void 0 : _a.remove();
    }
    var removeOutputs = [];
    infoNode.outputs[output_class].connections.forEach(function (connection) {
        removeOutputs.push({
            id: id,
            id_input: connection.node,
            output_class: output_class,
            input_class: connection.output
        });
    });
    removeOutputs.forEach(function (item) {
        context.removeSingleConnection(item.id, item.id_input, item.output_class, item.input_class);
    });
    delete context.drawflow.drawflow[moduleName].data[id].outputs[output_class];
    var connections = Object.values(context.drawflow.drawflow[moduleName].data[id].outputs);
    context.drawflow.drawflow[moduleName].data[id].outputs = {};
    var output_class_id = parseInt(output_class.slice(7), 10);
    var nodeUpdates = [];
    connections.forEach(function (item, index) {
        item.connections.forEach(function (conn) {
            nodeUpdates.push({ node: conn.node, output: conn.output });
        });
        context.drawflow.drawflow[moduleName].data[id].outputs["output_".concat(index + 1)] = item;
    });
    nodeUpdates = Array.from(new Set(nodeUpdates.map(function (e) { return JSON.stringify(e); }))).map(function (e) { return JSON.parse(e); });
    if (context.module === moduleName) {
        var eles = context.container.querySelectorAll("#node-".concat(id, " .outputs .output"));
        eles.forEach(function (item) {
            var outputClassName = findClassWithPrefix(item.classList, 'output_');
            if (!outputClassName) {
                return;
            }
            var id_class = parseInt(outputClassName.slice(7), 10);
            if (output_class_id < id_class) {
                item.classList.remove("output_".concat(id_class));
                item.classList.add("output_".concat(id_class - 1));
            }
        });
    }
    nodeUpdates.forEach(function (itemx) {
        context.drawflow.drawflow[moduleName].data[itemx.node].inputs[itemx.output].connections.forEach(function (itemz, index) {
            if (itemz.node === id) {
                var input_id = parseInt(itemz.input.slice(7), 10);
                if (output_class_id < input_id) {
                    if (context.module === moduleName) {
                        var ele = context.container.querySelector(".connection.node_in_node-".concat(itemx.node, ".node_out_node-").concat(id, ".output_").concat(input_id, ".").concat(itemx.output));
                        ele === null || ele === void 0 ? void 0 : ele.classList.remove("output_".concat(input_id));
                        ele === null || ele === void 0 ? void 0 : ele.classList.add("output_".concat(input_id - 1));
                    }
                    if (itemz.points) {
                        context.drawflow.drawflow[moduleName].data[itemx.node].inputs[itemx.output].connections[index] = {
                            node: itemz.node,
                            input: "output_".concat(input_id - 1),
                            points: itemz.points
                        };
                    }
                    else {
                        context.drawflow.drawflow[moduleName].data[itemx.node].inputs[itemx.output].connections[index] = {
                            node: itemz.node,
                            input: "output_".concat(input_id - 1)
                        };
                    }
                }
            }
        });
    });
    context.updateConnectionNodes("node-".concat(id));
}
function removeNodeId(context, id) {
    var _a;
    context.removeConnectionNodeId(id.slice(5));
    var moduleName = context.getModuleFromNodeId(id.slice(5));
    if (context.module === moduleName) {
        (_a = context.container.querySelector("#".concat(id))) === null || _a === void 0 ? void 0 : _a.remove();
    }
    delete context.drawflow.drawflow[moduleName].data[id.slice(5)];
    context.dispatch('nodeRemoved', id.slice(5));
}

// CONCATENATED MODULE: ./src/core/modules.ts

function getModuleFromNodeId(context, id) {
    var nameModule;
    var editor = context.drawflow.drawflow;
    Object.keys(editor).forEach(function (moduleName) {
        Object.keys(editor[moduleName].data).forEach(function (node) {
            if (String(node) === String(id)) {
                nameModule = moduleName;
            }
        });
    });
    return nameModule;
}
function addModule(context, name) {
    context.drawflow.drawflow[name] = { data: {} };
    context.dispatch('moduleCreated', name);
}
function changeModule(context, name) {
    context.dispatch('moduleChanged', name);
    context.module = name;
    if (context.precanvas) {
        context.precanvas.innerHTML = '';
    }
    context.canvas_x = 0;
    context.canvas_y = 0;
    context.pos_x = 0;
    context.pos_y = 0;
    context.mouse_x = 0;
    context.mouse_y = 0;
    context.zoom = 1;
    context.zoom_last_value = 1;
    resetCanvasTransform(context);
    context.import(context.drawflow, false);
}
function removeModule(context, name) {
    if (context.module === name) {
        context.changeModule('Home');
    }
    delete context.drawflow.drawflow[name];
    context.dispatch('moduleRemoved', name);
}
function clearModuleSelected(context) {
    if (context.precanvas) {
        context.precanvas.innerHTML = '';
    }
    context.drawflow.drawflow[context.module] = { data: {} };
}
function clear(context) {
    if (context.precanvas) {
        context.precanvas.innerHTML = '';
    }
    context.drawflow = { drawflow: { Home: { data: {} } } };
}

// CONCATENATED MODULE: ./src/core/serialization.ts

function exportData(context) {
    var dataExport = JSON.parse(JSON.stringify(context.drawflow));
    context.dispatch('export', dataExport);
    return dataExport;
}
function importData(context, data, notify) {
    if (notify === void 0) { notify = true; }
    context.clear();
    context.drawflow = JSON.parse(JSON.stringify(data));
    context.load();
    refreshWirelessPorts(context);
    if (notify) {
        context.dispatch('import', 'import');
    }
}

// CONCATENATED MODULE: ./src/core/events.ts
function on(context, event, callback) {
    if (typeof callback !== 'function') {
        console.error("The listener callback must be a function, the given type is ".concat(typeof callback));
        return;
    }
    if (typeof event !== 'string') {
        console.error("The event name must be a string, the given type is ".concat(typeof event));
        return;
    }
    if (!context.events[event]) {
        context.events[event] = { listeners: [] };
    }
    context.events[event].listeners.push(callback);
}
function removeListener(context, event, callback) {
    var registry = context.events[event];
    if (!registry)
        return;
    var listeners = registry.listeners;
    var index = listeners.indexOf(callback);
    if (index > -1) {
        listeners.splice(index, 1);
    }
}
function dispatch(context, event, details) {
    var registry = context.events[event];
    if (!registry) {
        return;
    }
    registry.listeners.forEach(function (listener) { return listener(details); });
}

// CONCATENATED MODULE: ./src/core/zoom.ts

function zoom_enter(context, event) {
    if (event.ctrlKey) {
        event.preventDefault();
        if (event.deltaY > 0) {
            context.zoom_out();
        }
        else {
            context.zoom_in();
        }
    }
}
function zoom_refresh(context) {
    context.dispatch('zoom', context.zoom);
    context.canvas_x = (context.canvas_x / context.zoom_last_value) * context.zoom;
    context.canvas_y = (context.canvas_y / context.zoom_last_value) * context.zoom;
    context.zoom_last_value = context.zoom;
    applyStoredCanvasTranslation(context);
}
function zoom_in(context) {
    if (context.zoom < context.zoom_max) {
        context.zoom += context.zoom_value;
        context.zoom_refresh();
    }
}
function zoom_out(context) {
    if (context.zoom > context.zoom_min) {
        context.zoom -= context.zoom_value;
        context.zoom_refresh();
    }
}
function zoom_reset(context) {
    if (context.zoom !== 1) {
        context.zoom = 1;
        context.zoom_refresh();
    }
}
function zoom_fit(context, paddingTop, paddingRight, paddingBottom, paddingLeft) {
    if (paddingTop === void 0) { paddingTop = 0; }
    if (paddingRight === void 0) { paddingRight = 0; }
    if (paddingBottom === void 0) { paddingBottom = 0; }
    if (paddingLeft === void 0) { paddingLeft = 0; }
    if (!context.precanvas)
        return;
    var nodes = Array.from(context.precanvas.querySelectorAll('.drawflow-node'));
    if (nodes.length === 0)
        return;
    var bounds = nodes.reduce(function (acc, node) {
        var left = node.offsetLeft;
        var top = node.offsetTop;
        var right = left + node.offsetWidth;
        var bottom = top + node.offsetHeight;
        return {
            minX: Math.min(acc.minX, left),
            minY: Math.min(acc.minY, top),
            maxX: Math.max(acc.maxX, right),
            maxY: Math.max(acc.maxY, bottom)
        };
    }, { minX: Number.POSITIVE_INFINITY, minY: Number.POSITIVE_INFINITY, maxX: Number.NEGATIVE_INFINITY, maxY: Number.NEGATIVE_INFINITY });
    var contentWidth = Math.max(bounds.maxX - bounds.minX, 1);
    var contentHeight = Math.max(bounds.maxY - bounds.minY, 1);
    var containerWidth = context.container.clientWidth;
    var containerHeight = context.container.clientHeight;
    var normalizedPaddingTop = Math.max(0, paddingTop);
    var normalizedPaddingRight = Math.max(0, paddingRight);
    var normalizedPaddingBottom = Math.max(0, paddingBottom);
    var normalizedPaddingLeft = Math.max(0, paddingLeft);
    var availableWidth = Math.max(containerWidth - (normalizedPaddingLeft + normalizedPaddingRight), 0);
    var availableHeight = Math.max(containerHeight - (normalizedPaddingTop + normalizedPaddingBottom), 0);
    if (availableWidth === 0 || availableHeight === 0)
        return;
    var scaleX = availableWidth / contentWidth;
    var scaleY = availableHeight / contentHeight;
    var newZoom = Math.min(Math.max(Math.min(scaleX, scaleY), context.zoom_min), context.zoom_max);
    var contentCenterX = bounds.minX + contentWidth / 2;
    var contentCenterY = bounds.minY + contentHeight / 2;
    var paddedCenterX = containerWidth / 2;
    var paddedCenterY = containerHeight / 2;
    context.zoom = newZoom;
    context.zoom_last_value = newZoom;
    var panShiftX = ((paddedCenterX - contentCenterX) * newZoom) + ((normalizedPaddingLeft - normalizedPaddingRight) / 2);
    var panShiftY = ((paddedCenterY - contentCenterY) * newZoom) + ((normalizedPaddingTop - normalizedPaddingBottom) / 2);
    context.dispatch('zoom', context.zoom);
    setCanvasTranslation(context, panShiftX, panShiftY);
}

// CONCATENATED MODULE: ./src/core/Drawflow.ts







var Drawflow_Drawflow = /** @class */ (function () {
    function Drawflow(container, render, parent) {
        if (render === void 0) { render = null; }
        if (parent === void 0) { parent = null; }
        var _this = this;
        this.start = function () { return start(_this); };
        this.pointerdown_handler = function (ev) { return pointerdown_handler(_this, ev); };
        this.pointermove_handler = function (ev) { return pointermove_handler(_this, ev); };
        this.pointerup_handler = function (ev) { return pointerup_handler(_this, ev); };
        this.remove_event = function (ev) { return remove_event(_this, ev); };
        this.load = function () { return load(_this); };
        this.removeReouteConnectionSelected = function () { return removeReouteConnectionSelected(_this); };
        this.click = function (e) { return click(_this, e); };
        this.position = function (e) { return position(_this, e); };
        this.dragEnd = function (e) { return dragEnd(_this, e); };
        this.contextmenu = function (e) { return contextmenu(_this, e); };
        this.contextmenuDel = function () { return contextmenuDel(_this); };
        this.key = function (e) { return key(_this, e); };
        this.zoom_enter = function (event) { return zoom_enter(_this, event); };
        this.zoom_refresh = function () { return zoom_refresh(_this); };
        this.zoom_in = function () { return zoom_in(_this); };
        this.zoom_out = function () { return zoom_out(_this); };
        this.zoom_reset = function () { return zoom_reset(_this); };
        this.zoom_fit = function (paddingTop, paddingRight, paddingBottom, paddingLeft) {
            if (paddingTop === void 0) { paddingTop = 0; }
            if (paddingRight === void 0) { paddingRight = 0; }
            if (paddingBottom === void 0) { paddingBottom = 0; }
            if (paddingLeft === void 0) { paddingLeft = 0; }
            return zoom_fit(_this, paddingTop, paddingRight, paddingBottom, paddingLeft);
        };
        this.createCurvature = function (start_pos_x, start_pos_y, end_pos_x, end_pos_y, curvature_value, type) { return createCurvature(_this, start_pos_x, start_pos_y, end_pos_x, end_pos_y, curvature_value, type); };
        this.drawConnection = function (ele) { return drawConnection(_this, ele); };
        this.updateConnection = function (eX, eY) { return updateConnection(_this, eX, eY); };
        this.addConnection = function (id_output, id_input, output_class, input_class, options) {
            if (options === void 0) { options = {}; }
            return addConnection(_this, id_output, id_input, output_class, input_class, options);
        };
        this.updateConnectionNodes = function (id) { return updateConnectionNodes(_this, id); };
        this.dblclick = function (e) { return dblclick(_this, e); };
        this.createReroutePoint = function (ele) { return createReroutePoint(_this, ele); };
        this.removeReroutePoint = function (ele) { return removeReroutePoint(_this, ele); };
        this.addRerouteImport = function (dataNode) { return addRerouteImport(_this, dataNode); };
        this.removeConnection = function () { return removeConnection(_this); };
        this.removeSingleConnection = function (id_output, id_input, output_class, input_class) {
            return removeSingleConnection(_this, id_output, id_input, output_class, input_class);
        };
        this.removeConnectionNodeId = function (id) { return removeConnectionNodeId(_this, id); };
        this.registerNode = function (name, html, props, options) {
            if (props === void 0) { props = null; }
            if (options === void 0) { options = null; }
            return registerNode(_this, name, html, props, options);
        };
        this.getNodeFromId = function (id) { return getNodeFromId(_this, id); };
        this.getNodesFromName = function (name) { return getNodesFromName(_this, name); };
        this.addNode = function (name, num_in, num_out, ele_pos_x, ele_pos_y, classoverride, data, html, typenode) {
            if (typenode === void 0) { typenode = false; }
            return addNode(_this, name, num_in, num_out, ele_pos_x, ele_pos_y, classoverride, data, html, typenode);
        };
        this.addNodeImport = function (dataNode, precanvas) { return addNodeImport(_this, dataNode, precanvas); };
        this.updateNodeValue = function (event) { return updateNodeValue(_this, event); };
        this.updateNodeDataFromId = function (id, data) { return updateNodeDataFromId(_this, id, data); };
        this.addNodeInput = function (id) { return addNodeInput(_this, id); };
        this.addNodeOutput = function (id) { return addNodeOutput(_this, id); };
        this.removeNodeInput = function (id, input_class) { return removeNodeInput(_this, id, input_class); };
        this.removeNodeOutput = function (id, output_class) { return removeNodeOutput(_this, id, output_class); };
        this.removeNodeId = function (id) { return removeNodeId(_this, id); };
        this.getModuleFromNodeId = function (id) { return getModuleFromNodeId(_this, id); };
        this.addModule = function (name) { return addModule(_this, name); };
        this.changeModule = function (name) { return changeModule(_this, name); };
        this.removeModule = function (name) { return removeModule(_this, name); };
        this.clearModuleSelected = function () { return clearModuleSelected(_this); };
        this.clear = function () { return clear(_this); };
        this.export = function () { return exportData(_this); };
        this.import = function (data, notify) {
            if (notify === void 0) { notify = true; }
            return importData(_this, data, notify);
        };
        this.on = function (eventName, callback) { return on(_this, eventName, callback); };
        this.removeListener = function (eventName, callback) {
            return removeListener(_this, eventName, callback);
        };
        this.dispatch = function (eventName, payload) { return dispatch(_this, eventName, payload); };
        this.getUuid = function () {
            var s = [];
            var hexDigits = '0123456789abcdef';
            for (var i = 0; i < 36; i += 1) {
                s[i] = hexDigits.substr(Math.floor(Math.random() * 0x10), 1);
            }
            s[14] = '4';
            s[19] = hexDigits.substr((parseInt(s[19], 16) & 0x3) | 0x8, 1);
            s[8] = s[13] = s[18] = s[23] = '-';
            return s.join('');
        };
        this.events = {};
        this.container = container;
        this.precanvas = null;
        this.nodeId = 1;
        this.ele_selected = null;
        this.node_selected = null;
        this.drag = false;
        this.reroute = false;
        this.reroute_fix_curvature = false;
        this.curvature = 0.5;
        this.reroute_curvature_start_end = 0.5;
        this.reroute_curvature = 0.5;
        this.reroute_width = 6;
        this.drag_point = false;
        this.editor_selected = false;
        this.connection = false;
        this.connection_ele = null;
        this.connection_selected = null;
        this.canvas_x = 0;
        this.canvas_y = 0;
        this.pos_x = 0;
        this.pos_x_start = 0;
        this.pos_y = 0;
        this.pos_y_start = 0;
        this.mouse_x = 0;
        this.mouse_y = 0;
        this.line_path = 5;
        this.first_click = null;
        this.force_first_input = false;
        this.draggable_inputs = true;
        this.useuuid = false;
        this.parent = parent;
        this.noderegister = {};
        this.render = render;
        this.drawflow = { drawflow: { Home: { data: {} } } };
        this.module = 'Home';
        this.editor_mode = 'edit';
        this.zoom = 1;
        this.zoom_max = 1.6;
        this.zoom_min = 0.5;
        this.zoom_value = 0.1;
        this.zoom_last_value = 1;
        this.evCache = [];
        this.prevDiff = -1;
        this.autoPanEdgeMargin = 40;
        this.autoPanSpeed = 12;
        this.autoPanPointerX = 0;
        this.autoPanPointerY = 0;
        this.autoPanFrame = null;
        this.autoPanMode = null;
        this.pending_wireless = null;
    }
    return Drawflow;
}());
/* harmony default export */ var core_Drawflow = (Drawflow_Drawflow);

// CONCATENATED MODULE: ./src/core/types.ts


// CONCATENATED MODULE: ./src/index.ts




/***/ })
/******/ ])["default"];
});